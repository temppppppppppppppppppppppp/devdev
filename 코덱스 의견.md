# 코덱스 의견

작성일: 2026-02-13
기준 문서: `참고자료.md`, `CLAUDE.md`
비교 대상: 현재 워크스페이스(`C:\Users\wjjo\Desktop\글도비`)

## 1) 현재 상태 비교 요약
- 기준 문서와 현재 코드 상태는 큰 방향은 일치합니다.
- 다만 일부 항목은 문서 내부 시점이 섞여 있어(예: 6-A/6-C 미착수 표기와 완료 표기 공존) 최신 상태 기준으로 정규화가 필요합니다.
- 최신 판단 기준은 `CLAUDE.md` + 실제 파일 존재/코드 스캔 결과로 잡는 것이 안전합니다.

## 2) 완료된 작업 (비교 후 확인)
- Phase 1: logging 전환/except 정비 (문서 완료 표기)
- Phase 1.5: 레거시 일부 정리 (writer 보류, 일부 파일 삭제) (문서 완료 표기)
- Phase 2-B: Type hints 95.5% (문서 완료 표기)
- Phase 5-A: 프롬프트 YAML 외부화 (문서 완료 표기)
- Phase 5-C: 의존성/인프라 정비
  - 확인: `requirements.txt`, `.gitignore`, `pyproject.toml` 존재
- Phase 6-C: 코드 품질 자동화
  - 확인: `.pre-commit-config.yaml` 존재, `pyproject.toml`에 ruff/pytest 설정 존재
- Phase 6-A: 테스트 도입
  - 확인: `tests/` 존재, `test_*.py` 27개 확인 (문서의 "63개"는 케이스/함수 기준일 가능성 큼)
- 추가로 이번 세션 완료:
  - `CX_조사_modules.md` 생성(116개 파일 정적 조사, High 3 / Medium 11 / Low 102)

## 3) 아직 필요한 작업 (문서 대비 현재 미완)
- Phase 5-A': 사용처 import를 `PromptLoader.load()`로 전환
  - 현재 상태: 미착수에 가까움
  - 근거: `modules/domain/agents/*`에서 `_prompts import` 다수 잔존
- Phase 5-B / 5-B': 설정 관리 + 장르/작품 가드 외부화
- Phase 2-A: Pydantic 모델 도입
- Phase 2.5: `ChromaDB -> sqlite-vec` 전환
  - 근거: `memory_engine.py`, `blueprint_memory.py`에 `chromadb` 의존 잔존
- Phase 3: 검증 강화/NPC 이력/수정 모드/안정화
- Phase 4: 구조 리팩토링(God Object 분해, 파일 분할, async 정리)

## 4) 현재 리스크 포인트 (비교 + 코드 실측)
- God Object 의존성은 여전히 매우 큼
  - 실측: `stage2_orchestrator.py` `self.app.` 341회, `stage4_orchestrator.py` 312회
  - 문서 수치(332/291)보다 증가한 상태
- 벡터 계층은 구조적으로 미완
  - `memory_engine.py`/`blueprint_memory.py`의 `chromadb` 의존이 남아 있어 2.5 전환 전까지 일관성 리스크 지속
- Prompt 외부화는 "데이터(YAML) 완료, 호출부 전환 미완" 상태

## 5) 전체 로드맵 (실행 우선순위)
1. [SAFE] Phase 5-A' 완료
- 목표: `_prompts import` 제거, 호출부를 `PromptLoader.load()`로 전환
- 완료 기준: `_prompts import` 0건, 동일 출력 스냅샷 비교 통과

2. [SAFE] Phase 5-B/5-B' 시작
- 목표: 설정/가드 YAML 구조 도입(장르 공통 + 작품 오버레이)
- 완료 기준: 최소 1개 장르+1개 작품에서 코드 수정 없이 설정 교체 가능

3. [RISKY] Phase 2-A (Pydantic)
- 목표: 핵심 입출력(dict)을 모델로 승격, ValidationError 표면화
- 완료 기준: Stage2/4 핵심 경로 모델 검증 통과, 실패는 명시적 에러로 수집

4. [RISKY] Phase 2.5 (sqlite-vec)
- 목표: 벡터 저장/검색을 SQLite SSOT로 통합
- 완료 기준: ChromaDB import 제거, `memory_engine/blueprint_memory/semantic_plot_guard` 경로 통합

5. [RISKY] Phase 3 (검증/안정화)
- 목표: NPC 이력, 관계 일관성, 수정 모드, 검증 실패 가시화
- 완료 기준: 핵심 실패 시나리오(문서의 NPC 시나리오군) 재현 테스트 통과

6. [RISKY] Phase 4 (구조 리팩토링)
- 목표: `self.app` 의존 분해(DI), 대형 파일 분할, async/Thread 모델 정리
- 완료 기준: stage2/stage4 분할 후 회귀 테스트 통과, `self.app` 참조 대폭 감소

7. [RISKY] Phase 6-B (E2E)
- 목표: 실제 파이프라인 통합 검증
- 완료 기준: 소규모 시나리오(1~2화) 안정 통과 후 범위 확장

## 6) 즉시 실행 권장 (다음 1개 세션)
- 1순위: Phase 5-A'만 집중 실행
- 산출물:
  - 전환 대상 파일 목록
  - 파일별 before/after import 매핑
  - 전환 후 smoke 검증 체크리스트

---
결론: 현재 프로젝트는 "기반 정비(1/1.5/2-B/5-A/5-C/6-A/6-C) 완료, 핵심 구조 변경(2-A/2.5/3/4) 대기" 상태입니다. 다음 최적 스텝은 리스크가 낮고 파급이 큰 `5-A'` 선완료입니다.

## 7) Phase 5-A' 파일별 변환 작업표 (즉시 실행용)
목표: `_prompts` 직접 import를 제거하고, 호출부를 `PromptLoader.load()`/`get_raw()`로 통일.

### 7-1. 1차 전환 대상 (직접 import 확인된 파일)
1. `modules/domain/agents/analyst.py`
- 현재 import: `analyst_prompts`
- 사용 심볼: `POST_STITCH_REPAIR_PROMPT`, `ENRICH_BLOCK_PROMPT_V30`, `PLAN_VOLUME_PROMPT_V25`, `PLAN_ARC_PROMPT_V25`, `ANALYST_SELF_CRITIC_PROMPT`, `get_recovery_prompt`, `get_volume_strategy_prompt`, `get_surgery_prompt`, `get_calibration_prompt`
- 전환 방식:
  - 상수형 프롬프트: `loader.load("analyst", "<KEY>", **kwargs)`로 치환
  - 함수형(`get_*_prompt`)은 `analyst.yaml`에 키 추가 후 `loader.load(...)`로 대체

2. `modules/domain/agents/arc_ensemble.py`
- 현재 import: `ensemble_prompts`
- 사용 심볼: `ENSEMBLE_ARC_PROMPT`
- 전환 방식: `loader.load("ensemble", "ENSEMBLE_ARC_PROMPT", **kwargs)`

3. `modules/domain/agents/blueprint_ensemble.py`
- 현재 import: `ensemble_prompts`
- 사용 심볼: `BLUEPRINT_GENERATION_PROMPT`
- 전환 방식: `loader.load("ensemble", "BLUEPRINT_GENERATION_PROMPT", **kwargs)`

4. `modules/domain/agents/chief_writer.py`
- 현재 import: `chief_writer_prompts`
- 사용 심볼: `PROMPT_TEMPLATE_OUTPUT`, `COMMON_RULES_SECTION`, `WRITING_GUIDELINES_SECTION`, `PRIMITIVE_CONSTRAINT_FALLBACK`, `MODERN_ORIGIN_SECTION`, `build_chief_writer_main_prompt`, `get_fix_issues_prompt`, `get_anti_trope_instructions`
- 전환 방식:
  - 상수형 5개: `loader.load("chief_writer", "<KEY>", **kwargs)`
  - 함수형 3개는 키 분해 후 YAML 조합 템플릿으로 전환

5. `modules/domain/agents/director_ensemble.py`
- 현재 import: `director_prompts`
- 사용 심볼: `ENSEMBLE_SELECTION_PROMPT`
- 전환 방식: `loader.load("director", "ENSEMBLE_SELECTION_PROMPT", **kwargs)`

6. `modules/domain/agents/director_continuity.py`
- 현재 import: `director_prompts`
- 사용 심볼: `MANUSCRIPT_HISTORY_CONFLICT_PROMPT`
- 전환 방식: `loader.load("director", "MANUSCRIPT_HISTORY_CONFLICT_PROMPT", **kwargs)`

7. `modules/domain/agents/director_auditor.py`
- 현재 import: `director_prompts`
- 사용 심볼: `STRATEGIC_AUDIT_PROMPT_V30`, `DIRECTOR_AUDIT_PROMPT_V30`
- 전환 방식: `loader.load("director", "<KEY>", **kwargs)`

### 7-2. 구현 규칙 (공통)
1. 각 파일 상단에 `from modules.core.prompt_loader import PromptLoader` 추가.
2. 클래스 초기화 시 `self.prompt_loader = PromptLoader()` 1회만 생성.
3. 기존 `.format(...)`/f-string 조합은 `load(..., **kwargs)`로 흡수.
4. `load()`가 `None` 반환할 때는 기존 상수 문자열(임시 fallback) 유지 후 경고 로그 남김.
5. 함수형 프롬프트 helper 제거는 2단계로 분리:
- 1단계: helper 내부에서 `PromptLoader` 호출하도록 래핑
- 2단계: 호출부 직접 전환 후 helper 삭제

### 7-3. 작업 순서 (한 세션 기준)
1. `arc_ensemble.py`, `blueprint_ensemble.py`, `director_ensemble.py` 전환
- 이유: 상수 1개 치환형이라 난이도 최저
2. `director_continuity.py`, `director_auditor.py` 전환
- 이유: director 도메인 묶음 검증 가능
3. `analyst.py` 전환
- 이유: 사용 심볼 다수, 영향도 큼
4. `chief_writer.py` 전환
- 이유: 함수형 프롬프트 포함, 난이도 최고

### 7-4. 완료 기준 (DoD)
1. `modules/domain/agents` 내 `_prompts import` 0건.
2. 대상 7파일에서 프롬프트 로딩 경로가 `PromptLoader`로 단일화.
3. 각 파일에서 `load()==None` fallback 경고 로그 지점 확인.
4. 정적 점검:
- `rg -n "_prompts import|import .*_prompts" modules/domain/agents` 결과 0
- `rg -n "PromptLoader\\(" modules/domain/agents` 결과 대상 파일 모두 포함

### 7-5. 실행 후 체크리스트
1. import 에러 없는지 (`python -m compileall modules/domain/agents`)
2. prompt key 오타 없는지 (`PromptLoader().list_keys("analyst|director|ensemble|chief_writer")` 대조)
3. 문서 동기화
- `참고자료.md`, `CLAUDE.md`의 5-A' 상태를 `진행중/완료`로 갱신
