# Manual Ops

`expand_ep15.py` in `tools2/` is a one-off operator utility for live-network/manual content generation.

It is not part of the production runtime, API/Desktop bridge, or CI/pytest contract.
