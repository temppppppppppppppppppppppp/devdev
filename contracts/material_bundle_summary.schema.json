{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "material_bundle_summary.schema.json",
  "title": "Material Bundle Summary",
  "description": "Stage 0 material collection summary: events, NPCs, crises, terms, scenes.",
  "type": "object",
  "required": [
    "events",
    "npc_candidates",
    "crisis_candidates",
    "terms",
    "scene_details",
    "notes"
  ],
  "properties": {
    "events": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Key plot events extracted from source materials."
    },
    "npc_candidates": {
      "type": "array",
      "items": { "type": "string" },
      "description": "NPC candidates with role summaries."
    },
    "crisis_candidates": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Crisis/conflict candidates from materials."
    },
    "terms": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Domain-specific terminology collected."
    },
    "scene_details": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Scene-level details ready for Phase 0 transfer."
    },
    "notes": {
      "type": "string",
      "description": "Free-text notes on material collection status."
    },
    "opening_bundle_contract": {
      "type": "object",
      "description": "Executable opening pacing contract for the first TR bundle (typically TR 2~6).",
      "required": [
        "bundle_window",
        "macro_battlefield",
        "macro_battlefield_map",
        "bundle_goal",
        "first_signboard_block",
        "representative_reevaluation_block",
        "next_battlefield_ticket_block",
        "timing_reconciliation_note"
      ],
      "properties": {
        "bundle_window": {
          "type": "string",
          "description": "Locked reader-earning evidence window, such as 'TR 2~6'."
        },
        "macro_battlefield": {
          "type": "string",
          "description": "Primary opening battlefield that the reader perceives as the first campaign zone."
        },
        "macro_battlefield_map": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Named sub-arenas or lanes that explain how the opening battlefield changes within the bundle."
        },
        "bundle_goal": {
          "type": "string",
          "description": "Concrete opening bundle payoff goal that the TR 2~6 window must earn."
        },
        "first_signboard_block": {
          "type": "integer",
          "description": "First block inside the opening bundle where public signboard evidence lands."
        },
        "representative_reevaluation_block": {
          "type": "integer",
          "description": "First block inside the opening bundle where representative reevaluation lands."
        },
        "next_battlefield_ticket_block": {
          "type": "integer",
          "description": "First block inside the opening bundle where the next battlefield ticket lands."
        },
        "timing_reconciliation_note": {
          "type": "string",
          "description": "Planner note reconciling Phase0 arc scale with the faster TR 2~6 reader-earning window."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
