{
  "cutover_rules": {
    "cutover_date": "2026-03-12",
    "grace_period_days": 30,
    "grace_expiry": "2026-04-11",
    "rules": {
      "existing_works": "run_class가 sequential_production으로 전환되는 시점부터 JSON 필수",
      "new_works": "phase0_ready_snapshot.manual_audit_pass == true 시점부터 JSON 필수",
      "grace_period": "cutover 이후 30일간 md 병행 허용, 이후 JSON-only",
      "read_priority": "JSON → (유예 기간 내) md fallback → (유예 만료) status_missing 처리"
    }
  },
  "quality_gates": {
    "stage0_gate": "4 artifacts present + manual_audit_pass == true + UTF-8 parse OK",
    "planning_gate": "stage0_gate PASS + stage0_handoff_validator.py exit 0",
    "production_gate": "phase0_design exists + UTF-8 parse OK",
    "bi_gate": "tr_block_070_draft exists + source TR audit snapshot PASS",
    "completion_gate": "BI exists + 5-pass audit PASS"
  }
}
