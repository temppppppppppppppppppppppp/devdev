<!-- [참고자료] -->
Interactive Prompt Contract Refresh Evidence
Date: 2026-03-15
Status: raw-evidence

Topic slug: interactive-prompt-contract-refresh

1. Fresh live-run transcript evidence
- Source: user-supplied `python main_a.py` terminal transcript in the current task thread.
- Observed operator issue A:
  - menu `7` no longer asks the initial Arc-count question after Frontier Lag starts.
  - transcript shows `Choice: 7` -> `FrontierLag` header -> immediate `auto-selected default batch_size: 3`.
- Observed operator issue B:
  - the genre-selection prompt and project-selection prompt are visibly printed twice before the typed answer.

2. Live workspace code evidence
- `main_a.py:4188-4201`
  - menu `7` normal path computes `default_count = min(remaining_design, 3)`
  - when `batch_size_override` is absent it logs `auto-selected default batch_size`
  - no `_get_int_input(...)` prompt exists on this normal path
- `main_a.py:4467-4475`
  - menu `6` still has the older one-time initial Arc-count prompt
  - this is the closest reusable interactive pattern for restoring a one-time Frontier Lag prompt
- `modules/core/studio_visualizer.py:133-145`
  - `prompt()` first calls `self.log(prompt_text, ...)`
  - then calls `self.console.input(prompt_text)`
  - this guarantees duplicate operator-visible prompt rendering on the console surface
- `modules/core/services/ui_service.py:187-205`
  - `get_int_input()` calls `_prompt(prompt, ...)`
  - after the response is parsed it calls `_selection(prompt, value, visible=False)`
  - because the selection label is the raw prompt text, hidden sink/log output repeats the same prompt string again

3. Fresh session-log evidence
- `logs/session_20260315_123149.log:36-38`
  - the genre prompt string appears three times with identical message text
- `logs/session_20260315_123149.log:52`
- `logs/session_20260315_123149.log:54`
- `logs/session_20260315_123149.log:56`
  - the project prompt string `👉 Choice:` appears three times with identical message text
- Interpretation:
  - console duplication is driven by `StudioVisualizer.prompt()`
  - session-log triplication is driven by `prompt` + `prompt_response` + `selection` all reusing the same prompt text

4. Predecessor document evidence
- `docs/2026-03-14/frontier-lag-nonstop-contract-remediation-execution-ssot.md:80-92`
  - prior closed contract explicitly locked menu `7` normal path to no initial Arc-count question
  - default policy became `min(remaining_design, 3)` with no operator prompt
- This predecessor is accurate for the previously implemented contract, but it now conflicts with the fresh operator preference observed in the live run

5. Side-effect surfaces implicated
- console / operator UX:
  - duplicate prompt rendering is directly user-visible
- session logs / UI logs:
  - repeated identical prompt strings inflate logs and complicate post-run audit reading
- operator event sink / ui_events DB mirror:
  - prompt, prompt_response, and selection events currently share overlapping message text
- tests / harness semantics:
  - Frontier Lag tests currently assert zero normal-path `_get_int_input(...)` calls
  - prompt-surface tests do not currently lock the one-render-per-prompt rule

6. Excluded from this evidence bundle
- the fresh live-run shutdown race after Stage 2 Analyst call
- closed-database write failures during shutdown
- encoding / mojibake concerns outside prompt duplication
