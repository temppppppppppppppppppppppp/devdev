# codebase-global-log-evidence-merged Uncertainty And Contradiction Ledger

Date: 2026-03-15
Status: final
Canonical Path: `docs/2026-03-15/codebase-global-log-evidence-merged-uncertainty-contradiction-ledger.md`

## Contradictions

| ID | Claim Area | Conflicting Evidence | Current Interpretation | Confidence Impact | Closure Action | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `C-01` | FrontierLag menu `7` contract | older March 15 partial run evidence showed a one-time prompt, while the latest secured run logs `auto-selected default batch_size: 3` with no prompt replay and `15일.txt` shows the same no-input auto batch plus auto-continue continuation | the latest secured runtime evidence is authoritative; menu `7` no-input auto batch is the current live behavior for the CLI path | closed in favor of latest runtime evidence | keep old prompt-related docs as predecessor history only | closed |
| `C-02` | Proof-digest contract vs observed timing | `runtime_audit_summary.json` declares `proof_digest_truth_scope=committed_persistence_only`, but summary timestamp is `17:24:09` while `pass_rate_monitor.json` updates at `17:24:25.933860` and `llm_io.jsonl` continues to `17:24:58` | summary content is authoritative only for the durable state visible at its own timestamp; current finalization point is too early for full-run truth | action-bearing | move summary generation behind a true quiescent point and test it | open |
| `C-03` | Structured sinks vs visible mojibake | `ui_events.jsonl`, `episode_production.jsonl`, and DB reads are UTF-8-clean, while active source files and cp949 shell output still show corruption or unsafe rendering | corruption is not one global sink failure; it is split across source text, tool emission, and shell-host rendering boundaries | bounded; action-bearing | repair scoped source corruption and make tooling output shell-safe | open |
| `C-04` | Stage 4 mismatch counts | latest Stage 4 rows for ep10/ep11 look coherent, but summary still reports `selection_reason_mismatches=2` and `verdict_reason_mismatches=2` | mismatch table localizes the issue to attempt keys `s4:ep4:arc1:a1:20260315_144741` and `s4:ep5:arc2:a1:20260315_144741`; the defect is real but bounded | closed to exact keys; remains action-bearing | close the exact sink-normalization path for those fields | closed |
| `C-05` | Session identity | plain log filename token is `20260315_144654`, while structured UI sinks carry `session_id=20260315_144741` | sink identity is not normalized across all surfaces today; current post-run analysis must treat the plain log token and structured session id as linked but distinct | action-bearing | define and test one lineage contract across plain log, DB, JSONL, and summary sinks | open |
| `C-06` | Desktop/control-plane severity | source-only desktop survey found readiness/reconnect/prompt concurrency gaps, but the secured runtime evidence is CLI-only | keep the desktop lane active and high-priority, but do not overstate it as runtime-proven in this bundle | bounded; still action-bearing | keep the lane source-led until a fresh desktop run is captured | open |
| `C-07` | Artifact truth vs stored hash lineage | every `stage_attempts.content_hash` and every linked `episode_production` `content_hash` / `selection_content_hash` mismatches the current bytes on disk, while the artifact files themselves exist and decode cleanly | the files are durable, but the stored integrity metadata is systematically stale and cannot currently prove artifact truth | action-bearing | move artifact hash capture to the durable final write point and lock it with DB/JSONL regression tests | open |
| `C-08` | App shutdown completion vs process teardown | `15일.txt` shows `[System] DB 연결 안전하게 해제됨` and `종료 완료`, but Python then emits `threading/_python_exit -> KeyboardInterrupt` and `BaseEventLoop.__del__ -> AttributeError` | current shutdown messages are only app-level completion, not guaranteed process-level quiescence | action-bearing | fold teardown quiescence into the persistence/finalization lane and verify no post-completion exceptions remain | open |

## Uncertainty

| ID | Topic | Missing Proof | Why It Matters | Temporary Bound | Confidence Impact | Closure Action | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `U-01` | Fresh desktop runtime proof | no Electron/Desktop fresh run was captured with logs in this bundle | backend-front risk is currently source-led rather than runtime-proven | keep desktop claims architectural and contract-based | bounded only | capture a desktop live-merge run after the first two remediation lanes | open |
| `U-02` | Exact late-write call chain | plain log proves late `save_ui_event` and `save_llm_call` after DB close, but the exact callback chain is not yet instrumented end-to-end | implementation could fix the symptom without closing the true lifecycle root cause | bounded only | add targeted instrumentation/tests while closing the persistence lane | open |
| `U-03` | Plain session log readability | direct UTF-8 file reads are cleaner than shell render output, but not every operator surface was replayed outside the current PowerShell host | the remediation should not overcorrect a file-encoding surface that is actually shell-host-specific | bounded only | keep runtime log checks split between file encoding and shell-safe display | open |
| `U-04` | Bundle-wide dirty workspace noise | unrelated modified and deleted files remain in the workspace | they can distract queue review or future patching if not kept out of authority | bounded only | keep claim authority tied to the included survey scope and selected runtime artifacts | open |
| `U-05` | Exact stale-hash capture point | artifact-truth evidence proves systematic hash drift, but the specific write path that stamps stale hashes before/after final file mutation is not yet directly traced end-to-end | implementation could normalize one sink while leaving the real write-order defect alive in another | bounded only | instrument and regression-test the exact `content_hash` / `selection_content_hash` capture point during persistence-lane realization | open |
