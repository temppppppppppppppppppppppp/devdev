# 파이프라인 효율성 감사 결과

수행 방식: 코드 읽기 전용 수동 감사 (코드 수정 없음)  
점검 범위: `main_a.py`, `modules/domain/agents/*.py`, `modules/core/*.py`, `modules/validation/*.py`, `config/settings/validation.yaml`, `config/prompts/*.yaml`

## 요약
- Dead Config: 4건
- Information Bottleneck: 6건 (이전 `docs/컨텍스트_활용_조사_결과.md`에 없는 신규 손실만 기재)
- Broken Wiring: 12건

## 1. Dead Config 목록 (심각도순)

### [HIGH] `config/settings/validation.yaml:13`~`config/settings/validation.yaml:16` — `manuscript.*` 임계값 미사용
- 정의: `manuscript.min_length/warning_length/target_length/max_length`
- 근거: `_threshold("manuscript.*", ...)` 호출 없음 (`modules` 전수 검색)
- 실제 분기: 원고 길이 판정은 상수 기반(`modules/core/constants.py:90`) 사용 경로가 다수
- 영향: YAML 수정이 실제 심사/집필 로직에 반영되지 않음 (운영자 관점 설정 무효)

### [MEDIUM] `modules/validation/validation_orchestrator.py:194` — `use_parallel_validation` 미반영
- 정의: `self.use_parallel_validation = config.get("use_parallel_validation", True)`
- 근거: 해당 필드 참조/분기 없음 (동파일 검색)
- 실제 동작: 병렬 검증은 항상 실행 (`modules/validation/validation_orchestrator.py:1017`)
- 영향: 병렬 검증 ON/OFF 제어 불가

### [MEDIUM] `modules/domain/agents/base_agent.py:210` / `modules/domain/agents/base_agent.py:222` / `modules/domain/agents/base_agent.py:223` — `enable_cascade`, `cascade` 미사용
- 정의: 생성자 인자/속성으로 보관
- 근거: 프로젝트 전역 참조 없음 (`enable_cascade`, `self.cascade` 검색 결과 정의부만 존재)
- 영향: 모델 캐스케이드 관련 설계 흔적만 있고 런타임 경로 부재

### [LOW] `config/prompts/*.yaml` 다수 — PromptLoader 로딩 경로 미연결
- 근거 파일 예시:
`config/prompts/chain_of_verification.yaml:1`,
`config/prompts/tree_of_thoughts.yaml:1`,
`config/prompts/multi_agent_deliberation.yaml:1`,
`config/prompts/adversarial_self_play.yaml:1`,
`config/prompts/cross_agent_verifier.yaml:1`,
`config/prompts/director_auditor.yaml:1`
- 근거: 실제 `PromptLoader.load()` 호출 도메인은 소수(`analyst`, `chief_writer`, `arc_generator`, `blueprint_generator`, `ensemble`, `director`, `director_continuity`)에 집중
  - 호출 근거: `modules/domain/agents/analyst_prompt_api.py:11`, `modules/domain/agents/chief_writer_prompts.py:82`, `modules/domain/agents/arc_ensemble.py:352`, `modules/domain/agents/blueprint_ensemble.py:342`, `modules/domain/agents/director_ensemble.py:342`, `modules/domain/agents/director_continuity.py:387`
- 영향: 프롬프트 파일 유지비 대비 실사용 불명확 (레거시/이행중 자산 가능성 높음)

## 2. Information Bottleneck 목록 (심각도순)

### [HIGH] `modules/core/stage4_interview_round.py:344` / `modules/core/stage4_interview_round.py:363` / `modules/core/stage4_interview_round.py:381` / `modules/core/stage4_interview_round.py:388` — 구조화된 검증 결과가 문자열로 평탄화
- 입력: `violations`/`failures`/`warnings`의 dict 구조 (reason 외 category/severity/location 가능)
- 출력: `"reason"` 문자열만 `validation_results[*]["warnings"]`에 누적
- 손실: 정밀 판정에 필요한 구조 필드 소실
- 영향: Director 단계에서 원인 유형/심각도 기반 판단 정밀도 하락

### [HIGH] `modules/core/stage4_interview_round.py:509` — Director 전달 경고 `[:10]` 상한
- 입력: 후보별 누적 경고 전체
- 출력: 후보당 상위 10개만 전달
- 손실: 후순위 경고가 무조건 누락 (경고 밀집 회차에서 중요 신호 탈락 가능)

### [HIGH] `modules/domain/agents/director_ensemble.py:464`~`modules/domain/agents/director_ensemble.py:469` + `modules/core/stage4_interview_round.py:614`~`modules/core/stage4_interview_round.py:625` — Director 결과의 재작성 루프 전달 축소
- 입력: Director 결과에 `score_breakdown`, `selection_reason`, `state_updates`, `other_candidates_notes` 포함
- 출력: REJECT 시 `action_items` + `issues` 중심 문자열/축약 dict만 `previous_attempt`에 보존
- 손실: 세부 채점 근거/후보 비교 정보/구조화 상태 정보 소실

### [MEDIUM] `modules/domain/agents/chief_writer.py:696` / `modules/domain/agents/chief_writer.py:703` — 패치 모드 원본 원고 30,000자 절삭
- 입력: 원본 원고 전체
- 출력: `original_manuscript[:30000]`
- 손실: 장문 회차 후반부 문맥 미전달
- 영향: “원본 보존 패치” 의도 대비 후반 교정 정확도 저하 가능

### [MEDIUM] `modules/core/vec_memory.py:210` / `modules/core/vec_memory.py:211` / `modules/core/vec_memory.py:212` / `modules/core/vec_memory.py:217` — 벡터 메타데이터 저장 시 다중 절삭
- 입력: `causal_links`, `event_types`, `entity_names`, `summary`
- 출력: 각각 500/200/300/500자 상한
- 손실: 인과/엔티티 정보의 장문 상세가 저장 단계에서 축소

### [LOW] `modules/core/project_manager.py:838` / `modules/core/project_manager.py:853` / `modules/core/project_manager.py:861` — 초기 동기화 요약/엔티티 추출 과소
- 입력: 원고 전문
- 출력: 요약 300자, 엔티티 탐색 3000자 범위+상위 10개
- 손실: 장문 후반 사건/엔티티가 초기 벡터 주입에서 누락될 가능성

## 3. Broken Wiring 목록 (심각도순)

### [HIGH] `main_a.py:1645` — `ChainOfVerification` 초기화 후 미호출
- 초기화: `self.chain_of_verification = ChainOfVerification(...)`
- 구현: `verify()` 존재 (`modules/core/chain_of_verification.py:190`)
- 호출: 프로젝트 전역 검색 시 실호출 경로 없음
- 영향: “생성 후 모순 재검증” 핵심 안전장치 비작동

### [HIGH] `main_a.py:1634` — `CrossAgentVerifier` 초기화 후 미호출
- 초기화: `self.cross_verifier = CrossAgentVerifier(...)`
- 구현: 검증 메서드 존재 (`modules/core/cross_agent_verifier.py:265`, `modules/core/cross_agent_verifier.py:349`)
- 호출: 실호출 경로 없음

### [HIGH] `main_a.py:1659` — `PreDirectorChecklist` 초기화 후 미호출
- 초기화: `self.pre_director_checklist = PreDirectorChecklist()`
- 구현: 체크리스트 클래스 존재 (`modules/core/pre_director_checklist.py:84`)
- 호출: `.pre_director_checklist` 사용 경로 없음

### [HIGH] `main_a.py:1709` — `ManuscriptEnhancer` 초기화 후 미호출
- 초기화: `self.manuscript_enhancer = ManuscriptEnhancer(...)`
- 구현: 클래스 존재 (`modules/core/manuscript_enhancer.py:672`)
- 호출: 파이프라인 실사용 경로 없음

### [MEDIUM] `main_a.py:1641` — `DynamicPromptWeighter` 초기화 후 미호출

### [MEDIUM] `main_a.py:1652` — `ConfidenceCalibrator` 초기화 후 미호출

### [MEDIUM] `main_a.py:1663` — `TreeOfThoughts` 초기화 후 미호출
- 구현: `explore()` 존재 (`modules/core/tree_of_thoughts.py:183`)

### [MEDIUM] `main_a.py:1670` — `AdversarialSelfPlay` 초기화 후 미호출
- 구현: `generate_with_adversary()` 존재 (`modules/core/adversarial_self_play.py:225`)

### [MEDIUM] `main_a.py:1677` — `MultiAgentDeliberation` 초기화 후 미호출
- 구현: `deliberate()` 존재 (`modules/core/multi_agent_deliberation.py:273`)

### [MEDIUM] `main_a.py:1688` — `SemanticCache` 초기화 후 미호출

### [MEDIUM] `main_a.py:1692` — `ContextCompressor` 초기화 후 미호출
- 구현: `compress()` 존재 (`modules/core/context_compression.py:83`)
- 호출 근거: `compress_for_writer/architect` 참조 없음

### [MEDIUM] `modules/core/adaptive_retry.py:486`~`modules/core/adaptive_retry.py:491` + `modules/core/adaptive_retry.py:652` + `modules/core/adaptive_retry.py:676`~`modules/core/adaptive_retry.py:681` — “필살기 권고” 문자열만 있고 실행 디스패치 부재
- 상태: `adversarial_self_play`/`tree_of_thoughts`/`multi_agent_deliberation` 추천값 반환 로직은 존재
- 문제: 추천값을 받아 실제 모듈 메서드를 호출하는 경로 미확인

## 4. 피드백 루프 손실 분석

### Director → Writer
- Director 결과의 전체 필드: `modules/domain/agents/director_ensemble.py:464`~`modules/domain/agents/director_ensemble.py:469`
- Stage4 REJECT 경로 전달 필드: `modules/core/stage4_interview_round.py:614`~`modules/core/stage4_interview_round.py:625`
- Writer 재작성 입력 소비 필드: `modules/domain/agents/chief_writer.py:587`~`modules/domain/agents/chief_writer.py:596`
- 결론: 구조화 피드백(`score_breakdown`, `other_candidates_notes`, 세부 `state_updates`)이 재작성 루프로 충분히 전달되지 않음

### Validator → Director
- Validator 결과 문자열화 지점: `modules/core/stage4_interview_round.py:344`, `modules/core/stage4_interview_round.py:363`, `modules/core/stage4_interview_round.py:381`, `modules/core/stage4_interview_round.py:388`
- Director 전달 상한 지점: `modules/core/stage4_interview_round.py:509`
- 결론: 검증 신호가 “평탄화 + 상위 10개 제한”으로 전달되어, Director가 원본 구조를 활용한 정밀 판정이 어려움

## 5. 개선 우선순위 Top 10

1. `ChainOfVerification`를 Stage4 최종 선택 직후 강제 호출로 연결 (`main_a.py:1645`, `modules/core/chain_of_verification.py:190`)
2. Stage4에서 Validator 결과를 문자열이 아닌 구조화 객체로 Director에 전달 (`modules/core/stage4_interview_round.py:344`, `modules/core/stage4_interview_round.py:363`)
3. Director 경고 전달 `[:10]` 제거 또는 심각도 우선 정렬 후 전달 (`modules/core/stage4_interview_round.py:509`)
4. REJECT 재작성 시 `score_breakdown/selection_reason/other_candidates_notes`를 `previous_attempt`에 보존 (`modules/core/stage4_interview_round.py:620`)
5. `use_parallel_validation` 플래그를 실제 병렬 분기에 연결 (`modules/validation/validation_orchestrator.py:194`, `modules/validation/validation_orchestrator.py:1017`)
6. `adaptive_retry`의 필살기 추천값을 실제 모듈 호출로 디스패치 (`modules/core/adaptive_retry.py:676`)
7. `manuscript.*` 임계값을 `_threshold()` 소비 경로로 이관하거나 YAML에서 제거 (`config/settings/validation.yaml:13`)
8. `ContextCompressor`/`SemanticCache`를 Stage2/4 컨텍스트 조립 경로에 실제 삽입 (`main_a.py:1688`, `main_a.py:1692`)
9. 패치 모드 원고 절삭(30K) 대신 구간 분할 주입 또는 tail 보존 혼합 절삭 (`modules/domain/agents/chief_writer.py:696`)
10. VecMemory 저장 메타 필드 상한 재설계(특히 `causal_links`/`entity_names`) (`modules/core/vec_memory.py:210`)

