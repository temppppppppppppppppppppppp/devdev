# 2차 전수조사 — 1번 터미널 보고서

> 터미널: 1번 (Stage 2 파이프라인 전체)
> 기준 커밋: `9a55ac0`
> 조사일: 2026-03-06
> 담당 파일: 9개

---

## 담당 파일 목록

```
modules/core/stage2_orchestrator.py          (907줄)
modules/core/stage2_preflight.py             (637줄)
modules/core/stage2_finalizer.py             (535줄)
modules/core/stage2_validation_pipeline.py
modules/core/stage2_context.py
modules/core/stage2_optimizer.py
modules/domain/agents/analyst.py
modules/domain/agents/analyst_prompts.py
modules/domain/agents/analyst_prompt_api.py
```

---

## TF 목록 (감리 3회 완료)

| TF번호 | 등급 | 파일:줄 | 종류 | 설명 | 패치 방향 |
|--------|------|---------|------|------|-----------|
| TF-1-01 | P1 | stage2_finalizer.py:568 | GAP | [공통-A] calc_patch_change_ratio 비교 실패 — `except Exception: pass`, logging 없음 | `logging.debug("F-2 change_ratio calc failed: %s", e)` 추가 |
| TF-1-02 | P1 | analyst.py:142 | GAP | [공통-A] `get_protagonist_name()` 실패 — `except Exception: protagonist_name = "주인공"`, logging 없음 | `logging.debug("protagonist_name fallback: %s", e)` 추가 |
| TF-1-03 | P1 | analyst.py:1537 | GAP | [S2-5] `_get_current_genre()` 예외 시 `GenreTypes.WUXIA` 폴백 — 비무협 작품에 무협 플레이스홀더 오염 가능 | 폴백 장르를 `app.genre` or `project_genre` SSOT에서 읽거나 None 반환 후 호출자에서 핸들 |
| TF-1-04 | P2 | stage2_validation_pipeline.py:11 | GAP | [공통-E] `_JACCARD_SIMILARITY_THRESHOLD = 0.50` 하드코딩 — `_threshold()` 미사용 | `_threshold("jaccard_similarity_threshold", 0.50)` 교체 |
| TF-1-05 | P2 | stage2_preflight.py:1017 | GAP | [S2-1 보완] `_st_snapshot` deepcopy 직전 `_st is None` 가드 없음 — state_tracker 미주입 시 TypeError | `if _st is not None:` 가드 후 deepcopy |
| TF-1-06 | P2 | stage2_preflight.py (해당없음) | GAP | [S2-10] 체크 명세 오류 — NS-4-S2 실체는 `four_phase_arc_generator._ns4_extract_time_markers()`. stage2_preflight에는 `state_tracker.extract_time_markers_from_arc()`(별도 메서드) 존재. 기능 자체는 정상 | 체크 명세를 `four_phase_arc_generator` 경로로 정정 (기능 이상 없음) |

---

## 감리 기록

### 감리 1회 — 오탐 1차 제거 (CLAUDE.md 의도적 설계)

```
[오탐] 공통-A / stage2_preflight.py:471
  근거: except Exception: pass에 "[Phase 3-QR] advisory 비치명" 주석 명시 — 의도적 설계
```

### 감리 2회 — 오탐 2차 제거 (대원칙 1~4 준수 확인)

- TF-1-01~06 전체: Python이 판단하지 않음. logging 누락·폴백·하드코딩 문제만. 대원칙 위반 없음.
- S2-3 (PASS_WITH_FIX 패치 이력 주입): `stage2_finalizer.py:583`에서 `[PASS_WITH_FIX 재심사 — 이미 적용된 패치]` Director story_context 주입 확인 → OK
- S2-4 (fix_scope 전파): `stage2_preflight.py:832`에서 `previous_attempt.get("fix_scope", "")` 소비 확인. validation_pipeline은 pre-Director라 fix_scope 불필요 → OK

### 감리 3회 — 오탐 3차 제거 (기존 테스트 커버 확인)

- TF-1-03 (WUXIA 폴백): 기존 테스트에서 비무협 + `_get_current_genre()` 예외 시나리오 미커버 → P1 유지
- TF-1-05 (None 가드): state_tracker=None 주입 경로 테스트 미커버 → P2 유지
- TF-1-04 (하드코딩): 기능 동작은 정상, 임계값 외부화 정책 불일치 → P2 유지

---

## OK 확인 항목

| 체크 항목 | 근거 |
|-----------|------|
| S2-1 — V70 rollback | finalizer.py L675/804/896/1085-1094에서 StateTracker snapshot rollback 4경로 확인 |
| S2-2 — LM-G injection | preflight.py L511에서 `NarrativeContextFormatter.format_all()` 결과 `enhanced_context`에 prepend 확인 |
| S2-3 — PWF-S2 patch history | finalizer.py L583에서 PASS_WITH_FIX 패치 이력 Director 재심사 story_context 주입 확인 |
| S2-4 — fix_scope 전파 | orchestrator `_previous_attempt` → preflight L832 `previous_attempt.get("fix_scope")` 소비 확인. validation_pipeline은 pre-Director라 불필요 |
| S2-6 — ARC_DESIGN_SCHEMA 키 | response_schemas.py `status_shadow.key_stat_change` 확인 (TF-45 완료, internal_energy_loss 없음) |
| S2-7 — 44슬롯 None 방어 | stage2_context.py `from_app()` 전 슬롯 `getattr(app, ..., None)` 패턴 확인 |
| S2-8 — TODO/FIXME 잔존 | stage2_optimizer.py 전체 grep — TODO/FIXME 0건 (A-2 완료 확인) |
| S2-9 — 플레이스홀더 치환 | analyst.py L788 `format_map(_SafeDict(**cache_safe_data))` 실행 — `{energy_tracking_rules}` 정상 치환 확인 |
| 공통-B — LLM 파싱 폴백 | orchestrator/finalizer/preflight 전체 — `_extract_json_robust` + 폴백 경로 정상 |
| 공통-C — 대원칙 1·2·3 | Python이 직접 판단하는 코드 없음. 모든 REJECT/PASS 결정 LLM(Director) 경유 |
| 공통-D — 모델명 하드코딩 | 9개 파일 전체 — `AIModels.*` 또는 `_load_model_from_yaml()` 사용. 문자열 직접 박힘 없음 |
| 공통-F — DB 리소스 | orchestrator/finalizer — `with conn:` 또는 DBManager 컨텍스트 매니저 사용 확인 |
| 공통-G — dead code | stage2_optimizer.py 전 클래스 호출 확인 (Stage2Optimizer → orchestrator에서 사용) |
| 공통-H — 사망 NPC 방어 | Stage 2는 원고 미생성 단계 (Arc/Blueprint). 사망 NPC 행동 노출 경로 없음 |

---

## 요약

| 등급 | 건수 |
|------|------|
| P0 | 0건 |
| P1 | 2건 (공통-A silent except 2건) + 1건 (S2-5 WUXIA 폴백) = **3건** |
| P2 | 3건 (공통-E 하드코딩, S2-1 None 가드, S2-10 명세 오류) |
| 오탐 제거 | 1건 (preflight:471 의도적 설계) |
| OK | 14건 |
