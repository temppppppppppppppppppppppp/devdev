# 2차 전수조사 — 10번 터미널 보고서

> 담당: Config / SSOT / Protocols / Base / 진입점
> 기준 커밋: `9a55ac0`
> 조사일: 2026-03-06

---

## 조사 결과 요약

| 등급 | 건수 |
|------|------|
| P0   | 0    |
| P1   | 0    |
| P2   | 5    |
| OK   | 10   |

---

## 항목별 결과

### [CFG-1] constants.py — AIModels attr 카운트 문서 불일치
```
001 | modules/core/constants.py:222~251 | P2 | GAP | CLAUDE.md "AIModels 16개 attr" → 실제 19개 attr (SSOT 기능 정상, 문서 카운트만 오래된 값)
```
- 실제 attr 목록 (19개): V50_MODULE_MODEL, FLASH_ANALYSIS_MODEL, SUMMARY_MODEL, TIER_1_ARCHITECT, TIER_2_ARCHITECT, TIER_3_ARCHITECT, TIER_1_WRITER, TIER_2_WRITER, TIER_3_WRITER, EMERGENCY_FALLBACK, QUOTA_FALLBACK, DEFAULT_WRITER, DEFAULT_ARCHITECT, DEFAULT_ANALYST, DEFAULT_REVIEWER, STAGE4_FIXED_WRITER_MODEL, STAGE2_MAIN_MODEL, STAGE2_EXTRACTION_MODEL, STAGE2_VALIDATION_MODEL
- 모두 `_load_model_from_yaml()` 참조 → SSOT 기능 이상 없음
- **판정**: 오탐 후보 (기능 정상). CLAUDE.md 카운트 수정 권장 → P2

---

### [CFG-2] models.yaml — DEFAULT_MODEL_FALLBACK_CHAIN 하드코딩 미등재 모델
```
002 | modules/domain/agents/base_agent.py:46~51 | P2 | GAP | DEFAULT_MODEL_FALLBACK_CHAIN에 gemini-3.1-pro-preview / gemini-3-pro-preview / gemini-3-flash-preview 하드코딩 — models.yaml 미등재
```
- `_get_model_fallback_chain()`이 models.yaml `fallback_chain` 섹션 우선 사용하므로 정상 경로에서는 yaml 값이 덮어씀
- DEFAULT_MODEL_FALLBACK_CHAIN은 yaml 로드 실패 시 fallback으로만 사용
- models.yaml `fallback_chain` 섹션은 gemini-2.5-pro/flash만 등록 → gemini-3.x 라인은 yaml에 없음
- **판정**: 실전 영향 낮음 (yaml 우선). 미래 모델 하드코딩 위험 → P2

---

### [CFG-3] validation.yaml — _threshold() 키 전수 일치 여부
```
003 | config/settings/validation.yaml | OK | _threshold() 호출 키 전량 YAML에 존재 확인
```
- orchestrator.*, scoring.*, adaptive_threshold.*, retrospective.lookback_episodes, smart_retrieval.* 등 전량 validation.yaml에 정의됨
- **판정**: OK

---

### [CFG-4] prompt_loader.py — 싱글톤 스레드 안전성
```
004 | modules/core/prompt_loader.py:39~49 | OK | _instance_lock + _cache_lock 이중 잠금, double-checked locking 적용
```
- `__new__`: `_instance_lock` 안에서 double-check ✅
- `_load_yaml_file`: cache 읽기·쓰기 모두 `_cache_lock` 안에서 수행 ✅
- YAML 파일 I/O는 lock 밖 (읽기 전용 파일 → 안전), 결과 저장은 lock 안 ✅
- 두 스레드 동시 로드 시 동일 YAML 2회 파싱 가능 (무해한 중복) → 허용 범위
- **판정**: OK

---

### [CFG-5] base_agent.py — API_DELAY 적용 위치 문서 불일치
```
005 | modules/domain/agents/base_agent.py:464,953,1116,1739 | P2 | GAP | API_DELAY 적용 4곳 vs CLAUDE.md "3곳 모두" 언급 (문서-코드 불일치)
```
- L154: `API_DELAY = _SYSTEM_CFG.get("api", {}).get("delay", 0.1)` — system.yaml에서 0.1초로 로드 (MEMORY.md "0.3초" 기재와 불일치하나, 실제 값은 system.yaml 기준)
- 적용 위치: L464, L953, L1116, L1739 (4곳)
- CLAUDE.md "3곳 모두" → ask() B-1-9c 분할 후 4개 private 메서드에 각 1곳씩 적용된 것으로 추정
- **판정**: 기능 정상 (4곳 전부 적용). 문서 "3곳" 수정 필요 → P2

---

### [CFG-6] base_agent.py — fallback_chain 키 변경 불필요 사유 주석 미존재
```
006 | modules/domain/agents/base_agent.py:46~51 | P2 | GAP | DEFAULT_MODEL_FALLBACK_CHAIN 정의에 "변경 불필요" 사유 주석 없음
```
- CLAUDE.md: "base_agent.py:47 fallback_chain 키·metrics_collector.py 가격표는 변경 불필요"
- 실제 코드: 주석 없이 dict 정의만 존재
- 신규 기여자가 변경 이유를 알 수 없는 상황 → P2

---

### [CFG-7] base_agent.py — _get_or_create_context_cache() 스레드 안전성
```
007 | modules/domain/agents/base_agent.py:1578~1660 | OK | _cache_lock threading.Lock() 전 영역 보호
```
- `_context_caches = {}` + `_cache_lock = threading.Lock()` 클래스 변수
- 읽기(L1615-1621), 만료 삭제(L1624), 쓰기(L1648-1660) 전부 `with self._cache_lock:` 안에서 수행
- 만료 캐시 `.pop(cache_key, None)` — KeyError 방지 ✅
- 키 순환 시 `_context_caches.clear()`도 `_cache_lock` 안에서 수행 (L215-216) ✅
- **판정**: OK

---

### [CFG-8] director.yaml — VARIABLE_PROMPT/SELECTION_PROMPT Arc 첫 화 MAJOR 감점 조항
```
008 | config/prompts/director.yaml:73, 364 | OK | 양쪽 모두 Arc 경계 공간 연속성 MAJOR 감점 조항 존재
```
- ENSEMBLE_VARIABLE_PROMPT L73: "Arc 경계 공간 연속성 (Arc 첫 화): ... 없으면 → MAJOR 감점 (연속성 점수 -10점)" ✅
- ENSEMBLE_SELECTION_PROMPT L364: 동일 조항 ✅
- **판정**: OK

---

### [CFG-9] director.yaml — [NC-3B] few-shot golden example 4곳 삽입
```
009 | config/prompts/director.yaml:197, 488, 761, 1000 | OK | NC-3B few-shot 4곳 확인
```
- L197 (ENSEMBLE_VARIABLE_PROMPT), L488 (ENSEMBLE_SELECTION_PROMPT), L761, L1000
- **판정**: OK

---

### [CFG-10] director.yaml — consistency_checklist timeline_arc_consistency 11번째 키
```
010 | config/prompts/director.yaml:194 | OK | timeline_arc_consistency 11번째 항목 + fiction_term_leak 12번째 항목 존재
```
- 11. **timeline_arc_consistency** ✅
- 12. **fiction_term_leak** (TF-57-A 신규 항목도 존재)
- NC-3B few-shot 예시(L200-213)에도 12개 키 전량 포함 ✅
- **판정**: OK

---

### [CFG-11] chief_writer.yaml — 규칙 12/13/14 존재 여부
```
011 | config/prompts/chief_writer.yaml:39~47 | OK | 규칙 12(ending_hook 의무) / 13(씬별 대화 의무) / 14(4th wall 금지) 전량 존재
```
- 규칙 12 (L39): ending_hook 의무화 ✅
- 규칙 13 (L43): 씬별 대화 의무 ✅
- 규칙 14 (L44): "집필 시스템 내부 용어 금지 ... Block 1, Block 2, Arc 4, Stage 3, Blueprint, treatment" ✅
- **판정**: OK

---

### [CFG-12] main_a.py — _init_core_agents() / _init_v50_modules() DI 배선
```
012 | main_a.py:1428, 1532, 1895, 1989 | OK | God Object 2 분리 완료. _attach_agents() → 두 메서드 위임 확인
```
- `_init_core_agents(L1428)`: agents dict 구성 + arc_corrector/stage2_optimizer 초기화
- `_init_v50_modules(L1532)`: V50 서사 품질 향상 모듈 전체 초기화
- `_attach_agents()`에서 L1895, L1989 순서대로 호출 ✅
- **판정**: OK

---

### [CFG-13] protocols/agents.py — Protocol 미준수 클래스 잔존 여부
```
013 | modules/protocols/agents.py:37~50 | OK | B-3 표준화 후 미적합 에이전트 파일 내 명시적 문서화
```
- 미적합 5개 (ChiefWriter, ConsensusValidator, Critic, Director, StateExtractor) → 파일 주석에 "미적합 에이전트 (메서드명 불일치 — 향후 어댑터 필요)"로 의도적 설계 문서화
- B-2에서 적합화된 5개 (ArcDraftValidator, ManuscriptValidator, ConstraintCompiler, BlueprintConstraintCompiler, StateTracker) ✅
- **판정**: OK (의도적 설계 명시됨)

---

### [CFG-14] models/npc.py — deceased 필드 미정의
```
014 | modules/models/npc.py:20~33 | P2 | GAP | NPCEntry에 deceased 필드 없음 — CLAUDE.md 대원칙 4 "deceased=True" vs 실제 status="dead" 표현 불일치
```
- NPCEntry 필드: name, status(default="alive"), weapon, level, death_arc, last_arc
- `deceased=True` 필드 없음 → truth_gate(L94)에서 `info.get("status") == "dead"` 체크로 대체
- 기능적으로는 정상 작동하나 대원칙 4 문서 표현("deceased=True")과 코드 실제 표현("status='dead'") 불일치
- extra="allow"로 동적 필드 허용하나 명시적 스키마에 없어 타입 힌트 미제공
- **판정**: 기능 정상. 문서-코드 용어 불일치 → P2

---

### [CFG-15] analyst.yaml — 플레이스홀더 존재 여부
```
015 | config/prompts/analyst.yaml:251, 477, 485 | OK | {energy_tracking_rules} / {episode_state_label} 전량 존재
```
- L251: `{energy_tracking_rules}` ✅
- L477, L485: `{episode_state_label}` 2곳 ✅
- **판정**: OK

---

## 감리 1회 — CLAUDE.md 명시 의도적 설계 교차 확인

| 항목 | 감리 결과 |
|------|----------|
| CFG-2 gemini-3.x 하드코딩 | CLAUDE.md "P2 유보(호출자 명시 주입)" 확인. DEFAULT_MODEL_FALLBACK_CHAIN은 미래 모델 예비 등록 패턴으로 보임. P2 유지. |
| CFG-13 미적합 Protocol | protocols/agents.py 파일 내 "미적합 에이전트" 주석 의도 설계 확인 → OK 유지. |
| CFG-14 status vs deceased | truth_gate 코드가 status=="dead" 체크로 완전히 작동 확인 → 기능 이상 없음. P2 유지. |

---

## 감리 2회 — 기존 테스트 커버리지 확인

| 항목 | 테스트 커버리지 |
|------|----------------|
| CFG-4 prompt_loader 스레드 안전 | tests/에서 PromptLoader 6개+ 테스트 확인됨 (6-A 완료) |
| CFG-7 context_cache 스레드 안전 | TF-7R 카오스 테스트 38개에서 캐시 동시성 커버됨 |
| CFG-8 Arc 첫 화 MAJOR 감점 | E2E wiring 테스트에서 director.yaml 프롬프트 로드 검증됨 |
| CFG-12 main_a 분리 | God Object 2 패치 후 E2E smoke 테스트 통과 기록 있음 |

---

## 감리 3회 — 대원칙 1~4 준수 최종 확인

| 항목 | 대원칙 확인 |
|------|------------|
| CFG-3 임계값 YAML 키 일치 | Python은 값 읽기만, 판단은 없음 → 대원칙 1 준수 |
| CFG-8 Arc 첫 화 MAJOR 감점 | 감점 지시만, 최종 PASS/REJECT는 Director → 대원칙 3 준수 |
| CFG-14 NPCEntry status | status="dead" 확인 후 경고 발행, 최종 판정은 LLM → 대원칙 1·4 준수 |

---

## TF 목록

| TF번호 | 등급 | 파일:줄 | 패치 명세 |
|--------|------|---------|----------|
| TF-10-01 | P2 | modules/core/constants.py:219 | CLAUDE.md AIModels 카운트 "16개" → "19개" 수정 (문서 동기화) |
| TF-10-02 | P2 | modules/domain/agents/base_agent.py:46 | DEFAULT_MODEL_FALLBACK_CHAIN 위에 "# [SSOT] models.yaml fallback_chain 로드 실패 시 하드코딩 fallback — 변경 불필요(yaml 우선)" 주석 추가 |
| TF-10-03 | P2 | modules/domain/agents/base_agent.py:153 | CLAUDE.md "API_DELAY 3곳" → "4곳" 수정 (문서 동기화) |
| TF-10-04 | P2 | modules/models/npc.py:28 | NPCEntry에 `deceased: bool = False` 필드 추가 — 대원칙 4 문서 표현과 코드 정렬 (truth_gate status=="dead" 체크와 병행 유지) |

---

## 최종 판정

- **P0**: 0건 — 즉시 처리 대상 없음
- **P1**: 0건
- **P2**: 5건 (TF-10-01~04, CFG-2 P2 유지) — 정기 정리 대상
- 전체 기능 정상, 실전 파이프라인 영향 없음
