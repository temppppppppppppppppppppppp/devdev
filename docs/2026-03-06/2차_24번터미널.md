# 2차 전수조사 — 24번 터미널 보고서

> 담당: 24번 터미널
> 일시: 2026-03-06
> 대상: director.yaml (Arc 경계 공간연속성 조항) + chief_writer.yaml (규칙 체계 전체)
> 기준 커밋: 9a55ac0

---

## 조사 요약

| TF 번호 | 등급 | 판정 |
|---------|------|------|
| TF-24-01 | P1 | DIRECTOR_AUDIT_PROMPT_V30에 Arc 경계 MAJOR 감점 조항 누락 |
| TF-24-02 | P2 | Arc 경계 조항 명시적 테스트 없음 |
| TF-24-03 | P2 | chief_writer.yaml 규칙14 명시적 테스트 없음 |

---

## TF 목록

### TF-24-01 | P1 | config/prompts/director.yaml | GAP | DIRECTOR_AUDIT_PROMPT_V30 Arc 경계 MAJOR 조항 누락

**발견 위치**: `config/prompts/director.yaml` DIRECTOR_AUDIT_PROMPT_V30 Step 1 (L891)

**현상**:
ENSEMBLE_VARIABLE_PROMPT (L73)와 ENSEMBLE_SELECTION_PROMPT (L364) 두 곳에는 Arc 경계 MAJOR 감점 조항이 올바르게 삽입되어 있음:
```
**Arc 경계 공간 연속성** (Arc 첫 화): mandatory_context에 이전 Arc 종료 위치가 명시된 경우,
현재 화 시작 위치가 다르면 이동 과정·시간 경과 표지가 반드시 있어야 한다.
없으면 → MAJOR 감점 (연속성 점수 -10점).
```

그러나 DIRECTOR_AUDIT_PROMPT_V30 Step 1 (L891)에는 동일 조항이 없음:
```
- [NC-2] 공간 이동이 자연스러운가? 장소 전환 시 이동 묘사 또는 전환 표지가 있는가?
- [NC-2] 에피소드 시작 시 시간 경과(다음날/N일 후 등)가 명시되었는가?
```
Arc 첫 화 / MAJOR 감점(-10점) 조건이 없음.

**영향**:
- PASS_WITH_FIX 재심사 경로: `audit_manuscript()` → DIRECTOR_AUDIT_PROMPT_V30 사용
- 앙상블 경로(ENSEMBLE)에서 MAJOR 감점을 받고 REJECT된 후 재작성 → Director 재심사 시 동일 기준이 미적용
- Arc 첫 화에서 공간 연속성 위반 원고가 재심사를 통과할 수 있음

**감리 1회 확인**: 의도적 설계인지 검토
- CLAUDE.md "Arc 경계 공간 연속성 — director.yaml 2곳(VARIABLE_PROMPT L72, SELECTION_PROMPT L359)에 Arc 첫 화 MAJOR 감점 조항 추가" → AUDIT 프롬프트 언급 없음
- 즉, AUDIT 경로 미적용은 의도적일 수 있음 (실전모순패치 스코프: ENSEMBLE 2곳만)
- 그러나 재심사 경로의 기준 불일치는 운영상 P1 위험 → 보고

**감리 2회 (대원칙 확인)**:
- 대원칙 3 (Director 주권): 조항 추가 = Director에게 더 명확한 기준 제공 → 위반 아님
- 대원칙 1/2/4: 해당 없음

**감리 3회 (테스트 커버)**:
- 테스트: `test_nc2_gaps.py::TestSpaceContinuityPrompt` → "공간 연속성", "공간 이동" 키워드 확인만 (AUDIT 경로 조항 부재는 미감지)
- Arc 경계 조항 위치(AUDIT 포함 여부) 테스트 없음

**패치 방향 (0번 터미널)**:
DIRECTOR_AUDIT_PROMPT_V30 Step 1 ([NC-2] 공간 이동 체크 직후) 에 Arc 경계 조항 추가:
```yaml
  - [NC-2] 공간 이동이 자연스러운가? 장소 전환 시 이동 묘사 또는 전환 표지가 있는가?
  - [NC-2] 에피소드 시작 시 시간 경과(다음날/N일 후 등)가 명시되었는가?
  - [Arc 경계 공간 연속성] arc_pos==1인 경우: mandatory_context에 이전 Arc 종료 위치가 명시된 경우, 현재 화 시작 위치가 다르면 이동 과정·시간 경과 표지가 반드시 있어야 한다. 없으면 → MAJOR 감점 (-10점).
```

---

### TF-24-02 | P2 | tests/ | GAP | Arc 경계 조항 명시적 테스트 없음

**현상**:
- `test_nc2_gaps.py::TestSpaceContinuityPrompt` → "공간 연속성", "공간 이동" 키워드 존재 여부만 확인
- "Arc 경계 공간 연속성" 문자열이 ENSEMBLE_VARIABLE_PROMPT + ENSEMBLE_SELECTION_PROMPT 2곳 모두에 있는지 검증하는 테스트 없음
- DIRECTOR_AUDIT_PROMPT_V30에서 누락 여부를 캐치하는 테스트도 없음

**영향**: 저위험. 기능 오작동이 아니라 커버리지 공백.

**감리 3회**: 기존 `test_nc2_gaps.py`가 존재하므로 테스트 확장으로 해결 가능.

**패치 방향**: P2 유지. 신규 테스트 1개 추가 권장 (0번 터미널 판단):
```python
def test_arc_boundary_space_continuity_in_both_prompts():
    content = Path("config/prompts/director.yaml").read_text(encoding="utf-8")
    assert content.count("Arc 경계 공간 연속성") >= 2
```

---

### TF-24-03 | P2 | tests/ | GAP | chief_writer.yaml 규칙14 명시적 테스트 없음

**현상**:
- `_check_system_term_exposure()` 코드 테스트는 20번 터미널 담당
- chief_writer.yaml 규칙14 존재 여부를 YAML 로드 경로로 확인하는 테스트 없음
- test_satisfaction_step2_prompts.py 또는 test_pipeline_wiring.py에서 "Block N", "Arc N" 금지 지시 문자열 확인 테스트 없음

**영향**: 저위험. 규칙14는 코드상 올바르게 존재하며 PromptLoader 로드 정상.

**패치 방향**: P2 유지.

---

## YM 체크 항목 전체 결과

### YM-1: director.yaml Arc 경계 공간연속성 조항 검증

| 위치 | 조항 포함 | 내용 일치 |
|------|-----------|-----------|
| ENSEMBLE_VARIABLE_PROMPT (실제 L73) | ✅ | — |
| ENSEMBLE_SELECTION_PROMPT (실제 L364) | ✅ | ✅ 완전 일치 |
| DIRECTOR_AUDIT_PROMPT_V30 Step 1 | ❌ 누락 | → TF-24-01 P1 |

두 ENSEMBLE 조항 내용 비교 (완전 일치 확인):
```
**Arc 경계 공간 연속성** (Arc 첫 화): mandatory_context에 이전 Arc 종료 위치가
명시된 경우, 현재 화 시작 위치가 다르면 이동 과정·시간 경과 표지가 반드시 있어야 한다.
없으면 → MAJOR 감점 (연속성 점수 -10점).
```
→ ENSEMBLE_VARIABLE_PROMPT ↔ ENSEMBLE_SELECTION_PROMPT 내용 완전 동일 ✅

### YM-2: NC-3 consistency_checklist와 충돌 여부

NC-3 항목 7 (`space_continuity`):
- "캐릭터의 장소 이동이 자연스럽고 전환 묘사가 있는가?" → 에피소드 내 이동, MINOR 수준
- "[TF-57-B] 각 에피소드의 [시작 상태:] 헤더에 명시된 위치와 본문 첫 문단 위치 묘사가 일치하는가?"

Arc 경계 MAJOR 감점 조항:
- "Arc 첫 화" 한정 + "mandatory_context에 이전 Arc 종료 위치가 명시된 경우" 조건부 → MAJOR

**충돌 여부**: 없음 ✅
- NC-3 space_continuity: 에피소드 내 장소 이동 전환 묘사 (MINOR 레벨)
- Arc 경계 조항: Arc 첫 화 전용 + 조건부 (MAJOR 레벨)
- 두 레이어는 서로 다른 검사 대상이며 보완 관계

**Gap (관찰)**: NC-3 체크리스트(선택사항)에 Arc 경계 공간연속성 항목이 별도로 없음.
그러나 NC-3은 "권장" (미작성 시 감점 없음)이므로 운영 위험 없음 → P2 이하.

### YM-3: chief_writer.yaml 규칙 전체 체계 정합성

규칙 번호 연속성 확인:

| 규칙 번호 | 내용 | 출처 |
|-----------|------|------|
| 1 | 감정어 삭제 → 행동 변환 | COMMON_RULES_SECTION |
| 2 | 감각적 묘사 강화 (오감 활용) | COMMON_RULES_SECTION |
| 3 | 요약된 대화의 장면화 | COMMON_RULES_SECTION |
| 4 | 문장 밀도 조절 | COMMON_RULES_SECTION |
| 5 | [V63] 예측 가능한 전개 지양 | COMMON_RULES_SECTION |
| 6 | [V63] 긴장 유지 원칙 | COMMON_RULES_SECTION |
| 7 | [V63] 캐릭터 고유 반응 | COMMON_RULES_SECTION |
| 8 | [TF-36] 웹소설 줄바꿈 규칙 | COMMON_RULES_SECTION |
| 9 | [TF-54] 은유 다양성 | COMMON_RULES_SECTION |
| 10 | [TF-54] 강도 조절 | COMMON_RULES_SECTION |
| 11 | [TF-54] NPC 음성 분화 | COMMON_RULES_SECTION |
| 12 | [품질] ending_hook 의무화 | COMMON_RULES_SECTION |
| 13 | [품질] 씬별 대화 의무 | COMMON_RULES_SECTION |
| 14 | [메타 월] 집필 시스템 내부 용어 금지 | COMMON_RULES_SECTION |

- 규칙 1~14 연속 번호 충돌 없음 ✅
- 14가 정확히 14번째 규칙 ✅
- 규칙 간 충돌 없음 ✅
- TF-E 톤 조절 반영 확인:
  - "감점 대상입니다" (L51, L52) ✅
  - "사용하지 마세요" (L56, L57) ✅
  - 핵심 금지 유지: "죽은 NPC 부활, 미습득 무공 사용 절대 금지" (WRITING_GUIDELINES L55) ✅

규칙14 내용 확인 (L44~47):
```yaml
14. [🚫 메타 월] 집필 시스템 내부 용어 금지: 원고 대사/서술에 "Block 1", "Block 2", "Arc 4",
    "Stage 3", "Blueprint", "treatment" 등 집필 시스템 메타용어를 사용하지 마세요.
    등장인물이 이 용어를 직접 언급하면 4th Wall 파괴입니다.
    세계관 내 표현으로 대체하세요 (예: "처음 원유 투자할 때", "지난번 거래 초반에").
```
- "Block N/Arc N/Stage N/Blueprint/treatment" 패턴 명시 ✅
- 기존 규칙 1~13과 충돌 없음 ✅
- 세계관 내 대체 표현 예시 포함 ✅

### YM-4: YAML 로드 경로 확인

- PromptLoader: 싱글톤 패턴, import-time에 YAML 로드
- 변경사항 반영: Python 프로세스 재시작 시 자동 반영 (의도된 설계)
- 런타임 캐시 무효화 필요 없음 ✅
- director.yaml 변경 → PromptLoader.load("director", ...) 정상 반환 ✅

### YM-5: 기존 테스트 커버

| 테스트 | 커버 내용 | 판정 |
|--------|-----------|------|
| `test_nc2_gaps.py::TestSpaceContinuityPrompt::test_director_yaml_contains_space_keywords` | "공간 연속성" 키워드 존재 | ✅ |
| `test_nc2_gaps.py::TestSpaceContinuityPrompt::test_audit_step1_space_check` | "공간 이동" 키워드 존재 | ✅ |
| `test_nc3_checklist.py::test_director_yaml_has_all_10_keys` | space_continuity 키 포함 | ✅ |
| `test_satisfaction_step2_prompts.py::test_ensemble_prompt_length_reasonable` | ENSEMBLE_SELECTION_PROMPT 로드 (간접) | ✅ |
| **Arc 경계 조항 2곳 명시 확인** | **없음** | ❌ → TF-24-02 P2 |
| **chief_writer.yaml 규칙14 존재 확인** | **없음** | ❌ → TF-24-03 P2 |

---

## 결론 및 0번 터미널 보고 요약

### P1 (패치 권장)
- **TF-24-01**: DIRECTOR_AUDIT_PROMPT_V30 Step 1에 Arc 경계 공간연속성 MAJOR 감점 조항 추가
  → ENSEMBLE 2곳과 AUDIT 경로 기준 일치화
  → 패치 크기: YAML 3줄 추가, 기존 NC-2 공간이동 체크 직후 삽입

### P2 (현상 유지 가능)
- **TF-24-02**: Arc 경계 조항 명시적 테스트 신규 추가 권장
- **TF-24-03**: chief_writer.yaml 규칙14 존재 테스트 권장

### CORRECT 판정 항목
- YM-1: ENSEMBLE_VARIABLE_PROMPT + ENSEMBLE_SELECTION_PROMPT 두 곳 모두 조항 올바르게 포함 ✅
- YM-2: NC-3 space_continuity와 Arc 경계 조항 간 충돌 없음 ✅
- YM-3: chief_writer.yaml 규칙 1~14 번호 연속성 정합, 규칙 간 충돌 없음, TF-E 톤 조절 반영 ✅
- YM-4: PromptLoader 로드 경로 정상 ✅
