# 2차 전수조사 — 12번 터미널 (Core 보조 에이전트)

> 기준 커밋: `9a55ac0`
> 담당 영역: Core 보조 에이전트 10개 파일
> 조사 원칙: 코드 수정 없음, 줄번호·상태 기록만

---

## 담당 파일

```
modules/core/self_reflection.py
modules/core/adversarial_self_play.py
modules/core/chain_of_verification.py
modules/core/cross_agent_verifier.py
modules/core/multi_agent_deliberation.py
modules/core/tree_of_thoughts.py
modules/core/confidence_calibration.py
modules/core/narrative_structure_analyzer.py
modules/core/agent_intelligence.py
modules/core/expert_mixture.py
```

---

## 전용 체크 항목 조사 결과

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|------------|------|------|------|
| BA-1 | adversarial_self_play.py:137 | - | OK | `model: str = "gemini-2.5-flash"` + `# [SSOT-P2]` 주석 — 호출부(main_a.py:L1929) model 명시 주입 확인 |
| BA-2 | chain_of_verification.py:123 | - | OK | `model: str = "gemini-2.5-flash"` + `# [SSOT-P2]` 주석 — 호출부(main_a.py:L1906) model 명시 주입 확인 |
| BA-3 | cross_agent_verifier.py:119 | - | OK | `model: str = "gemini-2.5-flash"` + `# [SSOT-P2]` 주석 — 호출부(main_a.py:L1895) model 명시 주입 확인 |
| BA-4 | multi_agent_deliberation.py:183 | - | OK | `model: str = "gemini-2.5-flash"` + `# [SSOT-P2]` 주석 — 호출부(main_a.py:L1936) model 명시 주입 확인 |
| BA-5 | tree_of_thoughts.py:152 | - | OK | `model: str = AIModels.DEFAULT_ARCHITECT` — SSOT 사용 |
| BA-6 | confidence_calibration.py:86 | - | OK | `model: str = AIModels.FLASH_ANALYSIS_MODEL` — SSOT 사용, 빈 문자열 방어(L95) 포함 |
| BA-7 | self_reflection.py:52 | - | OK | `model: str = AIModels.DEFAULT_ARCHITECT` — SSOT 사용 |
| BA-8 | narrative_structure_analyzer.py:70 | - | OK | `model: str = AIModels.DEFAULT_ARCHITECT` — SSOT 사용 |
| BA-9 | cross_agent_verifier.py:332 | P2 | GAP | Python이 `should_regenerate` 직접 결정(score+violations 기반) — advisory 값이나 대원칙 1 soft 노출 |
| BA-10 | multi_agent_deliberation.py:319 | P2 | GAP | Python이 `consensus_reached` 직접 결정(`all_pass or avg>=70`) — advisory 값이나 대원칙 1 soft 노출 |
| BA-11 | agent_intelligence.py:536 | - | OK | `quick_quality_check()` Python-only, 결과는 권고값, 판단은 호출부 — 대원칙 1 준수 |
| BA-12 | expert_mixture.py:256 | - | OK | Python-only, LLM 없음, 프롬프트 생성·씬 분류만 담당 — 대원칙 1 준수 |

---

## 공통 체크 항목 조사 결과

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|------------|------|------|------|
| 공통-A | adversarial_self_play.py:188 | P1 | BUG | `_get_adversary_feedback()` 파싱 실패 시 `decision="PASS"` 기본값 — 로깅 없는 fail-open, 검증 없이 통과 |
| 공통-A | chain_of_verification.py:145 | P1 | BUG | `_parse_result()` 파싱 실패 시 `{"passed": True}` 기본값 — fail-open, 검증 없이 통과 |
| 공통-A | self_reflection.py:189 | P1 | BUG | `_parse_critique()` 파싱 실패 시 `{"issues": [], "severity": "none"}` 기본값 — fail-open, 이슈 없는 것으로 처리 |
| 공통-A | cross_agent_verifier.py:137 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-A | multi_agent_deliberation.py:201 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-A | tree_of_thoughts.py:172 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-A | narrative_structure_analyzer.py:191 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-B | adversarial_self_play.py:188 | P1 | BUG | LLM 파싱 실패 → fail-open(PASS 기본값) — 공통-A와 동일 항목 |
| 공통-B | chain_of_verification.py:145 | P1 | BUG | LLM 파싱 실패 → fail-open(passed:True) — 공통-A와 동일 항목 |
| 공통-B | cross_agent_verifier.py:157 | - | OK | 파싱 실패 시 `compliance_score: 0.7` 중립값 반환 (fail-open 아님) |
| 공통-C | adversarial_self_play.py:80 | P1 | BUG | ADVERSARY_PROMPT 내 "무협 소설 평가 기준" 하드코딩 — 비무협 장르에서도 무협 기준 평가, TF-45 미포함 |
| 공통-C | chain_of_verification.py:270 | P2 | GAP | Python이 `should_regenerate = overall_severity == CRITICAL` 직접 결정 — 대원칙 1 soft 위반 |
| 공통-D | tree_of_thoughts.py:152 | - | OK | SSOT 사용 확인 |
| 공통-D | confidence_calibration.py:86 | - | OK | SSOT 사용 확인 |
| 공통-E | confidence_calibration.py:80 | P2 | GAP | `THRESHOLDS` 클래스 상수 하드코딩(50/30/85) — validation.yaml 외부화 미완 |
| 공통-F | (전체) | - | OK | DB 직접 커넥션 없음 — 10개 파일 전량 |
| 공통-G | agent_intelligence.py:229 | P1 | BUG | `analyst/writer_exemplars.get(genre, wuxia_fallback)` — 비무협 장르에서 wuxia 예시(복수극/경지돌파)·안티패턴("내공/경지 숫자 나열") 폴백 오염 |
| 공통-H | narrative_structure_analyzer.py:100 | - | OK | LLM 추출 실패 → `{"status": "PASS", "fallback": True}` — 보수적 PASS, 로깅 있음 (안전 설계) |

---

## 감리 3회 기록

### 감리 1회 — CLAUDE.md 명시 의도적 설계 제거
- BA-1~4: P2 유보 7개 목록(adversarial_self_play/chain_of_verification/cross_agent_verifier/multi_agent_deliberation 포함) — **오탐 제거, SSOT-P2 OK**
- adversarial_self_play.py L80 무협 프롬프트: TF-45 비무협 오염 근절 7건 패치 대상(`analyst.yaml`/`analyst_prompts.py`/`response_schemas.py` 등)에 이 파일 미포함 — **BUG 유지**
- fail-open 이슈들(L188/L145/L189): 9차 전수조사 "silent except 5건" 패치는 LLM 호출 예외 처리 보강이며, 파싱 실패 시 기본값 문제는 별개 이슈 — **BUG 유지**

### 감리 2회 — 테스트 커버리지 및 사용처 확인
- BA-9/BA-10/공통-C(chain_of_verification:270): `should_regenerate`/`consensus_reached`는 Python 결정이나, 최종 판단은 호출부(Director 체인)가 담당 → advisory 수준으로 대원칙 1 hard 위반 아님 — **P2 GAP 유지**
- agent_intelligence.py wuxia 폴백: writer_exemplars 자체가 wuxia 전용("내공/경지 숫자 나열", "발경" 등), 비무협 장르에서 무협 예시 Few-Shot 주입 → TF-45와 동일 성격 — **P1 BUG 확정**
- chain_of_verification.py L145 `passed:True`: P2 유보 파일로 운영 경로에서 호출됨, 파싱 실패 시 통과 → 실질적 검증 공백 — **P1 BUG 유지**

### 감리 3회 — 대원칙 1~4 준수 최종 확인
- 공통-C(adversarial_self_play:80): TruthGate/Director가 2차 방어하나, 검증 에이전트 자체가 무협 기준으로 비무협 작품 평가하는 것은 대원칙 위반은 아니나 품질 저하 — **P1 BUG 확정**
- 공통-A fail-open 3건: 검증 에이전트 파싱 실패 시 "문제 없음" 기본값은 Director 최종 판단 전 잘못된 advisory 주입 가능성 — **P1 BUG 확정**

---

## 확정 BUG/GAP 항목 (감리 완료)

| 번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|------|------------|------|------|------|
| 01 | adversarial_self_play.py:80 | P1 | BUG | ADVERSARY_PROMPT "무협 소설 평가 기준" 하드코딩 — 비무협 장르에서 무협 기준 평가, TF-45 미포함 |
| 02 | adversarial_self_play.py:188 | P1 | BUG | `_get_adversary_feedback()` 파싱 실패 시 `decision="PASS"` fail-open — 로깅 없이 검증 없이 통과 |
| 03 | chain_of_verification.py:145 | P1 | BUG | `_parse_result()` 파싱 실패 시 `{"passed": True}` fail-open — 검증 없이 통과 |
| 04 | self_reflection.py:189 | P1 | BUG | `_parse_critique()` 파싱 실패 시 `issues=[]` fail-open — 이슈 없는 것으로 처리 |
| 05 | agent_intelligence.py:229 | P1 | BUG | 비무협 장르에서 wuxia 예시/안티패턴 폴백 — Few-Shot에 무협 용어("내공/경지 숫자 나열") 주입, TF-45 동일 성격 미포함 |
| 06 | cross_agent_verifier.py:332 | P2 | GAP | Python이 `should_regenerate` 직접 결정 — advisory 값이나 대원칙 1 주석 명시 미비 |
| 07 | multi_agent_deliberation.py:319 | P2 | GAP | Python이 `consensus_reached` 직접 결정 — advisory 값이나 대원칙 1 주석 명시 미비 |
| 08 | chain_of_verification.py:270 | P2 | GAP | Python이 `should_regenerate = CRITICAL` 직접 결정 — advisory 수준이나 주석 미비 |
| 09 | confidence_calibration.py:80 | P2 | GAP | `THRESHOLDS` 클래스 상수 하드코딩(50/30/85) — validation.yaml 외부화 미완 |

---

## TF 목록

| TF ID | 등급 | 파일:줄 | 패치 명세 |
|-------|------|---------|---------|
| TF-12-01 | P1 | adversarial_self_play.py:80 | ADVERSARY_PROMPT "무협 소설 평가 기준" → `{genre_evaluation_criteria}` 플레이스홀더 교체, 비무협 시 장르 중립 평가 기준 주입 |
| TF-12-02 | P1 | adversarial_self_play.py:188 | 파싱 실패 시 `decision="PASS"` → `decision="FAIL"` (fail-closed) + `logging.warning("[AdversarialSelfPlay] 파싱 실패, FAIL 폴백")` |
| TF-12-03 | P1 | chain_of_verification.py:145 | 파싱 실패 시 `"passed": True` → `"passed": False` (fail-closed) + `logging.warning("[CoV] 파싱 실패, FAIL 폴백")` |
| TF-12-04 | P1 | self_reflection.py:189 | 파싱 실패 시 `issues=[]` 유지하되 `logging.warning("[SelfReflection] 파싱 실패, 빈 이슈 폴백")` 추가 |
| TF-12-05 | P1 | agent_intelligence.py:229 | wuxia 폴백 제거 또는 비무협 장르별 중립 예시 추가, `wuxia` fallback 대신 `[]` 반환 + 로깅 |
| TF-12-06 | P2 | cross_agent_verifier.py:332 | `should_regenerate` 결정 블록에 `# [대원칙 1 advisory-only]` 주석 추가 |
| TF-12-07 | P2 | multi_agent_deliberation.py:319 | `consensus_reached` 결정 라인에 `# [대원칙 1 advisory-only]` 주석 추가 |
| TF-12-08 | P2 | chain_of_verification.py:270 | `should_regenerate` 결정 라인에 `# [대원칙 1 advisory-only]` 주석 추가 |
| TF-12-09 | P2 | confidence_calibration.py:80 | `THRESHOLDS` → `_threshold("confidence.extra_verification", 50)` 등 validation.yaml 키 추가 |

---

## 요약

- **P0**: 0건
- **P1 BUG**: 5건 (TF-12-01~05: 무협 오염 1건 + fail-open 3건 + wuxia 예시 폴백 1건)
- **P2 GAP**: 4건 (TF-12-06~09: 대원칙 1 주석 미비 3건 + 임계값 하드코딩 1건)
- **OK**: BA-1~8, BA-11~12, 공통 다수 — SSOT 전량 정상, LLM 호출 예외 처리 전량 로깅 완비
