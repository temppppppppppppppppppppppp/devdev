# 전수조사 4차 마스터 오더 — 0번 터미널 발령

> 발령일: 2026-03-06
> 발령자: 0번 터미널 (총괄 감리)
> 목적: 1차(터미널 1~10) + 3차(터미널 11~16) 전수 후 누락된 미조사 영역 전량 검수
> 기준 커밋: `9a55ac0`
> 선행 특별 조사: `2차_S2_preflight_enrichment_조사.md` (stage2_preflight._preflight_enrichment 경로, 완료)

---

## 누락 파일 발굴 경위

1차(1~10번)·3차(11~16번) 마스터 오더와 실제 모듈 파일 목록을 교차 대조한 결과,
아래 파일들이 어떤 터미널에도 배정되지 않았음을 확인.

| 파일 | 누락 사유 | 중요도 |
|------|----------|--------|
| `modules/domain/agents/director_ensemble.py` | 6번 터미널에서 director.py 계열 담당했으나 이 파일만 빠짐 | **최고** |
| `modules/domain/agents/director_continuity.py` | 동일 — Context Caching 사용 중(CLAUDE.md 명시)이나 미배정 | **높음** |
| `modules/core/config_manager.py` | 6차 전수조사 P1 패치 이력 있으나 3차 오더 미포함 | **높음** |
| `modules/core/quality_dashboard.py` | 어떤 오더에도 미포함 | 중간 |
| `modules/core/response_schemas.py` | TF-45 언급(status_shadow 중립화) 이력 있으나 미배정 | 중간 |
| `modules/core/tactical_utils.py` | 어떤 오더에도 미포함 | 미상 |
| `modules/core/spinners.py` | stage0/spinner.py(11번 커버)와 별개 파일, 미포함 | 낮음 |
| `modules/protocols/db_repository.py` | 10번에서 agents/validators/app_services만 커버 | 중간 |

---

## 공통 작업 수칙 (1~3차와 동일)

```
1단계. TF 구성하며 조사
   → 형식: TF-[터미널번호]-[순번] | 등급 | 파일:줄번호 | BUG/GAP | 설명 | 패치 방향

2단계. 감리 1회 — CLAUDE.md 명시 의도적 설계 오탐 제거
3단계. 감리 2회 — 대원칙 1~4 준수 여부 재확인
4단계. 감리 3회 — 기존 테스트 커버 여부 확인, 최종 등급 확정
5단계. 문서화 후 0번 터미널에 보고
```

> **코드 수정은 0번 터미널(Claude)만 수행.**

---

## 17번 터미널 — 누락 Director 에이전트 2종

**배경**: 6번 터미널이 Director 계열 11개 파일을 담당했으나
`director_ensemble.py`·`director_continuity.py` 2개가 명시적으로 누락됨.
두 파일 모두 CLAUDE.md에서 핵심 패치 대상으로 여러 차례 언급.

**담당 파일:**
```
modules/domain/agents/director_ensemble.py   # Director 앙상블 + 최종 선택
modules/domain/agents/director_continuity.py # Director 연속성 + Context Caching
```

**전용 체크 항목:**
```
[DE-1]  director_ensemble — select_and_judge_ensemble() 반환 dict에 contradiction_types 포함 여부 (A-4)
[DE-2]  director_ensemble — score_breakdown 합산 ≠ score 시 breakdown 우선 자동 교정 위치 (_sb_sum > 0 조건 확인)
[DE-3]  director_ensemble — SCM 단일 후보 독점 방지: qualified_indices==1 시 경고 주입 + score 95→90 보정 실행 여부
[DE-4]  director_ensemble — NC-3 consistency_checklist ISSUE 3건+ python_warnings 상한 3점 적용 경로
[DE-5]  director_ensemble — NC-3 timeline_arc_consistency 11번째 키 포함 여부 (_nc3_keys 목록)
[DE-6]  director_ensemble — NC-3B few-shot golden example 삽입 위치 (4곳 중 ensemble 담당 위치)
[DE-7]  director_ensemble — PASS_WITH_FIX 반환 시 fix_scope/feedback 앙상블 결과에 전파 여부
[DE-8]  director_ensemble — 앙상블 timeout 만료 시 partial 결과 처리 vs silent 실패 (YAML 외부화 확인)
[DE-9]  director_ensemble — 모델 SSOT: select_and_judge_ensemble() 내부 LLM 호출 AIModels.* 사용 여부
[DE-10] director_continuity — Context Caching: _get_or_create_context_cache()/_ask_with_cached_context() 실제 사용 경로
[DE-11] director_continuity — 캐시 만료(TTL) 후 재생성 실패 시 fallback 처리
[DE-12] director_continuity — 연속성 검사 결과가 advisory인지 vs Python이 자동 REJECT하는지 (대원칙 3)
[DE-13] director_continuity — NPC 사망 연속성 체크: deceased 필드 확인 → 행동 등장 REJECT (대원칙 4)
[DE-14] director_continuity — LLM 파싱 실패 시 fail-closed vs fail-open 분기
```

**저장**: `docs/2026-03-06/2차_17번터미널.md`

---

## 18번 터미널 — 누락 Core 보조 파일 + Protocol

**배경**: 1~3차 오더에서 배정되지 않은 Core 파일 6개 + Protocol 1개.
일부(config_manager, response_schemas)는 CLAUDE.md 패치 이력이 있어 실질적 위험이 존재.

**담당 파일:**
```
modules/core/config_manager.py       # 에이전트 설정 동적 로드 (6차 전수조사 P1 패치 이력)
modules/core/quality_dashboard.py    # 품질 메트릭 대시보드
modules/core/response_schemas.py     # LLM 응답 스키마 정의 (TF-45 status_shadow 중립화 이력)
modules/core/tactical_utils.py       # 전술 유틸리티 (내용 미상)
modules/core/spinners.py             # UI 스피너 (stage0/spinner.py와 별개)
modules/protocols/db_repository.py   # DB 저장소 Protocol
```

**전용 체크 항목:**
```
[CM-1]  config_manager — _load_agents_from_yaml() 동적 로드: 파일 없음/키 누락 시 fallback 정렬 (6차 P1 패치 확인)
[CM-2]  config_manager — 로드된 에이전트 모델명이 models.yaml 키와 일치하는지 (SSOT 위반 가능성)
[CM-3]  config_manager — 로드 실패 시 except Exception: pass 여부 (silent 실패 위험)
[CM-4]  config_manager — 싱글톤/캐시 패턴 사용 시 스레드 안전성

[QD-1]  quality_dashboard — 품질 점수 저장 경로: DB 쓰기 실패 시 silent 처리 여부
[QD-2]  quality_dashboard — get_score_trend_summary() 반환값이 stage2_preflight L467에서 except pass로 처리되는지 (S2PE-07 연관)
[QD-3]  quality_dashboard — Python이 직접 품질 합격/불합격을 결정하는지 (대원칙 1·3)
[QD-4]  quality_dashboard — 메트릭 조회 중 division-by-zero 방어

[RS-1]  response_schemas — status_shadow 스키마에서 internal_energy_loss → key_stat_change 교체 완료 여부 (TF-45)
[RS-2]  response_schemas — 하위 호환 브릿지(internal_energy_loss alias) 존재 확인 (TF-45 설계 의도)
[RS-3]  response_schemas — ARC_DESIGN_SCHEMA 반환 키 목록과 four_phase_arc_generator 파싱 키 일치 여부 (S2-6 연관)
[RS-4]  response_schemas — DIRECTOR_AUDIT_SCHEMA에 consistency_checklist optional 처리 여부 (TF-C)
[RS-5]  response_schemas — 스키마 변경 이력 중 하위 호환 브릿지 없이 삭제된 키 여부

[TU-1]  tactical_utils — 모듈 역할 파악 + dead code 여부 확인
[TU-2]  tactical_utils — Python이 직접 전술 판단하는 코드 여부 (대원칙 1)
[TU-3]  tactical_utils — 사용처(호출 파일) 확인: 실제 운영 경로 포함 여부

[SP-1]  spinners.py — stage0/spinner.py와 기능 중복 여부 (dead code 가능성)
[SP-2]  spinners.py — 사용처 확인: main_a.py 또는 core 모듈에서 실제 import 여부

[DR-1]  db_repository.py — Protocol 정의 키 목록 vs db_manager.py 실제 구현 메서드 일치 여부
[DR-2]  db_repository.py — 구현체(db_manager.py)의 Protocol 준수 여부 (B-3 표준화 이후)
[DR-3]  db_repository.py — dead code 여부: Python 전체에서 import하는 파일 있는지
```

**저장**: `docs/2026-03-06/2차_18번터미널.md`

---

## 0번 터미널 — 4차 취합 프로세스

17~18번 터미널 완료 후:
1. 1~3차 + S2PE + 4차 전체 TF 통합
2. director_ensemble↔director_auditor, config_manager↔constants.py 등 교차 참조 검증
3. 최종 오탐 제거 3회 후 통합 패치 오더 발행

**최종 보고서**: `docs/2026-03-06/2차_0번터미널_최종취합.md` (기존 파일에 4차 내용 추가)

---

## 4차 패치 완료 현황 (0번 터미널 직접 수행 — 2026-03-06)

> 감리 3회 완료. 오탐 없음. 3개 패치 모두 정당.

| TF | 파일 | 변경 내용 | 감리 결론 |
|----|------|----------|----------|
| TF-17-05 | `modules/core/response_schemas.py:159` | `fiction_term_leak` 스키마 키 추가 → NC-3 4th wall 체크 활성화 | 오탐 아님 ✅ |
| TF-18-10A | `modules/core/arc_summary_utils.py:56` | `shadow.get("key_stat_change") or shadow.get("internal_energy_loss", "0%")` | 오탐 아님 ✅ |
| TF-18-10B | `modules/core/fact_ledger.py:292` | `shadow.get("key_stat_change") or shadow.get("internal_energy_loss")` | 오탐 아님 ✅ |

**대원칙 준수**: 전량 ✅ (스키마 추가 + 데이터 수집만, 판단 없음)
**테스트 커버**: P2 (fiction_term_leak 단위 테스트 + key_stat_change 브릿지 테스트 신규 권장)

---

## 4차 이후 발굴된 미조사 영역

> git diff HEAD 전수 비교 결과 — 기존 1~4차 오더 미배정 파일 3개 확인

| 파일 | 클래스 | 미배정 사유 |
|------|--------|------------|
| `modules/domain/agents/director_grading.py` | `DirectorGradingSystem` | Director 계열이나 6번/17번 오더에 누락 |
| `modules/domain/agents/manager.py` | `Manager(BaseAgent)` | BaseAgent 계열이나 어떤 오더에도 미포함 |
| `modules/core/state_delta_tracker.py` | `StateDeltaTracker` | 에너지/부상 델타 추적기, 미포함 |

> 신규 배포 코드 (실전모순패치 이후) — 기존 터미널 조사 범위 밖 신규 구현:
> - `chief_writer_quality._check_system_term_exposure()` (10번째 self-critique)
> - `numeric_consistency_checker._check_leverage_return_pct()` + `_LEVERAGE_PCT_RE`
> - `four_phase_arc_generator._ns4_extract_time_markers()` (NS-4-S2)
> - `stage4_interview_round` arc_pos==1 mandatory_context prepend + NS-4-S4
> - `director.yaml` Arc 경계 공간연속성 2곳 + `chief_writer.yaml` 규칙14
> - **미구현 P1 잔여**: `four_phase_arc_generator._generate_prev_context()` → `arc_start_state.location` 강제 주입

**→ 5차 마스터 오더 발령**: `docs/2026-03-06/00_전수조사_5차_마스터_오더.md`

---

## 커버리지 완결 확인표

| 영역 | 1차 오더 | 3차 오더 | 4차 오더 | S2PE | 비고 |
|------|----------|----------|----------|------|------|
| Stage 2 파이프라인 | ✅ 1번 | — | — | ✅ 심층 보완 | |
| Arc 생성/검증 | ✅ 2번 | — | — | — | |
| Blueprint/Continuity | ✅ 3번 | — | — | — | |
| Stage 4 오케스트레이터 | ✅ 4번 | — | — | — | |
| Chief Writer/Writer | ✅ 5번 | — | — | — | |
| Director 본체 체인 | ✅ 6번 | — | — | — | |
| Director 앙상블/연속성 | ❌ | ❌ | ✅ 17번 | — | **누락 확인** |
| Advisory 체인 | ✅ 7번 | — | — | — | |
| Memory/DB/상태 | ✅ 8번 | — | — | — | |
| Validation/Guard | ✅ 9번 | — | — | — | |
| Config/SSOT/Base | ✅ 10번 | — | — | — | |
| Stage 0 전체 | ❌ | ✅ 11번 | — | — | |
| 보조 에이전트 (P2 유보) | ❌ | ✅ 12번 | — | — | |
| 추적/관계/캐릭터 | ❌ | ✅ 13번 | — | — | |
| 품질/스타일/장르/HUD | ❌ | ✅ 14번 | — | — | |
| 운영/모니터링/lite_mode | ❌ | ✅ 15번 | — | — | |
| Config 잔여 + Tests | ❌ | ✅ 16번 | — | — | |
| config_manager | ❌ | ❌ | ✅ 18번 | — | **누락 확인** |
| quality_dashboard | ❌ | ❌ | ✅ 18번 | — | **누락 확인** |
| response_schemas | ❌ | ❌ | ✅ 18번 | — | **누락 확인** |
| tactical_utils / spinners | ❌ | ❌ | ✅ 18번 | — | **누락 확인** |
| protocols/db_repository | ❌ | ❌ | ✅ 18번 | — | **누락 확인** |
