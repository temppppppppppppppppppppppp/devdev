# 2차 전수조사 — 11번·16번 터미널 통합 보고서

> 통합 기준 커밋: `9a55ac0`
> 통합 작성일: 2026-03-06
> 원본: `2차_11번터미널.md` (13개 파일), `2차_16번터미널.md` (Config 잔여 + Tests)
> 감리: 통합 후 3회 추가 완료
> 결론: **P0 0건 / P1 9건 / P2 10건 / OK 28건 / 오탐 제거 1건**

---

## 담당 파일 목록

### 11번 터미널 담당 (Stage 0 파이프라인 + Services + 나머지 Domain Agents)

```
modules/core/stage0/__init__.py           (578줄)
modules/core/stage0/story_expander.py    (562줄)
modules/core/stage0/reverse_expander.py  (대형)
modules/core/stage0/style_extractor.py   (798줄)
modules/core/stage0/preset_registry.py   (737줄)
modules/core/stage0/spinner.py
modules/core/services/audit_service.py   (93줄)
modules/core/services/state_service.py   (368줄)
modules/core/services/ui_service.py      (138줄)
modules/core/services/project_service.py (370줄)
modules/domain/agents/block_enricher.py  (924줄)
modules/domain/agents/negative_example_injector.py (405줄)
modules/domain/models.py                 (63줄)
```

### 16번 터미널 담당 (Config 잔여 + Tests 핵심 감사)

```
config/prompts/analyst_libraries.json
config/prompts/analyst_libraries_investment.json
config/prompts/architect_rules.json
config/prompts/weaver_rules.json
config/prompts/writer.json
config/prompts/writer_rules.json
config/genres/*.yaml (10개)
config/smart_retrieval/genre_hints.yaml
config/system.yaml
config/settings.json
tests/conftest.py
tests/test_integrity.py
tests/test_protocol_conformance.py
tests/test_phase5_hygiene.py
tests/test_pass_with_fix.py
tests/test_pipeline_wiring.py
tests/e2e/test_l3_*.py (존재 확인)
```

---

## 전체 TF 목록 (통합 감리 3회 완료 후 확정)

### P1 항목 (9건) — 11번 터미널

| TF번호 | 등급 | 파일:줄 | 종류 | 설명 | 패치 방향 |
|--------|------|---------|------|------|-----------|
| TF-11-01 | P1 | block_enricher.py:476,519,831 | GAP | `"gemini-2.5-flash"` 하드코딩 3곳 — `_validate_enrichment`, `_director_audit_block`, `_check_causal_errors`에서 `AIModels.*` 미사용 | `AIModels.SUMMARY_MODEL` 또는 `AIModels.V50_MODULE_MODEL`로 교체 |
| TF-11-02 | P1 | block_enricher.py:451 | GAP | `enrich_block()` 최외곽 `except Exception as e: return {"enriched": False, ...}` — `logging.warning` 없음 | `logging.warning("enrich_block failed: %s", e)` 추가 |
| TF-11-03 | P1 | block_enricher.py:486 | GAP | `_validate_enrichment()` `except Exception as e: return {"validation_result": "FAIL", ...}` — `logging.warning` 없음 | `logging.warning("_validate_enrichment failed: %s", e)` 추가 |
| TF-11-04 | P1 | block_enricher.py:540 | GAP | `_director_audit_block()` `except Exception as e: return {"decision": "PASS", ...}` — `logging.warning` 없음 | `logging.warning("_director_audit_block failed: %s", e)` 추가 |
| TF-11-05 | P1 | block_enricher.py:528-536 | GAP | **[대원칙 3 위반]** Python이 LLM `decision` 필드를 `total_score >= 70` 하드코딩 임계값으로 덮어씀 — Director 주권주의 위반 | LLM이 `decision` 직접 반환하도록 프롬프트 강화 + Python 덮어쓰기 제거, score는 참고값으로만 활용 |
| TF-11-06 | P1 | project_service.py:354 | GAP | `wipe_production_data()`에서 `project.db.conn.commit()` 직접 호출 — `_safe_commit()` 우회 | `self._safe_commit(project.db)` 교체 (rollback 보장) |
| TF-11-07 | P1 | project_service.py:215-222 | GAP | `rollback_episode()` — DB commit 성공 후 물리 파일 삭제 시 원자성 미보장, 파일 삭제 실패 시 DB·파일 불일치 | 파일 삭제 실패 시 `logging.error` + `_safe_commit` rollback 시도 경로 추가 |
| TF-11-08 | P1 | stage0/__init__.py:435-437 | GAP | `run_reference_analysis()`의 `style_guide.json` 저장 `open()` — try/except OSError 없음, 디스크 오류 시 비정상 종료 | `try/except OSError as e: logging.warning(...)` 래핑 추가 |
| TF-11-09 | P1 | reverse_expander.py:117 | GAP | `_parse_json()` `except Exception: return None` — bare except, logging 없음 (story_expander._parse_json은 특정 예외 명시로 일관성 불일치) | `except (json.JSONDecodeError, ValueError, IndexError) as e: logging.debug("_parse_json failed: %s", e); return None` |

### P2 항목 (10건) — 11번 4건 + 16번 6건

| TF번호 | 등급 | 파일:줄 | 종류 | 설명 | 패치 방향 |
|--------|------|---------|------|------|-----------|
| TF-11-10 | P2 | preset_registry.py:460 | GAP | `__init__()` 미지원 장르 인자 시 `if base_genre in self.GENRE_PRESETS:` else 묵살 — warning 없음 | `else: logging.warning("PresetRegistry: unknown genre '%s', using common only", base_genre)` 추가 |
| TF-11-11 | P2 | block_enricher.py:533 | GAP | `if total_score >= 70:` 하드코딩 임계값 — validation.yaml 외부화 정책 불일치 (**TF-11-05 선행 패치 시 이 라인 자체 삭제되어 자동 소멸**) | TF-11-05 패치 후 라인 잔존 시 `_threshold("block_audit_pass_score", 70)` 교체 |
| TF-11-12 | P2 | state_service.py:153 | GAP | `load_character_archetypes()` `except Exception as e: print(...)` — `logging.warning` 미사용 | `logging.warning("load_character_archetypes failed: %s", e)` 교체 |
| TF-11-13 | P2 | models.py:52-59 | GAP | **[대원칙 2 형식적 위반]** `Bible.update_martial_status()`가 Python에서 직접 `self.realm`, `self.internal_energy` 수정. 사용처 불명확 (dead code 의심) | 사용처 확인 후 미사용 시 파일 전체 삭제; 사용 중이면 LLM 경유 경로로 이전 |
| TF-16-01 | P2 | config/prompts/architect_rules.json | GAP | 사문화 확인 — 프로덕션 코드(main_a/stage3_orchestrator/blueprint_ensemble) 전량 미로드, 백업·tests/stage3_isolated_test 에서만 참조 | 사용처 없으면 deprecated/ 이동 또는 삭제; blueprint 에이전트에서 로드 필요 시 stage3_orchestrator.py에 로드 로직 추가 |
| TF-16-02 | P2 | config/prompts/writer.json | GAP | 완전 사문화 — Python 전체 코드베이스 참조 0건. writer_rules.json은 정상 참조이나 writer.json은 방치 상태 | Python 전체 참조 0건 재확인 → deprecated/ 이동 또는 삭제 |
| TF-16-03 | P2 | config/settings.json:8-9 | GAP | `scoring_model: "gemini-2.5-pro"` / `advisory_model: "gemini-2.5-pro"` 하드코딩 — director_auditor.py:274-276 default_config에 직접 merge → models.yaml SSOT 우회 가능 | `scoring_model`/`advisory_model` 값을 `models.yaml` 경유 `AIModels.*` 상수로 참조하도록 director_auditor.py L274-276 교체. 또는 settings.json 해당 키 제거 후 models.yaml 단일 참조 |
| TF-16-04 | P2 | director_auditor.py:278 | GAP | settings.json 로드 `except Exception: pass` silent — 9차 전수조사 패턴과 동일하나 당시 누락된 항목 | `except Exception as e: logging.warning("[director_auditor] settings.json 로드 실패: %s", e)` |
| TF-16-05 | P2 | tests/test_integrity.py | GAP | 대원칙 2(팩트시트 수정 권한 LLM만)·대원칙 4(사망 NPC 방어) 전용 위반 탐지 테스트 없음. 10개 케이스 중 대원칙 1/3만 간접 커버 | 대원칙 2 검증 테스트 추가: "Python이 직접 NPC 속성 수정 시 npc_history에 반영되지 않는다" 시뮬레이션. 대원칙 4 검증: "deceased=True NPC가 원고에 행동/대사로 등장 시 REJECT" 시뮬레이션 케이스 추가 |
| TF-16-06 | P2 | tests/test_phase5_hygiene.py | GAP | `_threshold()` 키 vs `config/settings/validation.yaml` 키 일치 검증 테스트 없음 | `validation.yaml` 정의 키를 `_threshold()` 호출로 접근 시 None이 아닌 값 반환하는지 파라메트릭 테스트 추가 |

---

## 통합 감리 기록 (3회)

### 감리 1회 — 교차 참조 충돌·중복 제거

```
[검토] TF-11-01 (block_enricher 모델 하드코딩) vs TF-16-03 (settings.json 모델 하드코딩)
  근거: 서로 다른 파일의 동일 패턴 (block_enricher.py vs director_auditor.py 경유 settings.json)
  판정: 중복 아님 — 각각 독립 파일에서 SSOT 우회. 둘 다 P1/P2 유지

[검토] TF-11-05 (Python score→decision 덮어쓰기) vs TF-11-11 (같은 줄 임계값 하드코딩)
  근거: 동일 block_enricher.py:533을 두 각도에서 지적.
        TF-11-05 (P1) 패치 방향: 해당 라인 전체 제거 (LLM decision 직접 반환).
        TF-11-05 패치 완료 시 TF-11-11 대상 라인이 사라져 자동 소멸됨.
  판정: TF-11-11 P2 유지, "(TF-11-05 선행 패치 시 자동 소멸)" 노트 부착. 오탐 아님.

[검토] TF-11-02~04 (block_enricher silent except) vs TF-16-04 (director_auditor silent except)
  근거: 동일 패턴이나 서로 다른 파일. 9차 전수조사에서 5건 패치 시 director_auditor.py:278 누락.
  판정: 각자 독립 항목. 중복 아님. 두 파일 모두 P1/P2 유지.

[검토] TF-11-13 (models.py 대원칙 2) vs TF-16-05 (test_integrity.py 대원칙 2 테스트 누락)
  근거: TF-11-13은 실제 코드 위반(dead code 의심), TF-16-05는 테스트 커버리지 갭.
        서로 보완 관계이나 별개 항목. TF-11-13이 dead code로 판명 시 TF-16-05의 해당 케이스도 의미 상실.
  판정: 둘 다 P2 유지 (dead code 확인 후 연동 처리 권장)
```

### 감리 2회 — 대원칙 1~4 준수 재확인

```
[TF-11-05] 대원칙 3 "Director 주권주의" — Python이 total_score >= 70 비교로 decision 덮어씀.
  LLM이 total_score 반환 → Python이 임계값 비교 → PASS/REJECT 자동 결정 경로.
  이것은 Director를 우회하는 Python 자동 판정임. → P1 유지 확정.

[TF-11-13] 대원칙 2 "팩트시트 수정 권한은 LLM만" — Bible.update_martial_status().
  self.realm, self.internal_energy 직접 수정은 형식적 위반.
  단, 해당 클래스가 dead code일 경우 실질적 영향 없음. → P2 유지, dead code 판별 선행 필요.

[TF-16-05] 대원칙 2·4 테스트 누락.
  test_integrity.py가 대원칙 1/3만 간접 커버하여 2/4 위반 탐지 테스트 부재.
  이는 회귀 안전망 갭이지 현재 코드 위반은 아님. → P2 유지 (테스트 보강 권고).

[TF-16-03] Model SSOT — settings.json 모델명이 models.yaml 우회.
  대원칙 직접 위반은 아니나 CLAUDE.md "LLM 모델 설정 SSOT 완료" 정책 위반.
  director_auditor.py:274-276이 settings.json 값을 default_config에 merge하는 경로가 존재.
  → P2 유지 확정.
```

### 감리 3회 — 오탐 최종 판별·기존 테스트 커버 확인

```
[TF-11-07] project_service.py rollback_episode() 원자성 갭.
  재검토: OS 레벨 파일 삭제 실패는 드물지만 NFS·권한 문제 등 실제 발생 가능.
  기존 테스트: rollback_episode 파일 삭제 실패 + DB commit 완료 시나리오 미커버.
  판정: P1 유지 — 오탐 아님.

[TF-11-11] block_enricher.py:533 임계값 하드코딩.
  TF-11-05 패치 전까지는 해당 라인 존재. 테스트 미커버.
  판정: P2 유지 (TF-11-05 패치 전까지 독립 항목) — 오탐 아님.

[TF-11-13] models.py Bible.update_martial_status() dead code 의심.
  테스트 미커버. 실제 사용처 grep 확인 필요 → 문서 내 판별 불가.
  판정: P2 유지 (구현 단계에서 사용처 grep 후 처리 결정).

[TF-16-01] architect_rules.json 사문화.
  프로덕션 코드 전량 미로드 확인됨 (backup + isolated_test만 참조).
  판정: P2 유지 — 오탐 아님.

[TF-16-02] writer.json 사문화.
  Python 전체 참조 0건 확인됨.
  판정: P2 유지 — 오탐 아님.

[CF-7b = TF-16-04] director_auditor.py:278 silent except.
  9차 전수조사 5건 패치 시 해당 파일 누락 확인됨.
  판정: P2 유지 — 오탐 아님.

오탐 제거 결과: 0건 추가 제거 (11번 원본 1건 기 제거 포함 총 1건)
```

---

## OK 확인 항목 (28건)

### 11번 터미널 OK (14건)

| 체크 항목 | 근거 |
|-----------|------|
| S0-1 — PresetRegistry → StateTracker 연동 | CLAUDE.md "Stage 0 ↔ Stage 2 연동: StateTracker가 PresetRegistry 참조" 완료 명시 ✅ |
| S0-2 — Bible 파싱 실패 폴백 | `__init__.py:237-239` fail-closed ✅ / `story_expander.generate_bible():172` protagonist 빈값 시 `return None` ✅ |
| S0-3 — ReverseExpander VecMemory 실패 방어 | VecMemory 직접 호출 없음. main_a.py 위임 설계 의도 확인 ✅ |
| S0-4 — Arc/Blueprint 역추출 스킵 | `reverse_expander.py` docstring 명시 + 구현 내 Arc 생성 로직 없음 ✅ |
| S0-7 — audit_service DB 트랜잭션 | JSONL 파일 기록 방식, DB 트랜잭션 없음. `flush_audit_buffer():68` except 처리 ✅ |
| S0-10 — block_enricher 원본 블록 보존 | `enrich_block():348-358` `return {"enriched": False, "block": current_block}` 폴백 ✅ |
| S0-11 — negative_example_injector 대원칙 1 준수 | `generate_injection()` 텍스트 반환만. `_rejection_lock` 동시성 보호. Python 판단 없음 ✅ |
| S0-12 — story_expander `_parse_json` 예외 특정 | `except (json.JSONDecodeError, ValueError, IndexError): return None` — 특정 예외 명시 ✅ |
| 공통-A project_service traceback.print_exc | rollback_episode() 사용자 대면 에러 출력 맥락, UI 레이어 의도적 사용 — **오탐 제거** ✅ |
| 공통-B — LLM 파싱 폴백 | block_enricher 전체 `_extract_json_robust()` 사용 ✅ / story_expander `_parse_json` 폴백 ✅ |
| 공통-D — story_expander 모델 SSOT | `AIModels.SUMMARY_MODEL`, `AIModels.V50_MODULE_MODEL`, `AIModels.TIER_1_ARCHITECT` 사용 ✅ |
| 공통-F — audit_service 리소스 | JSONL append 방식, DB 커넥션 없음 ✅ |
| 공통-G — spinner.py dead code | Rich UI 유틸, 적극 사용 중 ✅ |
| 공통-H — 사망 NPC 방어 | Stage 0은 Bible/Treatment 생성 단계 (원고 미생성). 사망 NPC 행동 노출 경로 없음 ✅ |

### 16번 터미널 OK (14건)

| 체크 항목 | 근거 |
|-----------|------|
| CF-1 — analyst_libraries_investment.json | 서사 패턴 라이브러리 (LLM용). NC-1 `_check_leverage_return_pct`는 Python regex 별도 처리 — 수치 검증용이 아니므로 정합성 문제 없음 ✅ |
| CF-3 — weaver_rules.json | `weaver.py:97` 정상 로드 + except 처리 완비 ✅ |
| CF-4b — writer_rules.json | `main_a.py:1123` + `writer.py:242` 정상 로드, 파싱 실패 시 logging.warning ✅ |
| CF-5 — config/genres/*.yaml (10개) | 10종 전량 존재. `genre_name`/`forbidden_terms`/`mandatory_concepts` 완비. test_genre_yaml_loading.py 전수 검증 PASS ✅ |
| CF-6 — config/system.yaml | `base_agent.py` import-time 로드. `thinking_budget_map`/`api.*`/`network_retry`/`key_rotation`/`cache`/`ensemble_timeouts` 전량 확인 ✅ |
| CF-8 — genre_hints.yaml | `context_advisor.py:23` 정상 로드 + 로드 실패 시 logging.debug + 하드코딩 폴백 완비 ✅ |
| CF-9 — conftest.py | `mock_db_manager` temp_dir 기반 실전 DB 격리 완벽. 실전 DB 직접 연결 fixture 없음 ✅ |
| CF-11 — test_protocol_conformance.py | B-3 표준화 이후 Protocol 구조적 서브타이핑 검증 충분 ✅ |
| CF-12 — test_pass_with_fix.py | mock 3후보 생성 + MV/CV/BV/ContV 모두 mock 완비. fixture 준비 정상 ✅ |
| CF-13 — test_pipeline_wiring.py | TF-54/55b/합격률 8개 테스트 정상 실행 (3,390 passed 기준선 포함) ✅ |
| CF-15a — test_ns4 | 파일명 다름: `test_ns4_timeline_arc.py` + `test_ns4_s2_s4.py` 분리 존재. NS-4 핵심 케이스 커버됨 ✅ |
| CF-15b — test_numeric_consistency | 파일명 다름: `test_numeric_consistency_checker.py` 존재. NC-1/NC-2 핵심 케이스 커버됨 ✅ |
| CF-analyst-wuxia — analyst_libraries_wuxia.json | 파일 없음 (정상) — 무협은 analyst_libraries.json 공통 + config/genres/wuxia.yaml 가드로 처리 ✅ |
| CF-tone_presets — tone_presets.json | CLAUDE.md 별도 검수 지시 없음. 참조 파일 확인 범위 외 ✅ |

---

## 최종 요약

| 등급 | 건수 | 상세 |
|------|------|------|
| P0 | 0건 | — |
| P1 | 9건 | TF-11-01~09 (11번: 모델 SSOT 1 + silent except 3 + 대원칙 3 위반 1 + DB 안전성 2 + OSError 방어 1 + bare except 1) |
| P2 | 10건 | TF-11-10~13 (11번: 장르 묵살 + 임계값 외부화 + print 잔존 + dead code 대원칙 2) + TF-16-01~06 (16번: 사문화 2 + SSOT 우회 + silent except + 테스트 커버 갭 2) |
| OK | 28건 | 11번 14건 + 16번 14건 |
| 오탐 제거 | 1건 | project_service.py traceback.print_exc — UI 레이어 의도적 설계 |

### 패치 우선순위 권고

```
즉시 처리 (P1 — 9건):
  TF-11-01 block_enricher 모델 SSOT (3곳)
  TF-11-02 block_enricher enrich_block logging
  TF-11-03 block_enricher _validate_enrichment logging
  TF-11-04 block_enricher _director_audit_block logging
  TF-11-05 block_enricher Python decision 덮어쓰기 제거 [대원칙 3]
  TF-11-06 project_service _safe_commit 교체
  TF-11-07 project_service 파일 삭제 원자성 보강
  TF-11-08 stage0/__init__ OSError 방어
  TF-11-09 reverse_expander bare except 교체

후속 처리 (P2 — 10건):
  TF-11-10 preset_registry 미지원 장르 warning
  TF-11-11 block_enricher 임계값 외부화 (TF-11-05 선행 패치 후 잔존 라인 확인)
  TF-11-12 state_service print→logging 교체
  TF-11-13 models.py dead code 여부 확인 후 삭제 또는 LLM 경유 전환
  TF-16-01 architect_rules.json deprecated/ 이동 또는 삭제
  TF-16-02 writer.json deprecated/ 이동 또는 삭제
  TF-16-03 settings.json 모델명 models.yaml SSOT 경유
  TF-16-04 director_auditor.py:278 silent except logging 보강
  TF-16-05 test_integrity.py 대원칙 2·4 테스트 추가
  TF-16-06 test_phase5_hygiene.py _threshold() 키 매핑 테스트 추가
```

### 연관성 주의 항목

| 항목 | 연관 항목 | 처리 순서 |
|------|----------|----------|
| TF-11-05 (decision 덮어쓰기 제거) | TF-11-11 (같은 라인 임계값) | TF-11-05 → 라인 삭제 확인 → TF-11-11 moot 여부 판단 |
| TF-11-13 (models.py dead code) | TF-16-05 (대원칙 2 테스트) | TF-11-13 dead code 확인 → TF-16-05 케이스 연동 처리 |
| TF-16-03 (settings.json SSOT) | TF-11-01 (block_enricher SSOT) | 동일 models.yaml SSOT 목표, 각자 독립 패치 |
