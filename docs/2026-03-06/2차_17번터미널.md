# 2차 전수조사 — 17번 터미널
## 누락 Director 에이전트 2종

> 발령: 4차 마스터 오더 (2026-03-06)
> 담당 파일:
> - `modules/domain/agents/director_ensemble.py`
> - `modules/domain/agents/director_continuity.py`

---

## 1단계: TF 구성

### director_ensemble.py

```
TF-17-01 | OK  | director_ensemble.py:1062-1070 | DE-1 확인
  contradiction_types 반환 확인:
    return dict 내 "contradiction_types" 키 존재 (L1062)
    `_contradiction_check.get("found_contradictions", [])` 순회 → type 추출
    → [A-4] 설계대로 ✅

TF-17-02 | OK  | director_ensemble.py:838-848 | DE-2 확인
  score_breakdown 합산 ≠ score 시 breakdown 우선 교정:
    L840-848: `_sb_sum = sum(v for v in _sb_raw.values() ...)`
    `if _sb_sum != score and _sb_sum > 0: score = max(0, min(100, _sb_sum))`
    logging.warning 포함 ✅

TF-17-03 | OK  | director_ensemble.py:624-625, 694-698, 854-858 | DE-3 확인
  SCM 단일 후보 독점 방지:
    L624-625: `_scm_single_candidate = len(qualified_indices) == 1`
    L694-698: 경고 prefix 주입 ("절대 기준으로 독립 평가하세요")
    L854-858: `if _scm_single_candidate and score >= 95: score = min(score, 90)`
    → 경고 주입 + 95→90 보정 양쪽 구현 ✅

TF-17-04 | OK  | director_ensemble.py:931-969 | DE-4 확인
  NC-3 consistency_checklist ISSUE 3건+ python_warnings 3점 상한:
    L932: `_checklist = result.get("consistency_checklist") or {}`
    L948: `_issue_count = sum(1 for k in _nc3_keys if ... == "ISSUE")`
    L955-969: `if _issue_count >= 3: _pw = _sb.get("python_warnings", 10); if _pw > 3: ...` → 3점 클램프
    → NC-3B CLAUDE.md 설계대로 ✅

TF-17-05 | P1  | director_ensemble.py:944-945 / response_schemas.py:145-162 | DE-5 스키마-코드 불일치
  _nc3_keys 목록에 fiction_term_leak 포함되나 DIRECTOR_AUDIT_SCHEMA에 없음:
    director_ensemble.py L933-946: _nc3_keys = [numeric_accuracy, arithmetic,
      title_consistency, scene_overlap, percent_calculation, event_ordering,
      space_continuity, npc_identity, time_progression, opening_diversity,
      timeline_arc_consistency, "fiction_term_leak"]  # [TF-57-A] → 12개
    response_schemas.py L145-162: DIRECTOR_AUDIT_SCHEMA.consistency_checklist
      properties = 11개 (fiction_term_leak 없음)
  영향: Gemini response_schema가 fiction_term_leak 필드를 LLM이 반환하지 못하도록 강제
    → _checklist.get("fiction_term_leak", "") == "" → ISSUE 카운트 0
    → [TF-57-A] 4th wall 메타용어 NC-3 체크 비기능 (dead check)
    타임라인: timeline_arc_consistency (11번째)는 schema에 있음 ✅
    fiction_term_leak (12번째)만 누락 ❌
  패치 방향:
    DIRECTOR_AUDIT_SCHEMA.consistency_checklist.properties에
    "fiction_term_leak": types.Schema(type=types.Type.STRING, enum=["OK", "ISSUE"])
    추가 (required는 아님, optional 유지)

TF-17-06 | OK  | director_ensemble.py (director.yaml 4곳) | DE-6 확인
  NC-3B few-shot golden example:
    코드 레이어에서는 PromptLoader.load("director", ...)로 위임
    CLAUDE.md "NC-3B — director.yaml 4곳에 [NC-3B] few-shot golden example 삽입" 완료 이력
    Python 코드에서 별도 개입 없음 → YAML에 위임됨 ✅ (코드 레이어 OK)

TF-17-07 | OK  | director_ensemble.py:1048, 1058-1059 | DE-7 확인
  PASS_WITH_FIX 반환 시 fix_scope/feedback 앙상블 결과에 전파:
    L1048: "feedback": feedback (LLM result feedback 직접 반환)
    L1058: "fix_scope": result.get("fix_scope", "")
    L1059: "fix_scope_reasoning": result.get("fix_scope_reasoning", "")
    → PASS_WITH_FIX 경로에서도 동일 return dict 사용, 전파 ✅

TF-17-08 | OK  | director_ensemble.py:781-812 | DE-8 확인
  앙상블 timeout/ask() 실패 처리:
    L781: try:
      L782-793: cache 경로 / fallback ask() 경로
    L794-796: except Exception as _ask_err:
      logging.warning(...); response = ""
    L797: result = self._d._extract_json_robust(response)  # "" → parsing_error
    L799-812: `if not result or result.get("parsing_error"):` → fail-closed REJECT (score=0)
    YAML 외부화는 base_agent.py ask() 레이어에서 처리 (TF-26)
    → 앙상블 자체도 timeout → REJECT fail-closed ✅

TF-17-09 | OK  | director_ensemble.py:imports | DE-9 확인
  모델 SSOT — AIModels.* 사용 여부:
    director_ensemble.py imports: constants, prompt_loader, tactical_utils, threshold_helper
    AIModels import 없음 → 모든 LLM 호출은 self._d.ask() / self._d._ask_with_cached_context()
    Director 본체(director.py)가 AIModels.*로 모델 설정 후 ensemble에 self를 주입
    → 앙상블은 Director 레퍼런스를 통해 간접 사용 (SSOT 위반 아님) ✅
```

### director_continuity.py

```
TF-17-10 | OK  | director_continuity.py:653-658, 767-804 | DE-10 확인
  Context Caching 실제 사용:
    check_blueprint_continuity_with_cache() L653:
      cache_result = self._d._get_or_create_context_cache(cache_type="blueprint", ...)
    check_manuscript_continuity_with_cache() L767:
      cache_result = self._d._get_or_create_context_cache(cache_type="manuscript", ...)
    L802: self._d._ask_with_cached_context(cache_result["cache_name"], ...)
    두 경로 모두 실제 사용 ✅

TF-17-11 | OK  | director_continuity.py:801-804 | DE-11 확인
  캐시 만료/실패 시 fallback:
    L800-804:
      if cache_result.get("cache_name"):
          response = self._d._ask_with_cached_context(...)
      else:
          response = self._d.ask(...)  ← fallback
    _get_or_create_context_cache 실패 시 cache_name=None → fallback ask() ✅

TF-17-12 | OK  | director_continuity.py:695-725 | DE-12 확인
  연속성 검사 결과 advisory vs Python auto-REJECT:
    check_blueprint_continuity_with_cache() 반환:
      CRITICAL → "REJECT", MAJOR → "WARNING", else → "PASS"
    이 결과를 Director 또는 stage4 조율 레이어가 수신
    director_continuity.py 자체는 결과를 반환만 함, 원고 거부를 직접 실행하지 않음
    호출 체인에서 Director가 최종 판단 → 대원칙 3 준수 ✅
    (단, REJECT 반환 시 calling code가 Director를 우회 가능 — 이는 호출자 책임)

TF-17-13 | OK  | director_continuity.py:464-476, 587-600 | DE-13 확인
  NPC 사망 연속성 체크:
    check_manuscript_history_conflicts() / check_manuscript_history_with_cache():
      충돌 type "death" 감지 (LLM에게 history_conflict 프롬프트 전달)
      CRITICAL/MAJOR "death" conflict → "CONFLICT" decision
    deceased 필드 Python 강제 체크는 truth_gate.py(_check_deceased_npc_appearance)에서 담당
    director_continuity는 LLM advisory 경로 ✅ (대원칙 4 준수, truth_gate가 Python 방어선)

TF-17-14 | P2  | director_continuity.py:119-124 | DE-14 부분 확인
  LLM 파싱 실패 시 fail-closed vs fail-open:
    validate_entity_consistency(): L122-124:
      if not isinstance(result, dict) or result.get("parsing_error"):
          return {"decision": "PASS", "mismatches": [], "fix_instructions": "", "parsing_error": True}
    ← fail-open (PASS 반환) — entity 일관성 advisory 성격상 의도적 설계
    check_manuscript_history_conflicts(): L463-470: 파싱 실패 → REJECT fail-closed ✅
    check_manuscript_continuity_with_cache(): L807-813: 파싱 실패 → REJECT fail-closed ✅
    두 패턴 혼재 — validate_entity_consistency만 fail-open
    패치 방향: P2 (advisory 기능이므로 fail-open이 허용되나 주석 보강 권장)
```

---

## 2단계: 감리 1회 — CLAUDE.md 의도적 설계 오탐 제거

**TF-17-05 (fiction_term_leak schema 누락)**: CLAUDE.md에서 NC-3B 관련 내용에 `fiction_term_leak`은
명시되지 않음. CLAUDE.md "TF-57-A"는 언급 없음 (코드 내 주석만 존재). 의도적 설계가 아님.
→ **P1 유지**

**TF-17-14 (entity consistency fail-open)**: CLAUDE.md entity_consistency는 advisory 체크.
validate_entity_consistency의 fail-open은 `{"decision": "UNKNOWN", ...}`을 반환하는
별도 except(L173-175)도 있어, 완전한 예외는 UNKNOWN으로 처리됨. P2 적절.
→ **P2 유지**

**TF-17-12 (blueprint continuity REJECT)**: CLAUDE.md "Blueprint 연속성 검증 (Python + 캐싱)" 명시.
Python이 REJECT를 반환하더라도 calling code에서 Director 판단을 거치므로 대원칙 3 위반 아님.
→ **OK 유지**

---

## 3단계: 감리 2회 — 대원칙 1~4 준수 여부

**대원칙 1 (Python 수집만, 판단은 LLM)**: director_ensemble.py의 SCM 점수 보정(95→90)은
대원칙 1 경계에 있음. CLAUDE.md에서 명시적으로 허용된 Python 보정이며, QualityGate bypass
(TF-46) 설계 의도에 포함. 오탐 아님. ✅

**대원칙 3 (Director 주권주의)**: director_continuity.py의 check_blueprint_continuity_with_cache()
CRITICAL → REJECT 반환은 Python이 연속성 REJECT 판단을 내리는 것으로 보일 수 있으나,
이 결과를 Director.py가 수신하여 최종 판단. Python이 직접 원고 거부를 실행하지 않음. ✅

---

## 4단계: 감리 3회 — 기존 테스트 커버 여부

**TF-17-05**: NC-3 ISSUE 카운트 테스트 존재 (`test_stage2_preflight.py` 등에 NC-3 관련).
하지만 `fiction_term_leak` 키 특정 테스트는 없음 — 비기능 상태로 방치. 미커버 확인.

**TF-17-14**: entity consistency 파싱 실패 케이스 테스트 없음. P2 유지.

---

## 최종 결과

| 등급 | 건수 | 항목 |
|------|------|------|
| P0   | 0    | — |
| P1   | 1    | TF-17-05 (fiction_term_leak 스키마-코드 불일치 → NC-3 4th wall 체크 비기능) |
| P2   | 1    | TF-17-14 (validate_entity_consistency fail-open — advisory 성격 허용이나 주석 부재) |
| OK   | 12   | TF-17-01~04, 06~13 |

### TF-17-05 패치 요약

**파일**: `modules/core/response_schemas.py`
**위치**: `DIRECTOR_AUDIT_SCHEMA.consistency_checklist.properties`
**내용**: `"fiction_term_leak": types.Schema(type=types.Type.STRING, enum=["OK", "ISSUE"])` 추가
**우선순위**: P1 (기능 복원)
