# 전수조사 3차 마스터 오더 — 0번 터미널 발령

> 발령일: 2026-03-06
> 발령자: 0번 터미널 (총괄 감리)
> 목적: 1차 오더(터미널 1~10) 미커버 영역 전량 검수
> 기준 커밋: `9a55ac0`

---

## 1차 오더 대비 미커버 영역 (발견된 공백)

| 분류 | 미커버 파일 수 | 배정 터미널 |
|------|--------------|------------|
| Stage 0 전체 (stage0/) | 6개 | 11번 |
| Services 계층 (services/) | 4개 | 11번 |
| 잔여 도메인 에이전트 | 3개 | 11번 |
| Core 보조 에이전트 (P2 유보 7개 포함) | 10개 | 12번 |
| Core 추적/관계/캐릭터 계열 | 11개 | 13번 |
| Core 품질/스타일/장르/HUD 계열 | 18개 | 14번 |
| Core 운영/모니터링/유틸 + lite_mode | 20개+ | 15번 |
| Config 잔여 + Tests 핵심 감사 | 30개+ | 16번 |

**총 미커버: 70개+ 파일**

---

## 공통 작업 수칙 (1차 오더와 동일)

```
1단계. TF 구성하며 조사
   → 형식: TF-[터미널번호]-[순번] | 등급 | 파일:줄번호 | BUG/GAP | 설명 | 패치 방향

2단계. 감리 1회 — CLAUDE.md 명시 의도적 설계 오탐 제거
3단계. 감리 2회 — 대원칙 1~4 준수 여부 재확인
4단계. 감리 3회 — 기존 테스트 커버 여부 확인, 최종 등급 확정
5단계. 문서화 후 0번 터미널에 보고
```

> **코드 수정은 0번 터미널(Claude)만 수행.**

**저장 경로**: `docs/2026-03-06/2차_N번터미널.md` (N = 11~16)

---

## 11번 터미널 — Stage 0 + Services + 잔여 도메인

**담당 파일:**
```
modules/core/stage0/__init__.py          # StageZeroManager 통합
modules/core/stage0/story_expander.py    # 컨셉 → Bible + Treatment
modules/core/stage0/reverse_expander.py  # 역설계 (원고 → Bible)
modules/core/stage0/style_extractor.py   # 톤/문체 추출
modules/core/stage0/preset_registry.py   # 프리셋 스키마 체계
modules/core/stage0/spinner.py           # UI 스피너
modules/core/services/audit_service.py   # 감사 서비스
modules/core/services/state_service.py   # 상태 서비스
modules/core/services/ui_service.py      # UI 서비스
modules/core/services/project_service.py # 프로젝트 서비스
modules/domain/agents/block_enricher.py  # 블록 enricher
modules/domain/agents/negative_example_injector.py  # 부정 예시 주입
modules/domain/models.py                 # 도메인 모델
```

**전용 체크 항목:**
```
[S0-1]  stage0/__init__.py — Stage 0 → Stage 2 연동: PresetRegistry 참조가 StateTracker에 올바르게 전달되는지
[S0-2]  story_expander — LLM 파싱 실패 시 Bible 생성 폴백 경로 (치명적 — Stage 0 실패 시 전체 파이프라인 불가)
[S0-3]  reverse_expander — 역설계 원고 → 벡터 DB 저장 경로: VecMemory 호출 실패 시 방어
[S0-4]  reverse_expander — Arc/Blueprint 역추출 스킵 확인 (원고 직접 참조 대체 설계 준수 여부)
[S0-5]  style_extractor — 추출된 스타일 가이드 DB 저장 경로 및 실패 시 처리
[S0-6]  preset_registry — 장르 프리셋 조합 실패 시(미지원 장르) 폴백
[S0-7]  audit_service — DB 쓰기 실패 시 트랜잭션 롤백 보장
[S0-8]  state_service — 상태 읽기/쓰기 동시성 방어 (락 또는 트랜잭션)
[S0-9]  project_service — 프로젝트 생성/삭제 원자성 보장
[S0-10] block_enricher — enrich 결과 JSON 파싱 실패 시 원본 블록 보존
[S0-11] negative_example_injector — 부정 예시 주입이 LLM 판단 없이 Python이 직접 필터링하는지 (대원칙 1 위반 가능성)
[S0-12] domain/models.py — 모델 클래스 필드 타입 검증 누락 여부
```

**저장**: `docs/2026-03-06/2차_11번터미널.md`

---

## 12번 터미널 — Core 보조 에이전트 (P2 유보 7개 포함)

**담당 파일:**
```
modules/core/self_reflection.py           # 자기반성
modules/core/adversarial_self_play.py     # 적대적 자기 플레이  ← P2 유보
modules/core/chain_of_verification.py     # 검증 체인          ← P2 유보
modules/core/cross_agent_verifier.py      # 교차 에이전트 검증  ← P2 유보
modules/core/multi_agent_deliberation.py  # 다중 에이전트 심의  ← P2 유보
modules/core/tree_of_thoughts.py          # 생각의 나무        ← P2 유보
modules/core/confidence_calibration.py    # 신뢰도 보정        ← P2 유보
modules/core/narrative_structure_analyzer.py  # 서사 구조 분석
modules/core/agent_intelligence.py        # 에이전트 지능
modules/core/expert_mixture.py            # 전문가 혼합
```

**전용 체크 항목:**
```
[BA-1]  adversarial_self_play — model 인자 호출자 명시 주입 여부 (P2 유보 확인: main_a.py에서 model 주입)
[BA-2]  chain_of_verification — model 인자 호출자 명시 주입 여부 (동일)
[BA-3]  cross_agent_verifier — model 인자 호출자 명시 주입 여부 (동일)
[BA-4]  multi_agent_deliberation — model 인자 호출자 명시 주입 여부 (동일)
[BA-5]  tree_of_thoughts — model 인자 명시 주입 여부 + default 인자 AIModels.* 교체 완료 여부
[BA-6]  confidence_calibration — model 인자 인자화 여부 (6차 전수조사 P1 대상 확인)
[BA-7]  self_reflection — model 인자 default 인자 AIModels.* 교체 여부 (Model SSOT)
[BA-8]  narrative_structure_analyzer — model default 인자 AIModels.* 교체 여부 (Model SSOT)
[BA-9]  cross_agent_verifier — LLM 응답 파싱 실패 시 fail-closed 여부
[BA-10] multi_agent_deliberation — 다중 에이전트 결과 취합 시 동수/무응답 처리
[BA-11] agent_intelligence — Python이 직접 에이전트 판단을 대신하는 코드 여부 (대원칙 1)
[BA-12] expert_mixture — 전문가 선택 로직이 LLM이 아닌 Python 규칙 기반인지 확인 (대원칙 1)
```

**저장**: `docs/2026-03-06/2차_12번터미널.md`

---

## 13번 터미널 — Core 추적/관계/캐릭터 계열

**담당 파일:**
```
modules/core/relationship_tracker.py          # 관계 추적
modules/core/relationship_tracker_factions.py # 파벌 관계 추적
modules/core/relationship_tracker_npc.py      # NPC 관계 추적
modules/core/karma_service.py                 # 카르마 서비스
modules/core/emotion_tracker.py               # 감정 추적
modules/core/foreshadow_tracker.py            # 복선 추적
modules/core/information_diffusion.py         # 정보 확산
modules/core/lore_manager.py                  # 설정 관리
modules/core/character_voice.py               # 캐릭터 보이스
modules/core/character_voice_profiler.py      # 보이스 프로파일러
modules/core/semantic_item_registry.py        # 아이템 레지스트리
```

**전용 체크 항목:**
```
[TR-1]  relationship_tracker — 관계 업데이트 시 DB append-only 보장 여부 (LM-D 패턴과 일치 여부)
[TR-2]  relationship_tracker_npc — npc_relationship_history sorted key 적용 여부
[TR-3]  relationship_tracker_factions — 파벌 관계 변경이 LLM 판단 없이 Python이 직접 변경하는지 (대원칙 2)
[TR-4]  karma_service — 카르마 수치 변경 권한: Python 자동 변경 vs LLM state_updates 경유 (대원칙 2)
[TR-5]  emotion_tracker — config/prompts/emotion_tracker.yaml 실제 로드 여부 + 파싱 실패 시 방어
[TR-6]  foreshadow_tracker — 복선 등록/해소가 DB에 정상 기록되는지 + 트랜잭션 보장
[TR-7]  information_diffusion — 정보 확산 시뮬레이션이 순수 Python인지 LLM 판단 포함인지 (대원칙 1 확인)
[TR-8]  lore_manager — 설정 충돌 감지를 Python이 직접 판단하는지 (대원칙 1·2)
[TR-9]  character_voice — 보이스 DB 저장 경로 (character_voice/foreshadow DB 전환 완료 여부 — DB 효율화)
[TR-10] character_voice_profiler — 프로파일 생성 시 LLM 실패 시 기존 프로파일 보존 vs 덮어쓰기
[TR-11] semantic_item_registry — 아이템 등록 중복 처리 + 사망 NPC 아이템 귀속 처리 (대원칙 4)
```

**저장**: `docs/2026-03-06/2차_13번터미널.md`

---

## 14번 터미널 — Core 품질/스타일/장르/HUD 계열

**담당 파일:**
```
modules/core/context_compression.py        # 컨텍스트 압축
modules/core/dynamic_prompt_weighting.py   # 동적 프롬프트 가중치
modules/core/diversity_sampler.py          # 다양성 샘플러
modules/core/quality_amplifier.py          # 품질 증폭기
modules/core/quality_constitution.py       # 품질 헌법
modules/core/technique_weaver.py           # 기법 직조
modules/core/narrative_diversity.py        # 서사 다양성
modules/core/constitutional_checker.py     # 헌법 검사기
modules/core/primitive_guard.py            # 원시인 모드 방어
modules/core/repetition_guard.py           # 반복 방어
modules/core/writer_template.py            # Writer 템플릿
modules/core/genre_hud_manager.py          # 장르 HUD 관리
modules/core/hud_utils.py                  # HUD 유틸
modules/core/martial_manager.py            # 무공 관리
modules/core/jianghu_logic.py              # 강호 로직
modules/core/power_scaling.py              # 파워 스케일링
modules/core/pacing_analyzer.py            # 페이싱 분석
modules/core/material_db.py                # 소재 DB
modules/core/justification_patterns.py     # 정당화 패턴
```

**전용 체크 항목:**
```
[QS-1]  context_compression — 압축 후 중요 정보(사망 NPC/팩트) 손실 여부 (대원칙 2 간접 위반 가능성)
[QS-2]  dynamic_prompt_weighting — 가중치 조정이 Python 규칙 기반인지 LLM 판단 기반인지 (대원칙 1)
[QS-3]  diversity_sampler — 샘플링 로직이 Director를 우회하는지 (대원칙 3)
[QS-4]  quality_amplifier — 품질 판단을 Python이 직접 수행하는지 (대원칙 1·3)
[QS-5]  quality_constitution — 헌법 규칙 위반 시 자동 REJECT가 Python에서 발생하는지 (대원칙 3)
[QS-6]  constitutional_checker — 체크 결과가 advisory로 전달되는지 vs Python이 직접 차단하는지
[QS-7]  primitive_guard — "원시인 모드" 현대용어 감지 후 Python 자동 REJECT vs advisory (대원칙 3)
[QS-8]  repetition_guard — 반복 감지 후 처리가 advisory인지 자동 차단인지 (대원칙 3)
[QS-9]  genre_hud_manager — HUD Anomaly 파이프라인: hud_snapshot 컬럼 활성화 여부 (TF-E)
[QS-10] hud_utils — HUD 수치 Python 자동 변경 여부 (대원칙 2 — LLM state_updates 경유 필수)
[QS-11] martial_manager — 무공 습득/상실 기록이 LLM 판단 없이 Python이 직접 변경하는지 (대원칙 2)
[QS-12] power_scaling — 파워 레벨 자동 조정이 Python 단독 판단인지 (대원칙 1·2)
[QS-13] pacing_analyzer — 페이싱 분석 결과가 advisory인지 자동 차단인지 (대원칙 3)
[QS-14] jianghu_logic — 강호 로직이 무협 전용인지 + 비무협 장르 오염 여부 (TF-45 맥락)
[QS-15] material_db — 소재 DB 조회 실패 시 silent 폴백 여부
```

**저장**: `docs/2026-03-06/2차_14번터미널.md`

---

## 15번 터미널 — Core 운영/모니터링/유틸 + lite_mode 핵심

**담당 파일:**
```
modules/core/pass_rate_monitor.py     # 합격률 모니터
modules/core/adaptive_retry.py        # 적응형 재시도
modules/core/feedback_system.py       # 피드백 시스템
modules/core/reflexion_manager.py     # 반성 관리
modules/core/failure_learning.py      # 실패 학습
modules/core/data_collector.py        # 데이터 수집
modules/core/stage01_helpers.py       # Stage 0/1 헬퍼
modules/core/system.py                # 시스템 설정
modules/core/prompt_builder.py        # 프롬프트 빌더
modules/core/reference_anchor.py      # 참조 앵커 (6차 SSOT 패치 대상)
modules/core/constraint_db.py         # 제약 DB
modules/core/state_text_verifier.py   # 상태 텍스트 검증
modules/core/arc_summary_utils.py     # Arc 요약 유틸
modules/core/studio_visualizer.py     # 스튜디오 시각화
modules/core/logger.py                # 로거
modules/core/session_logger.py        # 세션 로거
modules/core/error_helper.py          # 에러 헬퍼
modules/core/escape_utils.py          # 이스케이프 유틸
modules/core/perf_timer.py            # 성능 타이머
modules/core/slack_bot.py             # 슬랙 봇
lite_mode/main_lite.py                # Lite 모드 진입점
lite_mode/run_stage.py                # 스테이지 실행
lite_mode/free_writer.py              # 자유 작성
lite_mode/run_fix_arcs.py             # Arc 수정 실행
lite_mode/run_revalidate.py           # 재검증 실행
```

**전용 체크 항목:**
```
[OP-1]  pass_rate_monitor — 합격률 계산이 Python 단독 판단인지 (대원칙 1·3 확인: 통계 수집 vs 합격 판정)
[OP-2]  adaptive_retry — 재시도 횟수 상한이 validation.yaml _threshold() 참조인지 vs 매직넘버
[OP-3]  feedback_system — 피드백 저장/로드 중 DB 트랜잭션 보장
[OP-4]  reflexion_manager — 반성 결과가 LLM 판단으로 전달되는지 vs Python이 직접 수정 결정 (대원칙 1)
[OP-5]  reference_anchor — AIModels.* 교체 완료 여부 (6차 전수조사 P1 대상 확인)
[OP-6]  constraint_db — 제약 DB 쓰기 충돌 방어 + 사망 NPC 제약 보존 여부 (대원칙 4)
[OP-7]  state_text_verifier — 텍스트 기반 상태 검증이 Python이 직접 판단하는지 (대원칙 1)
[OP-8]  prompt_builder — 빌드 결과 프롬프트의 {플레이스홀더} 미치환 탐지 로직
[OP-9]  logger.py — llm_calls 테이블 로깅 배선 확인 (Log-Phase2 패치 이후)
[OP-10] session_logger — 세션 로그 파일 핸들 미해제 여부 (리소스 누수)
[OP-11] lite_mode/main_lite.py — Stage 0→2→4 파이프라인 연동 시 main_a.py 대비 누락 모듈
[OP-12] lite_mode/run_fix_arcs.py — Arc 수정 시 Director 우회 여부 (대원칙 3)
[OP-13] lite_mode/run_revalidate.py — 재검증 시 동일 검증 체인 재사용 여부 vs 단순 Python 판단
[OP-14] failure_learning.py — 실패 패턴 학습 결과가 Python 자동 적용인지 advisory인지 (대원칙 1)
[OP-15] slack_bot.py — 토큰/웹훅 하드코딩 여부 (보안)
```

**저장**: `docs/2026-03-06/2차_15번터미널.md`

---

## 16번 터미널 — Config 잔여 + Tests 핵심 감사

**담당 파일:**
```
[Config 잔여]
config/prompts/analyst_libraries.json           # 기본 라이브러리
config/prompts/analyst_libraries_investment.json # 투자 라이브러리
config/prompts/analyst_libraries_wuxia (없는지 확인)
config/prompts/architect_rules.json
config/prompts/weaver_rules.json
config/prompts/writer.json
config/prompts/writer_rules.json
config/prompts/emotion_tracker.yaml
config/genres/*.yaml                            # 11개 장르 YAML
config/smart_retrieval/genre_hints.yaml
config/system.yaml
config/settings.json
config/tone_presets.json

[Tests 핵심 감사 — 커버리지 갭 탐지]
tests/conftest.py
tests/test_integrity.py
tests/test_protocols.py
tests/test_protocol_conformance.py
tests/test_phase5_hygiene.py
tests/test_pass_with_fix.py         # PASS_WITH_FIX 테스트
tests/test_pipeline_wiring.py       # 파이프라인 배선 테스트
tests/test_ns4_time_continuity.py   (존재 여부 확인)
tests/test_numeric_consistency.py   (존재 여부 확인)
tests/e2e/test_l3_golden_route.py
tests/e2e/test_l3_stage4_smoke.py
```

**전용 체크 항목:**
```
[CF-1]  analyst_libraries_investment.json — 레버리지/수익률 관련 라이브러리 항목 최신화 여부
[CF-2]  architect_rules.json — Director/Blueprint 프롬프트에서 실제 참조되는지 vs 사문화
[CF-3]  weaver_rules.json — Weaver 에이전트에서 실제 로드되는지 확인
[CF-4]  writer.json / writer_rules.json — writer_prompt_builders.py 또는 writer.py에서 로드 여부
[CF-5]  config/genres/*.yaml — 각 장르 YAML 필수 키 (name/guard_class/hud_fields) 전수 확인
[CF-6]  config/system.yaml — 시스템 설정 키가 modules/core/system.py와 일치하는지
[CF-7]  config/settings.json — validation.yaml 대비 중복/충돌 설정 여부 (SSOT 위반 가능성)
[CF-8]  genre_hints.yaml — Smart Context Retrieval 슬롯에서 실제 참조되는지 (TF-55b 이후)

[Tests]
[CF-9]  conftest.py — 공통 fixture 중 실전 DB 연결하는 fixture 격리 여부 (테스트 오염 위험)
[CF-10] test_integrity.py — 대원칙 1~4 위반 탐지 테스트 항목 전수 (누락된 대원칙 있는지)
[CF-11] test_protocol_conformance.py — B-3 표준화 이후 Protocol 미준수 에이전트 감지 여부
[CF-12] test_pass_with_fix.py — PASS_WITH_FIX 3-tier 라우팅 전 케이스 커버 여부
[CF-13] test_pipeline_wiring.py — TF-54/55b/합격률 8개 테스트 실제 배선 검증 충분성
[CF-14] test_phase5_hygiene.py — 임계값 _threshold() 전수 검사 vs validation.yaml 키 일치
[CF-15] test_ns4_time_continuity / test_numeric_consistency — 파일 존재 여부 + NS-4/NC 핵심 케이스 커버 확인
```

**저장**: `docs/2026-03-06/2차_16번터미널.md`

---

## 0번 터미널 — 3차 취합 프로세스

11~16번 터미널 완료 후:
1. 1차 오더(1~10번) + 3차 오더(11~16번) 전체 TF 통합
2. 파일 중복 참조 교차 검증
3. 최종 오탐 제거 3회 후 통합 패치 오더 발행

**최종 보고서**: `docs/2026-03-06/2차_0번터미널_최종취합.md`

---

## 커버리지 완결 확인표

| 영역 | 1차 오더 | 3차 오더 | 비고 |
|------|----------|----------|------|
| Stage 0 | ❌ | ✅ 11번 | |
| Stage 2 파이프라인 | ✅ 1번 | — | |
| Arc 생성/검증 | ✅ 2번 | — | |
| Blueprint/Continuity | ✅ 3번 | — | |
| Stage 4 오케스트레이터 | ✅ 4번 | — | |
| Chief Writer/Writer | ✅ 5번 | — | |
| Director 체인 | ✅ 6번 + 2번터미널 | — | |
| Advisory 체인 | ✅ 7번 | — | |
| Memory/DB/상태 | ✅ 8번 | — | |
| Validation/Guard | ✅ 9번 | — | |
| Config SSOT + Base | ✅ 10번 | — | |
| Services 계층 | ❌ | ✅ 11번 | |
| 보조 에이전트 (P2 유보) | ❌ | ✅ 12번 | |
| 추적/관계/캐릭터 | ❌ | ✅ 13번 | |
| 품질/스타일/장르/HUD | ❌ | ✅ 14번 | |
| 운영/모니터링/lite_mode | ❌ | ✅ 15번 | |
| Config 잔여 + Tests | ❌ | ✅ 16번 | |
