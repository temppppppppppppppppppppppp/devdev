# 2차 전수조사 — 2번 터미널 (Arc 생성/검증 에이전트)

> 기준 커밋: `9a55ac0`
> 담당 영역: Arc 생성/검증 에이전트 9개 파일
> 조사 원칙: 코드 수정 없음, 줄번호·상태 기록만

---

## 담당 파일

```
modules/domain/agents/four_phase_arc_generator.py
modules/domain/agents/arc_ensemble.py
modules/domain/agents/arc_corrector.py
modules/domain/agents/arc_critic.py
modules/domain/agents/arc_draft_validator.py
modules/domain/agents/unified_arc_validator.py
modules/domain/agents/state_locked_arc_generator.py
modules/core/stage3_orchestrator.py
modules/core/stage3_context.py
```

---

## 전용 체크 항목 조사 결과

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|------------|------|------|------|
| ARC-1 | four_phase_arc_generator.py:1015 | - | OK | _load_execution_state() — DB 없거나 예외 시 빈 dict 반환 폴백 완비, logging.debug 기록 |
| ARC-2 | four_phase_arc_generator.py:1127 | - | OK | _generate_prev_context() — 실행 결과 주입 시 "Arc 계획보다 우선" 지시 텍스트 포함 확인 |
| ARC-3 | four_phase_arc_generator.py:1249 | - | OK | NS-4 시간 마커 주입이 `return "\n".join(lines)` 직전에 위치 확인 |
| ARC-4 | four_phase_arc_generator.py:551 | - | OK | NS-3-B 경고 → `feedback = f"{_ns3b_header}\n\n{feedback}"` prepend 실행 확인 |
| ARC-5 | arc_ensemble.py:114 | - | OK | model_tier="gemini-2.5-pro" 기본값 — `[SSOT-P2]` 주석 명시, CLAUDE.md P2 유보 7개 목록 포함 |
| ARC-6 | arc_corrector.py:90 | - | OK | model_tier=None 기본값, create_arc_corrector `[SSOT-P2]` 주석 — P2 유보 확인 |
| ARC-7 | arc_critic.py:132 | - | OK | model_tier="gemini-2.5-flash" `[SSOT-P2]` 주석 — P2 유보 확인 |
| ARC-8 | unified_arc_validator.py:278 | - | OK | state_changes 누락→WARNING, timeline 누락→WARNING, 내부 필드→MINOR — CRITICAL/MAJOR 아님, REJECT 경로 없음 |
| ARC-9 | stage3_orchestrator.py:630 | - | OK | regex 파싱 실패 시 `except Exception as _ns4_err: _logging.debug(...)` 비차단 처리 (의도적 설계) |
| ARC-10 | stage3_orchestrator.py:605 | - | OK | NS-4 advisory 주입 위치가 TF9 Treatment Block except(L602) 직후 확인 |
| ARC-11 | stage3_context.py:16 | P2 | GAP | CLAUDE.md "19슬롯" 기재 vs 실제 __slots__ 21개 (adversarial_self_play + session_logger 추가됨) — 문서 불일치 |
| ARC-12 | state_locked_arc_generator.py:307 | - | OK | _lock_start_state() — None, 파싱 실패(50% 폴백), _generate_episode() None→상위 종료, _extract_state() 시작상태 유지 — 각 단계 폴백 완비 |

---

## 공통 체크 항목 조사 결과

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|------------|------|------|------|
| 공통-A | four_phase_arc_generator.py:194 | P1 | BUG | `except Exception: pass` — 장르 감지 실패 시 로깅 없이 silent 처리, wuxia 기본값으로 비무협 작품에 오염 위험 |
| 공통-A | arc_ensemble.py:175 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-A | state_locked_arc_generator.py:476 | - | OK | `except Exception as e: logging.warning(...)` 로깅 있음 |
| 공통-B | unified_arc_validator.py:604 | - | OK | LLM 파싱 실패 → fail-closed REJECT (CRITICAL 이슈 생성) |
| 공통-B | arc_critic.py:176 | - | OK | LLM 예외 → `_python_critique_fallback()` Python 폴백 사용 |
| 공통-C | unified_arc_validator.py:169 | - | OK | Python CRITICAL 있어도 LLM에 전달 후 결합 판정 (대원칙 1 준수), 사망 NPC REJECT는 대원칙 4 적용 |
| 공통-D | arc_ensemble.py:114 | - | OK | P2 유보 확인 (CLAUDE.md 명시) |
| 공통-D | state_locked_arc_generator.py:173 | - | OK | `AIModels.STAGE2_EXTRACTION_MODEL`, `AIModels.STAGE2_MAIN_MODEL` — SSOT 사용 |
| 공통-E | four_phase_arc_generator.py:61 | P1 | GAP | `threshold: float = 0.30` 하드코딩 — `_threshold()` 미사용, validation.yaml 외부화 미완 |
| 공통-F | four_phase_arc_generator.py:1023 | - | OK | DB 커넥션은 db_manager 위임, 직접 커넥션 생성 없음 |
| 공통-G | arc_corrector.py:83 | P2 | GAP | FourPhaseArcGenerator에서 import/사용 없음 — dead code 가능성 (다른 호출처 미확인) |
| 공통-G | arc_draft_validator.py:28 | P2 | GAP | UnifiedArcValidator로 대체 완료, 현재 파이프라인 사용처 미확인 — dead code 가능성 |
| 공통-H | unified_arc_validator.py:199 | P1 | GAP | `if not state_tracker or not prev_arcs: return issues` — state_tracker=None 시 사망 NPC 체크 전면 skip, 로깅 없음 |

---

## 감리 3회 기록

### 감리 1회 — CLAUDE.md 명시 의도적 설계 제거
- ARC-5/6/7: P2 유보 7개 목록에 arc_ensemble/arc_corrector/arc_critic 명시 → **오탐 제거**
- ARC-9/10: 비차단 의도적 설계 패턴 → **오탐 제거**
- 공통-A(four_phase_arc_generator.py:194): 9차 전수조사 "silent except 5건" 패치 목록에 미포함 — **BUG 유지**

### 감리 2회 — 기존 테스트 커버리지 확인
- arc_corrector.py dead code: stage2_orchestrator 등 다른 파일에서 import 가능성 존재 → P2 GAP 유지 (확정 전 사용처 확인 필요)
- arc_draft_validator.py dead code: 동일 사유 → P2 GAP 유지
- 공통-E threshold 하드코딩: 테스트에서 매직넘버 직접 사용 중일 가능성 → P1 GAP 유지

### 감리 3회 — 대원칙 1~4 준수 여부 최종 확인
- 공통-H(state_tracker=None 시 사망 NPC 체크 skip): TruthGate(LM-A) + advisory 체인이 2차 방어 담당하므로 P0 아님, 그러나 skip 사실 자체가 로깅되지 않음 → P1 GAP 유지
- 공통-A(장르 감지 silent except): 비무협 작품에서 wuxia 기본값 적용 시 내공 시스템 오염 → 대원칙 준수 측면에서 P1 BUG 확정

---

## 확정 BUG/GAP 항목 (감리 완료)

| 번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|------|------------|------|------|------|
| 01 | four_phase_arc_generator.py:194 | P1 | BUG | `except Exception: pass` 로깅 없는 silent except — 장르 감지 실패 시 wuxia 기본값으로 폴백 무통보 |
| 02 | four_phase_arc_generator.py:61 | P1 | GAP | `threshold: float = 0.30` 임계값 하드코딩 — `_threshold()` + validation.yaml 외부화 미완 |
| 03 | unified_arc_validator.py:199 | P1 | GAP | state_tracker=None 시 사망 NPC 체크 전면 skip, 로깅 없음 — 대원칙 4 경미 노출 |
| 04 | stage3_context.py:12 | P2 | GAP | CLAUDE.md "19슬롯" 기재 vs 실제 21슬롯 — 문서 불일치 |
| 05 | arc_corrector.py:83 | P2 | GAP | FourPhaseArcGenerator 파이프라인에서 미사용 — dead code 가능성 (사용처 확인 후 정리) |
| 06 | arc_draft_validator.py:28 | P2 | GAP | UnifiedArcValidator로 기능 대체, 사용처 미확인 — dead code 가능성 |

---

## TF 목록

| TF ID | 등급 | 파일:줄 | 패치 명세 |
|-------|------|---------|---------|
| TF-2-01 | P1 | four_phase_arc_generator.py:194 | `except Exception: pass` → `except Exception as _e: logging.warning("[FourPhase] 장르 감지 실패, wuxia 기본값 사용: %s", _e)` |
| TF-2-02 | P1 | four_phase_arc_generator.py:61 | `threshold: float = 0.30` → `_threshold("arc.ns3b_divergence_threshold", 0.30)` 또는 validation.yaml 키 추가 |
| TF-2-03 | P1 | unified_arc_validator.py:199 | state_tracker=None 조기 반환 시 `logging.warning("[UnifiedValidator] state_tracker 없음 — 사망 NPC 체크 skip")` 추가 |
| TF-2-04 | P2 | stage3_context.py:12 | CLAUDE.md "19슬롯" → "21슬롯" 문서 교정 (adversarial_self_play + session_logger 포함) |
| TF-2-05 | P2 | arc_corrector.py:83 | grep으로 전체 import 확인 후 미사용 시 deprecated/ 이동 또는 삭제 |
| TF-2-06 | P2 | arc_draft_validator.py:28 | grep으로 전체 import 확인 후 미사용 시 deprecated/ 이동 또는 삭제 |

---

## 요약

- **P0**: 0건
- **P1 BUG**: 1건 (TF-2-01: silent except 로깅 누락)
- **P1 GAP**: 2건 (TF-2-02: 임계값 하드코딩, TF-2-03: NPC 체크 skip 무통보)
- **P2 GAP**: 3건 (TF-2-04: 문서 불일치, TF-2-05/06: dead code 가능성)
- **OK**: ARC-1~10, 12 / 공통 다수 — 주요 패치 전량 정상 작동 확인
