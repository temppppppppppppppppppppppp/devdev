# 2차 전수조사 — 18번 터미널
## 누락 Core 보조 파일 + Protocol

> 발령: 4차 마스터 오더 (2026-03-06)
> 담당 파일:
> - `modules/core/config_manager.py`
> - `modules/core/quality_dashboard.py`
> - `modules/core/response_schemas.py`
> - `modules/core/tactical_utils.py`
> - `modules/core/spinners.py`
> - `modules/protocols/db_repository.py`

---

## 1단계: TF 구성

### config_manager.py

```
TF-18-01 | OK  | config_manager.py:88-99 | CM-1 확인
  _load_agents_from_yaml() fallback:
    L88-99: models.yaml 없거나 파싱 실패 시 None 반환
    L41-65: `models_from_yaml if models_from_yaml else { ... }` hardcoded fallback
    fallback dict에 19개 에이전트 모델명 포함 ✅

TF-18-02 | P2  | config_manager.py:42-65 | CM-2 확인
  fallback dict 모델명이 models.yaml 키와 일치하는지:
    fallback dict: 리터럴 문자열 "gemini-2.5-pro", "gemini-2.5-flash" 사용
    SSOT 위반 가능성: models.yaml 모델명이 변경되어도 fallback dict는 자동 갱신 안 됨
    영향: models.yaml 로드 실패 시에만 발동 (2차 방어선). 실제 발동 빈도 매우 낮음
    패치 방향: P2 (AIModels.* 또는 상수 참조로 교체하면 SSOT 완성이나 ROI 낮음)

TF-18-03 | OK  | config_manager.py:97-98 | CM-3 확인
  except Exception: pass 여부:
    L97-98: `except Exception as e: logging.warning("[SilentPass:ConfigManager] ...")` ✅
    silent 아님 (9차 전수조사 보강 이력 확인)

TF-18-04 | OK  | config_manager.py:18-19, 118-141 | CM-4 확인
  스레드 안전성:
    L18-19: `_settings_lock = threading.Lock()` (클래스 레벨)
    L122-141: `with self._settings_lock:` + double-check locking 패턴
    [INF-P1-6] 코멘트로 경합 방지 목적 명시 ✅
```

### quality_dashboard.py

```
TF-18-05 | OK  | quality_dashboard.py:183-188 | QD-1 확인
  DB 쓰기 실패 시 silent 처리 여부:
    _save_record(): L187-188: `except Exception as e: logging.warning(...)`
    silent 아님 ✅

TF-18-06 | OK  | stage2_preflight.py:466-472 | QD-2 확인
  get_score_trend_summary() at L467 except pass 여부:
    L471-472: `except Exception as _qr_e: logging.debug("[S2-QR] 품질 추세 수집 실패 (비차단): %s", _qr_e)`
    logging.debug 레벨이므로 비차단. TF-S2PE-07에서 이미 debug 레벨 보강됨 ✅

TF-18-07 | OK  | quality_dashboard.py 전체 | QD-3 확인
  Python이 직접 품질 합격/불합격 결정 여부:
    get_summary(), get_trend_injection(), get_pass_prediction_warning() 등
    모두 데이터/문자열 반환만 수행. REJECT/PASS 판정 없음 ✅ (대원칙 1 준수)

TF-18-08 | OK  | quality_dashboard.py:217-220, 446-451 | QD-4 확인
  division-by-zero 방어:
    get_summary() L217: `total = stats["pass"] + stats["reject"]; if total > 0:` ✅
    analyze_score_trend() L451: `recent_avg = sum(recent_scores) / len(recent_scores)`
      → `len(stage_records) < 3` 검사 후 도달, recent_records 비어있으면 이전 early return ✅
    get_score_trend_summary() L883: len(recent) < min_samples 조기 반환 ✅
```

### response_schemas.py

```
TF-18-09 | OK  | response_schemas.py:323-329 | RS-1 확인
  status_shadow key_stat_change 교체 완료:
    L326: "key_stat_change": types.Schema(type=types.Type.STRING, ...) ✅
    internal_energy_loss 키 없음 (교체 완료)

TF-18-10 | P1  | response_schemas.py:323-329 / arc_summary_utils.py:56 / fact_ledger.py:292 | RS-2 확인
  하위 호환 브릿지(internal_energy_loss alias) 존재 여부:
    response_schemas.py ARC_DESIGN_SCHEMA.status_shadow: key_stat_change만 존재
    internal_energy_loss alias 없음 ❌
    CLAUDE.md TF-45 설계 의도: "하위 호환 브릿지(internal_energy_loss alias) 존재 확인"
    실제 칼러:
      arc_summary_utils.py:56: `loss_str = shadow.get("internal_energy_loss", "0%")`
        → ARC_DESIGN_SCHEMA 적용 아크에서 항상 "0%" 반환
      fact_ledger.py:292: `energy = shadow.get("internal_energy_loss")`
        → None 반환 → 에너지 추적 누락
    영향 범위:
      - 무협 장르: genre_schema_builder.py 프롬프트 텍스트는 여전히 internal_energy_loss 사용
        그러나 Gemini response_schema(ARC_DESIGN_SCHEMA) 강제 시 key_stat_change 반환
        → arc_summary_utils/fact_ledger도 무협 아크에서 에너지 추적 불가
      - 비무협 장르: 설계대로 key_stat_change 반환, arc_summary_utils 영향 동일
    패치 방향:
      방법A: response_schemas.py ARC_DESIGN_SCHEMA.status_shadow에
             "internal_energy_loss" 브릿지 필드 추가 (선택사항, 하위호환)
      방법B: arc_summary_utils.py + fact_ledger.py를 key_stat_change 우선 읽기로 수정
             `shadow.get("key_stat_change") or shadow.get("internal_energy_loss", "0%")`
    우선순위: P1 (에너지 추적 데이터 무결성)

TF-18-11 | OK  | response_schemas.py:294-398 / four_phase_arc_generator.py | RS-3 확인
  ARC_DESIGN_SCHEMA 반환 키 vs four_phase_arc_generator 파싱 키:
    ARC_DESIGN_SCHEMA required: arc_no, ep_count, ep_start, ep_end, title, beat_sequence, tactical_doc
    four_phase_arc_generator는 LLM 응답 dict에서 이들 키를 직접 접근
    response_schema 강제이므로 구조 불일치 발생 확률 낮음 ✅

TF-18-12 | OK  | response_schemas.py:145-162 | RS-4 확인
  DIRECTOR_AUDIT_SCHEMA consistency_checklist optional:
    L162: required=["decision", "score", "reason", "fix_scope"]
    consistency_checklist 미포함 → optional ✅ (대원칙 3 준수)

TF-18-13 | OK  | response_schemas.py 전체 | RS-5 확인
  하위 호환 브릿지 없이 삭제된 키:
    TF-18-10에서 internal_energy_loss 브릿지 누락 확인됨 (RS-2와 동일 이슈)
    다른 삭제 키는 미발견 ✅ (RS-2로 통합)
```

### tactical_utils.py

```
TF-18-14 | OK  | tactical_utils.py | TU-1 확인
  모듈 역할:
    "에피소드별 전술 추출 공유 유틸" — episode_details 우선, regex 폴백, 전체 반환 fallback
    LLM 0회, DB 0회, 순수 Python 텍스트 추출 ✅

TF-18-15 | OK  | tactical_utils.py:31-73 | TU-2 확인
  Python이 직접 전술 판단 여부:
    extract_episode_tactical(): 에피소드 헤더 패턴 regex로 텍스트 추출만 수행
    판단 없음 ✅ (대원칙 1 준수)

TF-18-16 | OK  | director_ensemble.py:14 | TU-3 확인
  사용처 확인:
    `from modules.core.tactical_utils import extract_episode_tactical`
    director_ensemble.py (L14)에서 import 확인 ✅
    dead code 아님
```

### spinners.py

```
TF-18-17 | OK  | modules/core/spinners.py 전체 | SP-1, SP-2 확인
  stage0/spinner.py와 기능 중복 여부 + 사용처 확인:
    spinners.py: [V65] 순환 참조 해소용 모듈. FancySpinner, StageSpinner, rich_console,
      V50_MODULES_AVAILABLE, STAGE0_AVAILABLE 전역 플래그 포함
    stage0/spinner.py: Stage 0 내부 전용 (11번 터미널 커버 대상)
    두 파일은 다른 역할 — 순환 참조 해소 허브 vs Stage 0 전용. 중복 아님 ✅
    실제 import 확인:
      stage2_orchestrator.py, stage3_orchestrator.py, stage4_orchestrator.py,
      stage2_preflight.py, stage2_finalizer.py, stage01_helpers.py에서 적극 사용 ✅
    dead code 아님
```

### db_repository.py

```
TF-18-18 | OK  | db_repository.py vs db_manager.py | DR-1 확인
  Protocol 키 목록 vs db_manager.py 실제 구현 메서드:
    db_repository.py 주석: "시그니처 근거: modules/core/db_manager.py (1,147줄, 50개 public)"
    Protocol은 db_manager.py 기반으로 설계됨 ✅

TF-18-19 | OK  | tests/test_protocols_services.py:254-315 | DR-2 확인
  구현체(db_manager.py) Protocol 준수 여부:
    test_protocols_services.py L258-315: TestDBRepositoryProtocol
      DBManager가 핵심 메서드 보유하는지 확인 테스트 ✅
    B-3 Protocol 표준화 이후 검증됨

TF-18-20 | P2  | db_repository.py, app_services.py | DR-3 확인
  dead code 여부 — 프로덕션 코드에서 import 여부:
    grep 결과: production import 없음
      tests/test_protocols_services.py: import ✅
      modules/protocols/app_services.py:107: 주석 참조만 ("# DBRepositoryProtocol 구현체")
    DBRepositoryProtocol은 @runtime_checkable이나 production에서 isinstance() 사용 없음
    실질적 역할: 문서화 + 테스트용 명세서 (타입 힌트로도 미활용)
    패치 방향: P2 유지. 실제 타입 어노테이션에 활용 시 완전 활성화 가능
```

---

## 2단계: 감리 1회 — CLAUDE.md 의도적 설계 오탐 제거

**TF-18-02 (CM fallback dict 리터럴)**: `_load_agents_from_yaml` 실패 시 마지막 안전망.
CLAUDE.md 주석에 "실패 시 None" + 즉시 fallback 설계가 명시. P2 (ROI 낮음) 적절.
→ **P2 유지**

**TF-18-10 (RS-2 internal_energy_loss 브릿지 누락)**: CLAUDE.md "하위호환 브릿지(internal_energy_loss alias)
존재 확인 (TF-45 설계 의도)" — 브릿지가 **존재해야** 하는데 없음. 의도적 설계 누락이 아닌 구현 누락.
→ **P1 유지**

**TF-18-20 (DB Protocol 프로덕션 미활용)**: CLAUDE.md B-3 "Protocol 전면 표준화 완료" 이력.
표준화는 done이나 실제 타입 어노테이션 적용은 pending. 의도 일부 미완성.
→ **P2 유지**

---

## 3단계: 감리 2회 — 대원칙 1~4 준수 여부

**quality_dashboard.py**: 모든 메서드가 데이터/advisory 반환만. PASS/REJECT 판정 없음.
대원칙 1 준수 ✅

**response_schemas.py**: ARC_DESIGN_SCHEMA, DIRECTOR_AUDIT_SCHEMA 등 스키마 정의만.
Python이 직접 판단하는 코드 없음. 대원칙 1 준수 ✅

**TF-18-10 (RS-2)**: arc_summary_utils.py, fact_ledger.py의 internal_energy_loss 읽기는
LLM이 반환한 데이터를 수집하는 Python 역할. 대원칙 1 위반 아님.
데이터 손실 문제이지 판단 우회가 아님.

---

## 4단계: 감리 3회 — 기존 테스트 커버 여부

**TF-18-02**: ConfigManager fallback dict 테스트 — test_pipeline_wiring.py 등에 config_manager 모킹.
fallback dict 구체적 검증 테스트 없음. P2 유지.

**TF-18-10**: arc_summary_utils.py의 internal_energy_loss 접근 테스트 없음.
fact_ledger의 status_shadow 에너지 추적 테스트 없음. P1 확정 (미커버 + 실제 데이터 손실).

**TF-18-20**: test_protocols_services.py에 DBRepositoryProtocol 테스트 존재 ✅.
프로덕션 미활용은 기능 결함은 아님. P2 유지.

---

## 최종 결과

| 등급 | 건수 | 항목 |
|------|------|------|
| P0   | 0    | — |
| P1   | 1    | TF-18-10 (internal_energy_loss 브릿지 누락 → 에너지 추적 데이터 손실) |
| P2   | 2    | TF-18-02 (fallback dict 리터럴 모델명), TF-18-20 (DBRepositoryProtocol 프로덕션 미활용) |
| OK   | 17   | TF-18-01, 03~09, 11~19 (단, TF-18-13은 RS-2 통합) |

### TF-18-10 패치 요약 (권장 방법B — 칼러 수정)

**파일 A**: `modules/core/arc_summary_utils.py:56`
```python
# Before:
loss_str = shadow.get("internal_energy_loss", "0%")
# After:
loss_str = shadow.get("key_stat_change") or shadow.get("internal_energy_loss", "0%")
```

**파일 B**: `modules/core/fact_ledger.py:292`
```python
# Before:
energy = shadow.get("internal_energy_loss")
# After:
energy = shadow.get("key_stat_change") or shadow.get("internal_energy_loss")
```

우선순위: P1 (에너지/핵심 수치 추적 데이터 손실 방지)
