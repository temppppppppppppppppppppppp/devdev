# 2차 전수조사 — 19번 터미널 보고서

> 조사일: 2026-03-06
> 담당: 19번 터미널
> 대상: director_grading.py / manager.py / state_delta_tracker.py (기존 오더 미배정 3종)

---

## 1단계. TF 구성

| ID | 등급 | 파일:줄번호 | 분류 | 설명 | 패치 방향 |
|----|------|------------|------|------|-----------|
| TF-19-01 | P2 | state_delta_tracker.py:76-84 | GAP | `RECOVERY_LIMITS` 무협 전용 키워드 하드코딩 — 비무협 장르에서 `_get_max_recovery()` 항상 기본_회복(5) 반환, 에너지 회복 advisory 경고 과다 발생 가능. `[TF-8-06]` 주석으로 이미 인지됨. | YAML 외부화 또는 비무협 bypass 조건 추가 (P2 유지) |

**발견된 P0: 0건 / P1: 0건 / P2: 1건**

---

## 2단계. 감리 1회 — CLAUDE.md 명시 의도적 설계 오탐 제거

### [DG-1] DirectorGradingSystem — Director 우회 여부 (대원칙 3)

**결론: CLEAR**

- `DirectorGradingSystem`은 `director.py`에 `self._grading`으로 주입된 Director **내부** 서브시스템.
- `apply_adaptive_decision()`은 CONDITIONAL_PASS를 반환할 수 있으나,
  `director_ensemble.py:982-989`에 명시적 보호 로직 존재:
  ```python
  if final_verdict == "CONDITIONAL_PASS":
      if original_verdict == "REJECT":
          # [TF-22b] 디렉터 주권: Director REJECT는 Python이 뒤집지 않음
          final_verdict = "REJECT"
  ```
- Director의 REJECT는 절대 번복되지 않음. CONDITIONAL_PASS = "Python이 조정하려 했으나 Director 주권 존중으로 원복" 의미.
- **대원칙 3 완전 준수.**

### [DG-2] LLM 파싱 실패 fail-closed/open

**결론: N/A**

- 파일 docstring: "순수 데이터 가공 — LLM 호출 없음, BaseAgent 의존 없음"
- LLM 호출 없음 → 파싱 실패 경로 없음.

### [DG-3] 채점 결과 성격 (advisory vs 자동 판정)

**결론: CLEAR**

- `grade_manuscript_v59()` 반환값: A/B/C/D 등급 + 수정 가이드 레이블 → advisory 성격. PASS/REJECT 판정 없음.
- `apply_adaptive_decision()` CONDITIONAL_PASS → TF-22b 보호 로직으로 실제 판정 영향 없음.
- **Director 주권 존중 구조.**

### [DG-4] 모델 SSOT

**결론: N/A** — LLM 호출 없으므로 모델 SSOT 이슈 없음.

### [MG-1] update_state_and_lore_v20() — Python 직접 상태 변경 여부 (대원칙 2)

**결론: CLEAR**

- 코드 흐름:
  1. 프롬프트 조립 (Python 포맷팅)
  2. `self.ask(full_prompt, temperature=0.1)` — **LLM 호출**
  3. `self._extract_json_robust(response)` — 파싱 후 그대로 반환
- 코드 주석: "AI가 준 데이터를 섣부르게 내부에서 가공(.get 호출 등)하지 않고 가장 강력한 파싱 엔진의 결과물만 그대로 main_a.py의 방탄 로직으로 넘깁니다."
- 실제 상태 적용(WorldState/DB 기록)은 `stage4_post_processor.py`가 담당.
- **대원칙 2 완전 준수.**

### [MG-2] NPC 속성 변경 npc_history append-only 기록

**결론: CLEAR (설계 의도)**

- Manager는 LLM 결과를 raw 반환만 함. npc_history 기록은 호출자(stage4_post_processor)가 담당.
- 원칙 2에 따라 Python(Manager)이 직접 NPC 속성을 수정하지 않는 올바른 구조.

### [MG-3] 사망 NPC 부활 Python 자동 허용

**결론: CLEAR**

- Manager 내부에 deceased NPC 필터링 로직 없음.
- TruthGate (LM-A) advisory 체인이 downstream에서 사망 NPC 등장 감지 담당.
- Manager는 데이터 수집/전달만 수행.

### [MG-4] audit_event() 구현 (TF-20 XC-002)

**결론: 구현 완료 확인**

- `stage4_post_processor.py:712-713`:
  ```python
  if callable(getattr(self.ctx, "audit_event", None)):
      self.ctx.audit_event("manager_parse_failure", "Manager LLM 파싱 실패", {"ep": next_ep})
  ```
- `stage4_post_processor.py:737-738`:
  ```python
  if callable(getattr(self.ctx, "audit_event", None)):
      self.ctx.audit_event("manager_complete_failure", "Manager LLM 완전 실패", {"ep": next_ep})
  ```
- `callable(getattr(self.ctx, "audit_event", None))` 가드로 audit_event 없는 컨텍스트에서도 안전.
- TF-20 XC-002 "Manager audit_event" **완전 구현 확인.**

### [MG-5] 예외 처리 silent 여부

**결론: CLEAR**

- `manager.py:263`: `except Exception as e: logging.warning(f" [Manager Parsing Error] {e}")` — 로깅 완비, silent pass 없음.

### [SD-1] apply_energy_delta() — Python 직접 에너지 변경 (대원칙 2)

**결론: CLEAR**

- `apply_energy_delta()` → `self.current_energy` 메모리 수정.
- 호출 경로: `continuity_tracker.py:349-354` → advisory warning 생성 후 Director MC 주입.
- StateDeltaTracker는 canonical 상태 저장소(WorldState/DB)가 아닌 **advisory 전용 메모리 추적기**.
- 실제 WorldState 에너지 변경은 LLM이 생성한 state_updates 경유.
- **대원칙 2 준수.**

### [SD-2] apply_injury() DB 저장 여부

**결론: CLEAR (적절한 설계)**

- 메모리 only. DB 기록 없음.
- advisory용 임시 추적기이므로 DB 저장 불필요. canonical 부상 기록은 Manager/WorldState 경로.

### [SD-3] 사용처 — 운영 경로 포함 여부

**결론: 활성 운영 경로 사용 확인**

- `main_a.py:1726`: `self.state_delta_tracker = StateDeltaTracker()` — V60.26 모듈로 실 운영 경로 생성.
- `continuity_tracker.py:55`: `self._ci.state_tracker = StateDeltaTracker(...)` — ContinuityTracker에서 advisory용 사용.
- `project_service.py:312-317`: rollback_to() 검증 + RollbackInvariant 로깅.
- **dead code 아님. 운영 경로 정상 사용.**

### [SD-4] 에너지 클램핑 Python 단독 처리 (대원칙 1)

**결론: CLEAR**

- `state_delta_tracker.py:124`: `after = max(0, min(100, before + delta))` — Python 범위 제한.
- 대원칙 1의 "판단"은 "오류인가?", "수정할까?" 같은 **서사적 판단**을 가리킴.
- 수치 범위 클램핑(0~100)은 데이터 포맷팅/정합성 — 서사적 판단 아님.
- 과도 회복 경고(`warning` 필드)도 advisory로만 반환, 강제 적용 없음.
- **대원칙 1 준수.**

---

## 3단계. 감리 2회 — 대원칙 1~4 준수 여부

| 대원칙 | 파일 | 판정 | 근거 |
|--------|------|------|------|
| 1. Python은 수집만 | state_delta_tracker — 클램핑 | ✅ | 범위 제한은 데이터 정합성, 서사 판단 아님 |
| 2. 팩트시트 수정 권한은 LLM | manager.py | ✅ | LLM 호출 → raw 반환, 호출자가 적용 |
| 2. 팩트시트 수정 권한은 LLM | state_delta_tracker | ✅ | 메모리 advisory 추적, canonical DB 불변 |
| 3. Director 주권주의 | director_grading — apply_adaptive_decision | ✅ | TF-22b 보호 로직: REJECT 번복 불가 |
| 3. Director 주권주의 | director_grading — on_approve_workflow | ✅ | Director 내부 서브시스템의 수치 범위 검증 |
| 4. 사망 캐릭터 | manager.py | ✅ | Python 필터링 없음, TruthGate 위임 |

**대원칙 위반: 0건**

---

## 4단계. 감리 3회 — 기존 테스트 커버 확인

| 항목 | 테스트 파일 | 커버 여부 |
|------|------------|----------|
| DirectorGradingSystem.grade_manuscript_v59 | tests/test_director_modules.py | ✅ |
| apply_adaptive_decision | tests/test_director_modules.py:724-742 (3개), test_pass_with_fix.py:465/479 | ✅ |
| CONDITIONAL_PASS TF-22b 보호 로직 | tests/test_v75c_contradiction_firewall.py:226-234 | ✅ |
| Manager.update_state_and_lore_v20 장르 분기 | tests/test_genre_schema_builder.py:200+ | ✅ |
| Manager 파싱 실패 반환 | tests/test_agents.py:66-80 | ✅ |
| StateDeltaTracker rollback_to | tests/property/test_rollback_props.py:178+ (8개) | ✅ |
| StateDeltaTracker rollback boundary | tests/chaos/test_rollback_boundary.py | ✅ |
| StateDeltaTracker RollbackInvariant | tests/chaos/test_partial_commit.py:50+ | ✅ |
| StateDeltaTracker apply_energy_delta 경고 | tests/property/test_rollback_props.py | ✅ |
| TF-19-01 비무협 RECOVERY_LIMITS 과다경고 | **없음** | ❌ P2 수준 |

**테스트 미커버: TF-19-01만 해당 (P2)**

---

## 5단계. 최종 등급 확정

| ID | 최종 등급 | 설명 |
|----|-----------|------|
| TF-19-01 | **P2** | state_delta_tracker RECOVERY_LIMITS 무협 전용 — advisory 전용이라 운영 영향 없음. 비무협에서 advisory 경고 과다 발생 가능. 이미 `[TF-8-06]` 주석으로 인지된 알려진 이슈. |

---

## 요약

**P0: 0건 / P1: 0건 / P2: 1건**

3개 파일 모두 대원칙 1~4 위반 없음:

- **DirectorGradingSystem**: LLM 호출 없는 순수 Python 데이터 가공. `apply_adaptive_decision()`은 Director 내부에서 호출되며 TF-22b 보호 로직으로 Director REJECT 번복 불가. 대원칙 3 완전 준수.
- **Manager**: `update_state_and_lore_v20()`은 LLM 호출 → raw 반환 구조로 대원칙 2 완전 준수. TF-20 XC-002 audit_event는 `stage4_post_processor.py`에 2곳(parse_failure/complete_failure) 구현 완료.
- **StateDeltaTracker**: 메모리 전용 advisory 추적기. canonical 상태(WorldState/DB) 불변. `apply_energy_delta()`는 ContinuityTracker advisory 경로에서만 호출. 대원칙 1·2 준수. 유일한 P2는 무협 전용 RECOVERY_LIMITS 키워드가 비무협에서 과다 advisory를 유발할 수 있으나 운영 영향 없음.
