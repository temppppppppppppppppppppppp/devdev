# 2차 전수조사 — 5번 터미널 보고서

> 담당: 5번 터미널
> 영역: Chief Writer + Writer 에이전트 체인
> 기준 커밋: `9a55ac0`
> 조사 완료: 2026-03-06

---

## 조사 대상 파일

| 파일 | 조사 방법 | 비고 |
|------|-----------|------|
| `modules/domain/agents/chief_writer.py` | 전량 + grep | 854줄 |
| `modules/domain/agents/chief_writer_context.py` | grep 전수 | 1,074줄 |
| `modules/domain/agents/chief_writer_quality.py` | 전량 읽음 | 859줄 |
| `modules/domain/agents/chief_writer_prompts.py` | (import 확인) | - |
| `modules/domain/agents/writer.py` | 전량 읽음 | 373줄 |
| `modules/core/writer_prompt_builders.py` | 전량 읽음 | 235줄 |
| `modules/core/writing_directive_generator.py` | 전량 읽음 | 197줄 |
| `modules/core/pattern_tracker.py` | 전량 읽음 | 1,126줄 |
| `config/prompts/chief_writer.yaml` | 전량 읽음 | 124줄 |

---

## 전용 체크 항목 결과 (CW-1 ~ CW-13)

| 항목 | 파일:줄번호 | 등급 | 상태 | 설명 |
|------|-------------|------|------|------|
| CW-1 | chief_writer_quality.py:673-692 | - | OK | `_check_ending_hook_presence`: blueprint→ending_hook 취득, 마지막 500자, 앞 20자 부분 일치 — 정상 구현 |
| CW-2 | chief_writer_quality.py:67-267 | - | OK | `apply_self_critique` 10개 체크 순서 확인: 1.HUD 2.클리셰 3.정당화 4.NPC 5.동기/약속 6.WritingDirective 7.표현신선도 8.ending_hook 9.산술 10.메타월. TF-G 게이트(L138-161)는 루프 진입 전 사전 실행 |
| CW-3 | chief_writer_quality.py:281-320 | - | OK | `_check_system_term_exposure`: regex `Block\s+\d+\|Arc\s+\d+\|Stage\s+\d+\|Blueprint\|treatment` 감지, 의료 장르 Stage/treatment 제외 처리 |
| CW-4 | chief_writer_quality.py:322-444 | - | OK | `_check_arithmetic_consistency`: mult/pct/add_sub 3패턴 모두 구현, 5% tolerance, 한국어 수 단위(억/만/조) 파싱 |
| CW-5 | chief_writer.py:826-875 | - | OK | `inplace_patch` JSON 1단계(L832-851): `{...}` 전체 파싱 → content/text/manuscript/patched_text/patched_manuscript 키 시도. rfind position 0 보호: L860 `_outer_start > 0` 조건 |
| CW-6 | stage4_interview_round.py:1868-1873 | - | OK | `_generate_candidates` InPlace 빈 manuscript 실패 처리(L1869): `not any(c.get("manuscript","").strip())` → candidates=None 폴백. 추가로 run()L357-358 후처리 필터링 |
| CW-7 | chief_writer_context.py | P2 | GAP | NS-3-A `_format_block_numeric_targets()`가 chief_writer_context.py에 없음. 실제 구현은 analyst.py:64. 마스터 오더 체크 항목 파일 오귀속 (오탐) |
| CW-8 | stage4_interview_round.py:336-338 | - | OK | [TF-I] L338 `_common_writer_kwargs["director_feedback"] = director_feedback` 설정 후 L1825 `generate_ensemble(**_common_writer_kwargs)` 전개. 명시적 director_feedback= 중복 주입 없음 |
| CW-9 | writing_directive_generator.py:132-196 | - | OK | `_parse_response`: 1단계 직접 `json.loads`, 2단계 `{...}` 구간 추출 폴백, 실패 시 `WritingDirective()` 빈 객체 반환 |
| CW-10 | pattern_tracker.py:226-243 | - | OK | `build_report`: LLM 호출 0회(순수 Python), DB 조회 실패 시 L229 `return PatternReport()` 빈 보고서 폴백 |
| CW-11 | chief_writer.yaml:39-47 | - | OK | 규칙 12(ending_hook 의무화, L39), 규칙 13(씬별 대화 의무, L43), 규칙 14(메타 월 금지, L44-47) 전량 존재 |
| CW-12 | writer.py:57 | - | OK | `write_v20_manuscript`만 유지. 파일 도입부 주석: "오케스트레이터 호출 경로 제거됨. 외부 진입점용" |
| CW-13 | chief_writer_context.py | P2 | GAP | TF-55b STATIC/DB_NPC_RELATIONSHIP 리다이렉트 경로가 chief_writer_context.py에 없음. 실제 구현은 stage4_context_builder.py:159-165. 마스터 오더 체크 항목 파일 오귀속 (오탐) |

---

## 공통 체크 항목 결과 (A~H)

| 항목 | 파일:줄번호 | 등급 | 상태 | 설명 |
|------|-------------|------|------|------|
| 공통-A | chief_writer.py:379-382 | P2 | GAP | PerfTimer try 블록 내 `except Exception: pass` — logging 없음. 비치명 경로이나 bare except |
| 공통-A | writer.py:300-301 | P2 | GAP | `get_genre_rules_prompt()` 내 `except Exception: return ""` — logging.warning 없음 |
| 공통-B | - | - | OK | writing_directive_generator.py 2단계 폴백, chief_writer.py _extract_json_robust 폴백 모두 완비 |
| 공통-C | - | - | OK | Python이 직접 pass/fail 결정하는 코드 없음. Self-Critique는 issues 반환만, 최종 판단은 Director |
| 공통-D | writer.py:38 | P2 | GAP | `model_tier="gemini-2.5-flash"` 하드코딩. AIModels.* 미사용. P2 유보 7개 파일 목록에 Writer 미포함 |
| 공통-E | chief_writer_quality.py:15 | P2 | GAP | `CLICHE_WINDOW = 10` 하드코딩. `_threshold()` 미사용 |
| 공통-F | - | - | OK | 파일 내 직접 DB cursor/connection 접근 없음. context.db 통해 간접 접근 |
| 공통-G | pattern_tracker.py | P2 | GAP | TF-54a 이후 `build_report()`만 사용되고, `analyze_manuscripts`, `analyze_genre_patterns_v59`, `analyze_trend_v59`, `generate_writer_injection`, `save_to_db`, `load_from_db`, `generate_trend_report_v59` 7개 메서드 잠재적 dead code (외부 호출 경로 미확인) |
| 공통-H | - | - | OK | deceased NPC 방어는 TruthGate(LM-A) 담당 (대원칙 4 설계). chief_writer_quality NPC 체크는 관계 일관성만. 아키텍처상 의도적 분리 |

---

## 감리 3회 기록

### 1차 감리 — CLAUDE.md 명시 의도적 설계 제거

- **CW-7**: NS-3-A는 CLAUDE.md에서 "Analyst 레벨 self-critic"으로 명시. chief_writer_context 체크 항목 자체 오귀속 → **오탐**. P2 유지(파일 잘못됨 기록용).
- **CW-13**: TF-55b는 CLAUDE.md에서 "context_advisor + stage4_context_builder" 배선으로 명시. chief_writer_context 체크 항목 오귀속 → **오탐**. P2 유지.
- **공통-H**: CLAUDE.md: "TruthGate(LM-A) → … 전부 `_director_mc_parts`에 주입, Director 최종 판정" — 의도적 분리. **오탐 제거**.
- **공통-C**: 대원칙 1·3 준수 확인 — Python self-critique는 issues list만 반환, Director가 결정. **오탐 제거**.

### 2차 감리 — 테스트 커버리지 및 기존 완료 패치 확인

- **CW-1~CW-6**: 9차 전수조사/TF-G/TF-I/TF-47 패치 항목들. CLAUDE.md 완료 기록과 코드 일치 확인.
- **writer.py:38 하드코딩**: CLAUDE.md "Model SSOT 완료" 항목에 writer.py 명시 없음. 9차 전수조사에서 "모델 SSOT 3건" 패치 대상이 `state_tracker_npc`/`manuscript_validator`. Writer 미포함 → 실제 GAP.
- **CLICHE_WINDOW**: CLAUDE.md에 외부화 기록 없음 → 실제 GAP.

### 3차 감리 — 대원칙 1~4 최종 확인

- 대원칙 1 (Python 판단 금지): `_check_arithmetic_consistency`, `_check_ending_hook_presence` 등은 issues 목록만 생성, LLM(Director)이 최종 결정 → OK.
- 대원칙 2 (팩트시트 LLM만): chief_writer 계열은 원고 생성만, 팩트 수정 없음 → OK.
- 대원칙 3 (Director 주권): self-critique는 CW advisory. Director bypass 없음 → OK.
- 대원칙 4 (사망 NPC): TruthGate 담당, chief_writer 레벨 의도적 비구현 → OK.

---

## TF 목록 (확정 BUG/GAP)

| TF ID | 등급 | 파일:줄 | 패치 명세 |
|-------|------|---------|-----------|
| TF-5-01 | P2 | chief_writer.py:379-382 | PerfTimer `except Exception: pass` → `except Exception as e: logging.debug("[CW] PerfTimer 기록 실패: %s", e)` |
| TF-5-02 | P2 | writer.py:300-301 | `get_genre_rules_prompt` `except Exception: return ""` → `except Exception as e: logging.warning("[Writer] genre_rules_prompt 실패: %s", e); return ""` |
| TF-5-03 | P2 | writer.py:38 | `model_tier="gemini-2.5-flash"` → `AIModels.FLASH` (Model SSOT 편입) |
| TF-5-04 | P2 | chief_writer_quality.py:15 | `CLICHE_WINDOW = 10` → `_threshold("quality.cliche_window", 10)` 연동 |
| TF-5-05 | P2 | pattern_tracker.py (전체) | `analyze_manuscripts`/`analyze_genre_patterns_v59`/`analyze_trend_v59`/`generate_writer_injection`/`save_to_db`/`load_from_db`/`generate_trend_report_v59` 7개 메서드 호출 경로 확인 후 dead code 판정 시 제거 |
| TF-5-06 | P2 | 마스터 오더 CW-7 | 체크 항목 파일 오귀속 기록 — NS-3-A 실제 위치: analyst.py:64 |
| TF-5-07 | P2 | 마스터 오더 CW-13 | 체크 항목 파일 오귀속 기록 — TF-55b 실제 위치: stage4_context_builder.py:159-165 |

---

## 요약

| 구분 | 건수 |
|------|------|
| P0 | 0 |
| P1 | 0 |
| P2 | 7 (TF-5-01~07) |
| OK | 13 |
| 오탐 제거 | 4 (CW-7/CW-13 오귀속 + 공통-C/H 의도적 설계) |

**P0/P1 없음. 전량 P2 (비치명, 정기 정리 대상).**
