# 2차 전수조사 — 11번 터미널 보고서

> 터미널: 11번 (Stage 0 파이프라인 + Services + 나머지 Domain Agents)
> 기준 커밋: `9a55ac0`
> 조사일: 2026-03-06
> 담당 파일: 13개

---

## 담당 파일 목록

```
modules/core/stage0/__init__.py           (578줄)
modules/core/stage0/story_expander.py    (562줄)
modules/core/stage0/reverse_expander.py  (대형)
modules/core/stage0/style_extractor.py   (798줄)
modules/core/stage0/preset_registry.py   (737줄)
modules/core/stage0/spinner.py
modules/core/services/audit_service.py   (93줄)
modules/core/services/state_service.py   (368줄)
modules/core/services/ui_service.py      (138줄)
modules/core/services/project_service.py (370줄)
modules/domain/agents/block_enricher.py  (924줄)
modules/domain/agents/negative_example_injector.py (405줄)
modules/domain/models.py                 (63줄)
```

---

## TF 목록 (감리 3회 완료)

| TF번호 | 등급 | 파일:줄 | 종류 | 설명 | 패치 방향 |
|--------|------|---------|------|------|-----------|
| TF-11-01 | P1 | block_enricher.py:476,519,831 | GAP | [공통-D] `"gemini-2.5-flash"` 하드코딩 3곳 — `_validate_enrichment`, `_director_audit_block`, `_check_causal_errors`에서 `AIModels.*` 미사용 | `AIModels.SUMMARY_MODEL` 또는 `AIModels.V50_MODULE_MODEL`로 교체 |
| TF-11-02 | P1 | block_enricher.py:451 | GAP | [공통-A] `enrich_block()` 최외곽 `except Exception as e: return {"enriched": False, ...}` — `logging.warning` 없음 | `logging.warning("enrich_block failed: %s", e)` 추가 |
| TF-11-03 | P1 | block_enricher.py:486 | GAP | [공통-A] `_validate_enrichment()` `except Exception as e: return {"validation_result": "FAIL", ...}` — `logging.warning` 없음 | `logging.warning("_validate_enrichment failed: %s", e)` 추가 |
| TF-11-04 | P1 | block_enricher.py:540 | GAP | [공통-A] `_director_audit_block()` `except Exception as e: return {"decision": "PASS", ...}` — `logging.warning` 없음 | `logging.warning("_director_audit_block failed: %s", e)` 추가 |
| TF-11-05 | P1 | block_enricher.py:528-536 | GAP | [공통-C] Python이 LLM `decision` 필드를 `total_score >= 70` 하드코딩 임계값으로 덮어씀 — 대원칙 3(Director 주권) 위반 | LLM이 `decision` 직접 반환하도록 프롬프트 강화 + Python 덮어쓰기 제거, score는 참고값으로만 활용 |
| TF-11-06 | P1 | project_service.py:354 | GAP | [공통-F] `wipe_production_data()`에서 `project.db.conn.commit()` 직접 호출 — `_safe_commit()` 우회 | `self._safe_commit(project.db)` 교체 (rollback 보장) |
| TF-11-07 | P1 | project_service.py:215-222 | GAP | [S0-9] `rollback_episode()` — DB commit 성공 후 물리 파일 삭제 시 원자성 미보장, 파일 삭제 실패 시 DB·파일 불일치 | 파일 삭제 실패 시 `logging.error` + `_safe_commit` rollback 시도 경로 추가 |
| TF-11-08 | P1 | stage0/__init__.py:435-437 | GAP | [S0-5] `run_reference_analysis()`의 `style_guide.json` 저장 `open()` — try/except OSError 없음, 디스크 오류 시 비정상 종료 | `try/except OSError as e: logging.warning(...)` 래핑 추가 |
| TF-11-09 | P1 | reverse_expander.py:117 | GAP | [공통-A] `_parse_json()` `except Exception: return None` — bare except, logging 없음 | `except (json.JSONDecodeError, ValueError, IndexError) as e: logging.debug("_parse_json failed: %s", e); return None` |
| TF-11-10 | P2 | preset_registry.py:460 | GAP | [S0-6] `__init__()` 미지원 장르 인자 시 `if base_genre in self.GENRE_PRESETS:` else 묵살 — warning 없음 | `else: logging.warning("PresetRegistry: unknown genre '%s', using common only", base_genre)` 추가 |
| TF-11-11 | P2 | block_enricher.py:533 | GAP | [공통-E] `if total_score >= 70:` 하드코딩 임계값 — validation.yaml 외부화 정책 불일치 | `_threshold("block_audit_pass_score", 70)` 교체 (단, TF-11-05 패치 시 이 라인 삭제 필요) |
| TF-11-12 | P2 | state_service.py:153 | GAP | [공통-A] `load_character_archetypes()` `except Exception as e: print(...)` — `logging.warning` 미사용 | `logging.warning("load_character_archetypes failed: %s", e)` 교체 |
| TF-11-13 | P2 | models.py:52-59 | GAP | [공통-C] `Bible.update_martial_status()`가 Python에서 직접 `self.realm`, `self.internal_energy` 수정 — 대원칙 2(팩트시트 수정 권한은 LLM만) 형식적 위반, 단 해당 클래스 사용처 불명확 | dead code 여부 확인 후 미사용 시 파일 전체 삭제, 사용 중이면 LLM 경유 경로로 이전 |

---

## 감리 기록

### 감리 1회 — 오탐 1차 제거 (의도적 설계 확인)

```
[검토] project_service.py:280-284 traceback.print_exc()
  근거: rollback_episode() 사용자 대면 에러 출력 맥락, UI 레이어 의도적 사용
  판정: 오탐 — 단, P2 수준으로 logging 전환 권장 항목에서 제외 (UI 레이어 특성 인정)

[검토] block_enricher.py:476,519 primary_model try/finally 임시 오버라이드
  근거: Flash 모델 임시 사용 설계이나 모델 문자열 자체는 하드코딩
  판정: P1 유지 (AIModels.* 미사용은 CLAUDE.md Model SSOT 정책 위반)

[검토] block_enricher.py:528-536 Python score→PASS/REJECT 변환
  근거: LLM이 total_score를 반환하고 Python이 임계값 비교로 decision 덮어씀
         "Director 주권주의" — LLM 판정을 Python이 override하는 것은 대원칙 3 위반
  판정: P1 유지 (대원칙 3 위반)
```

### 감리 2회 — 오탐 2차 제거 (대원칙 1~4 준수 확인)

- TF-11-01 (모델 하드코딩): 대원칙 외 SSOT 위반 — P1 유지
- TF-11-02~04 (silent except): Python이 판단하는 것 아님, logging 누락만 — P1 유지
- TF-11-05 (PASS/REJECT 덮어쓰기): Python이 `total_score >= 70`으로 합격 판정 — 대원칙 3 위반. P1 유지
- TF-11-06 (safe_commit 우회): 운영 중 `wipe_production_data()` DB 불일치 위험 — P1 유지
- TF-11-07 (파일 삭제 원자성): DB commit 후 OS 레벨 실패 시 상태 불일치 — P1 유지
- TF-11-08 (OSError 미방어): 디스크 오류 시 전체 Stage 0 프로세스 비정상 종료 — P1 유지
- TF-11-09 (bare except): `story_expander._parse_json`은 특정 예외 명시(`json.JSONDecodeError, ValueError, IndexError`)인데 `reverse_expander._parse_json`은 bare `except Exception` — 불일치. P1 유지
- TF-11-13 (models.py): 사용처 불명확하나 `Bible.update_martial_status()` 형식적 대원칙 2 위반 → P2 유지 (dead code 가능성 포함)

### 감리 3회 — 오탐 3차 제거 (기존 테스트 커버 확인)

- TF-11-01 (모델 하드코딩): `block_enricher.py` 모델 문자열 테스트 미커버 → P1 유지
- TF-11-05 (PASS/REJECT): `_director_audit_block()` Python 임계값 비교 경로 테스트 미커버 → P1 유지
- TF-11-06 (safe_commit 우회): `wipe_production_data()` 직접 commit 경로 테스트 미커버 → P1 유지
- TF-11-07 (원자성): 파일 삭제 실패 + DB commit 완료 시나리오 테스트 미커버 → P1 유지
- TF-11-10 (미지원 장르 묵살): `preset_registry` 미지원 장르 경로 warning 미커버 → P2 유지

---

## OK 확인 항목

| 체크 항목 | 근거 |
|-----------|------|
| S0-1 — PresetRegistry → StateTracker 연동 | CLAUDE.md §현재 상태 "Stage 0 ↔ Stage 2 연동: StateTracker가 PresetRegistry 참조" 완료 명시. main_a.py는 담당 파일 외 (0번 터미널) |
| S0-2 — Bible 파싱 실패 폴백 | `__init__.py:237-239` `if not self.bible.get("MasterBible"): return {}, [], None` fail-closed ✅. `story_expander.generate_bible():172` protagonist 빈값 시 `return None` ✅ |
| S0-3 — ReverseExpander VecMemory 실패 방어 | ReverseExpander 내부에 VecMemory 직접 호출 없음. `__init__.py`에서 `self._reverse_expander` 저장 후 main_a.py 위임. 담당 외 확인 불가, 설계 의도 확인 ✅ |
| S0-4 — Arc/Blueprint 역추출 스킵 | `reverse_expander.py` 모듈 docstring: "Arc/Blueprint는 스킵, 원고 직접 참조 방식" 명시. 구현 코드 내 Arc 생성 로직 없음 ✅ |
| S0-7 — audit_service DB 트랜잭션 | audit_service는 JSONL 파일 기록 (DB 트랜잭션 없음). `flush_audit_buffer():68` `except Exception as e: self._ui_log(...)` ✅ |
| S0-10 — block_enricher 원본 블록 보존 | `enrich_block():348-358` `[D5-P0-1]` 주석, `_extract_json_robust` + `return {"enriched": False, "block": current_block}` 폴백 ✅ |
| S0-11 — negative_example_injector 대원칙 1 준수 | `generate_injection()` 텍스트 반환만. `_rejection_lock = threading.Lock()` 동시성 보호. Python 판단 없음 ✅ |
| S0-12 — story_expander `_parse_json` 예외 특정 | `except (json.JSONDecodeError, ValueError, IndexError): return None` — 특정 예외 명시 ✅ |
| 공통-B — LLM 파싱 폴백 | block_enricher 전체 `_extract_json_robust()` 사용 ✅. story_expander `_parse_json` 폴백 ✅ |
| 공통-D — story_expander 모델 SSOT | `AIModels.SUMMARY_MODEL`, `AIModels.V50_MODULE_MODEL`, `AIModels.TIER_1_ARCHITECT` 사용 ✅ |
| 공통-F — audit_service 리소스 | JSONL append 방식, DB 커넥션 없음 ✅ |
| 공통-G — spinner.py dead code | Rich UI 유틸, 적극 사용 중 ✅ |
| 공통-H — 사망 NPC 방어 | Stage 0은 Bible/Treatment 생성 단계 (원고 미생성). 사망 NPC 행동 노출 경로 없음 ✅ |
| 공통-H — preset_registry | NPC 프리셋만, 원고 생성 없음 ✅ |

---

## 요약

| 등급 | 건수 |
|------|------|
| P0 | 0건 |
| P1 | 9건 (공통-A silent except 4건 + 공통-C 대원칙 3 위반 1건 + 공통-D 모델 하드코딩 1건 + 공통-F safe_commit 우회 1건 + S0-5 OSError 미방어 1건 + S0-9 원자성 갭 1건) |
| P2 | 4건 (S0-6 장르 묵살 + 공통-E 임계값 하드코딩 + 공통-A print 사용 + 공통-C dead code 대원칙 2) |
| 오탐 제거 | 1건 (project_service traceback.print_exc — UI 레이어 의도적 설계) |
| OK | 14건 |
