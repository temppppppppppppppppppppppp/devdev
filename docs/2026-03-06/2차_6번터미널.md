# 6번 터미널 — Director 체인 전수조사 보고서 (2차)

**감사 대상**: director.py, director_auditor.py, director_grading.py, director_caching.py, director_prompts.py, manager.py, preflight_checker.py, critic.py, weaver.py, consensus_validator.py, manuscript_validator.py
**감사일**: 2026-03-06
**감리**: 3회 완료 (자기감리 Round 1~3)
**결론**: P0 없음 / P1 3건 / P2 7건 / OK 8건

---

## 발견 목록

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|-----------|------|------|------|
| DIR-9 | director_caching.py:172 | P1 | GAP | get_protagonist_config() except Exception silent — logging 없음, protagonist_config 오류 미탐지 |
| DIR-9b | director_caching.py:122 | P1 | GAP | create_manuscript_cache() 재사용 조건이 count 일치만 체크 — TTL/만료 체크 없어 만료된 캐시명 반환 가능 |
| 공통-E | director_auditor.py:654 | P1 | GAP | RepetitionGuard DB조회 `except Exception: pass` silent — 반복 감지 무력화 미탐지 |
| 공통-C | preflight_checker.py:500 | P2 | GAP | factory 기본값 `"gemini-2.5-pro"` 하드코딩 — AIModels.* 미참조, Model SSOT 위반 |
| 공통-C | consensus_validator.py:455 | P2 | GAP | factory 기본값 `"gemini-2.5-flash"` 하드코딩 — AIModels.* 미참조, Model SSOT 위반 |
| 공통-D | consensus_validator.py:155 | P2 | GAP | `ENSEMBLE_TIMEOUT = 120` 하드코딩 — SINGLE_VOTE_TIMEOUT은 YAML 참조하나 전체 타임아웃은 미외부화 |
| 공통-D | director_grading.py:532 | P2 | GAP | adaptive threshold clamp `max(45, min(85, base))` / `max(3500, min(6000, length_base))` 하드코딩 — validation.yaml 미참조 |
| DIR-11 | critic.py:439 | P2 | GAP | get_llm_critique() `except Exception → return {}` fail-open — 외부 미호출(dead code)이나 패턴 자체가 위험 |
| DIR-11 | critic.py:566 | P2 | GAP | deep_review() `except Exception → return PASS 기본값` fail-open — critique_manuscript() 경유 실제 호출 경로, 합격률 왜곡 가능 |
| 공통-F | manager.py:282 | P2 | GAP | audit_for_consistency() `return self.ask(...)` raw string 반환 — _extract_json_robust 미사용, LLM hallucination 취약 |

---

## OK 항목 (오탐 제거 후 확정)

| 항목번호 | 파일 | 상태 | 근거 |
|---------|------|------|------|
| DIR-1 | director_ensemble.py:983 | OK | TF-22b guard 확인 — CONDITIONAL_PASS+REJECT 조합 → 최종 REJECT 강제 (Director 주권 보호) |
| DIR-2 | director_grading.py:549 | OK | PASS_WITH_FIX bypass 정상 — PASS일 때만 QualityGate 적용, PASS_WITH_FIX는 bypass (TF-46) |
| DIR-3 | director_grading.py:549 | OK | apply_adaptive_decision() CONDITIONAL_PASS 반환 — 호출자(director_ensemble.py)에서 TF-22b guard로 처리 |
| DIR-5 | director_auditor.py | OK | _strategic_audit_with_self_consistency() 다수결 투표 정상 구현 |
| DIR-6 | director_auditor.py:626 | OK | 분량 4000자 미만 Python REJECT — 대원칙 1 예외 허용 (명확한 룰 기반 기계적 판정) |
| DIR-7 | base_agent.py | OK | audit_event() — manager.py가 아닌 base_agent.py에 DEBUG 레벨로 구현 (위치 확인) |
| DIR-8 | preflight_checker.py | OK | ThreadPoolExecutor 미사용 — 단일 LLM 호출, 병렬 처리 불필요 |
| DIR-10 | manuscript_validator.py:608 | OK | `model=AIModels.FLASH_ANALYSIS_MODEL` — SSOT 적용 확인 |
| DIR-12 | consensus_validator.py:365 | OK | `majority_threshold = (total_count // 2) + 1` — 동수 tie → PASS 보수적 처리, 의도적 설계 |

---

## TF 목록 (패치 권고)

| TF번호 | 등급 | 파일:줄 | 패치 명세 |
|--------|------|---------|----------|
| TF-6-01 | P1 | director_caching.py:172 | `except Exception:` 블록에 `logging.warning("[V60.89] protagonist_config 로드 실패: ...")` 추가 |
| TF-6-02 | P1 | director_caching.py:122 | 캐시 생성 시 `self._cached_at = time.time()` 기록, 재사용 조건에 `time.time() - self._cached_at < ttl_seconds` TTL 체크 추가 |
| TF-6-03 | P1 | director_auditor.py:654 | `except Exception: pass` → `except Exception as e: logging.debug(f"[RepetitionGuard] DB조회 실패 ep={i}: {e}")` |
| TF-6-04 | P2 | preflight_checker.py:500 | factory 기본값 `"gemini-2.5-pro"` → `AIModels.PRO_ANALYSIS_MODEL` (또는 동등 SSOT 상수)로 교체 |
| TF-6-05 | P2 | consensus_validator.py:455 | factory 기본값 `"gemini-2.5-flash"` → `AIModels.FLASH_MODEL` (또는 동등 SSOT 상수)로 교체 |
| TF-6-06 | P2 | consensus_validator.py:155 | `ENSEMBLE_TIMEOUT = 120` → `_TIMEOUTS.get("ensemble", 120)` 로 YAML 외부화 (system.yaml ensemble_timeouts.consensus_vote.ensemble 키 추가) |
| TF-6-07 | P2 | director_grading.py:532 | clamp 상수 `(45, 85, 3500, 6000)` → validation.yaml `adaptive_threshold.clamp.*` 키 추가 후 `_threshold()` 참조로 교체 |
| TF-6-08 | P2 | critic.py:393 | get_llm_critique() dead code 제거 또는 fail-open → fail-closed(`return None`) 변경 후 호출자 None 처리 추가 |
| TF-6-09 | P2 | critic.py:566 | deep_review() 실패 시 `_default_review_result()` 반환 값 `"recommendation": "REVISE"` 로 변경 (PASS fail-open 제거) |
| TF-6-10 | P2 | manager.py:282 | audit_for_consistency() `return self.ask(...)` → `response = self.ask(...)` + `if "CRITICAL_ERROR" in response: ...` 명시적 파싱 또는 `_extract_json_robust` 구조화 응답 전환 |

---

## 메모

- **director_prompts.py** DIRECTOR_AUDIT_PROMPT_V30/ENSEMBLE_SELECTION_PROMPT의 PASS_WITH_FIX 미포함: YAML 경로가 메인(config/prompts/director.yaml), Python fallback 스키마는 구식 유지 허용 가능. 별도 TF 불필요.
- **weaver.py**: 전량 logging.warning 포함, fail-closed 정상. 이상 없음.
- **get_fix_scope_stats**: db_manager.py:2563 정의, stage4_interview_round.py L55/626에서 정상 참조. 이상 없음.
- **consensus_validator.py:155 주석**: "consensus는 ensemble key 미사용"으로 의도 문서화되어 있으나 YAML 외부화가 바람직 — TF-6-06 권고 유지.
