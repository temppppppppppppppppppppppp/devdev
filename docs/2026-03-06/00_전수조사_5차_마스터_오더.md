# 전수조사 5차 마스터 오더 — 0번 터미널 발령

> 발령일: 2026-03-06
> 발령자: 0번 터미널 (총괄 감리)
> 목적: 4차 오더 완료 후 발굴된 미조사 영역 + 실전모순패치 신규 코드 검증
> 기준 커밋: `9a55ac0` (+ 현재 working directory 패치 포함)
> 터미널: 19~26번 (8개)

---

## 발령 배경

1~4차 오더(1~18번 터미널)는 커밋 `9a55ac0` 기준 전체 Python 파일을 배정했으나,
`git diff HEAD` 전수 비교 결과 아래 두 가지 조사 공백이 확인됨:

**A. 기존 오더 미배정 파일 3개** — 어떤 터미널에도 배정되지 않음
**B. 실전모순패치 신규 구현 코드** — 기존 터미널 조사 범위 밖

---

## 공통 작업 수칙 (1~4차와 동일)

```
1단계. TF 구성하며 조사
   → 형식: TF-[터미널번호]-[순번] | 등급 | 파일:줄번호 | BUG/GAP | 설명 | 패치 방향

2단계. 감리 1회 — CLAUDE.md 명시 의도적 설계 오탐 제거
3단계. 감리 2회 — 대원칙 1~4 준수 여부 재확인
4단계. 감리 3회 — 기존 테스트 커버 여부 확인, 최종 등급 확정
5단계. 문서화 후 0번 터미널에 보고
```

> **코드 수정은 0번 터미널(Claude)만 수행.**
> **저장 경로**: `docs/2026-03-06/2차_N번터미널.md` (N = 19~26)

---

## 19번 터미널 — 기존 오더 미배정 파일 3종

**배경**: Director/Agent/Core 계열 파일이지만 1~4차 오더 어디에도 배정되지 않음.
특히 `Manager(BaseAgent)`는 `update_state_and_lore_v20()` 보유 — 대원칙 2 위반 가능성 높음.

**담당 파일:**
```
modules/domain/agents/director_grading.py  # DirectorGradingSystem — Director 계열 누락
modules/domain/agents/manager.py           # Manager(BaseAgent) — update_state_and_lore_v20
modules/core/state_delta_tracker.py        # StateDeltaTracker — 에너지/부상 델타 추적
```

**전용 체크 항목:**
```
[DG-1]  director_grading — DirectorGradingSystem 역할 파악: Director를 우회하는 채점 로직인지 (대원칙 3)
[DG-2]  director_grading — LLM 파싱 실패 시 fail-closed vs fail-open 분기
[DG-3]  director_grading — 채점 결과가 advisory인지 자동 PASS/REJECT 판정인지 (대원칙 3)
[DG-4]  director_grading — 모델 SSOT: AIModels.* 또는 self.host 경유 여부

[MG-1]  manager.py — update_state_and_lore_v20(): Python이 직접 상태/설정 변경인지
         vs LLM state_updates 경유인지 (대원칙 2 핵심 위험)
[MG-2]  manager.py — NPC 속성 변경 시 npc_history append-only 기록 여부 (3-5A 패턴)
[MG-3]  manager.py — 사망 NPC 부활/재등장 Python 자동 허용 여부 (대원칙 4)
[MG-4]  manager.py — audit_event() 구현 여부 (TF-20 XC-002: Manager audit_event)
[MG-5]  manager.py — 예외 처리 silent 여부

[SD-1]  state_delta_tracker — apply_energy_delta(): LLM 판단 없이 Python이 직접 에너지 변경인지 (대원칙 2)
[SD-2]  state_delta_tracker — apply_injury(): 부상 기록 DB 저장 여부 vs 메모리 only
[SD-3]  state_delta_tracker — 사용처(호출 파일) 확인: 운영 경로 포함 여부 vs dead code
[SD-4]  state_delta_tracker — 에너지 0 이하 클램핑 로직이 Python 단독 처리인지 (대원칙 1)
```

**저장**: `docs/2026-03-06/2차_19번터미널.md`

---

## 20번 터미널 — 4th wall 방어 신규 코드 검증

**배경**: 실전모순패치 "Block 2" 대사 노출 사례 이후 3단계 방어 체계를 신규 구현.
chief_writer_quality에 `_check_system_term_exposure()` 메서드 추가, chief_writer.yaml에 규칙14 추가.
신규 코드이므로 기존 조사(5번 터미널)가 이 코드를 커버하지 못함.

**담당 파일:**
```
modules/domain/agents/chief_writer_quality.py  # _check_system_term_exposure() — 신규
config/prompts/chief_writer.yaml               # 규칙14 (4th wall) — 신규
```

**전용 체크 항목:**
```
[FT-1]  chief_writer_quality._check_system_term_exposure() 구현 확인:
         - 탐지 패턴: "Block N/Arc N/Stage N/Blueprint/treatment" regex 올바른지
         - self-critique 10번째 체크로 올바르게 배선됐는지
         - 탐지 결과가 advisory인지 자동 REJECT인지 (대원칙 3)

[FT-2]  _check_system_term_exposure() 예외 처리:
         - except Exception: pass 여부 (silent 실패 위험)
         - 원고 없음/빈 문자열 처리

[FT-3]  chief_writer.yaml 규칙14 내용 검증:
         - "Block N/Arc N/Stage N" 등 시스템 용어 명시 여부
         - 기존 규칙 1~13과 충돌 여부
         - 규칙 번호 순서 정합성 (14가 실제로 14번째인지)

[FT-4]  4th wall 방어 3단계 체계 교차 검증:
         - 1단계 chief_writer.yaml 규칙14: 프롬프트 생성 단계 금지 ✅/❌
         - 2단계 _check_system_term_exposure(): self-critique 10번째 체크 ✅/❌
         - 3단계 truth_gate 8번째 메서드: CLAUDE.md "P2 후순위" — 미구현 확인 ✅/❌
         - 3단계 미구현 시 위험도 평가

[FT-5]  기존 테스트 커버:
         - test_pipeline_wiring.py 또는 test_satisfaction_step2_prompts.py에서
           _check_system_term_exposure 테스트 여부 확인
```

**저장**: `docs/2026-03-06/2차_20번터미널.md`

---

## 21번 터미널 — 레버리지 수익률 신규 검사 코드 검증

**배경**: 실전모순패치에서 `_check_leverage_return_pct()` + `_LEVERAGE_PCT_RE` 신규 추가.
5번 터미널 또는 9번 터미널이 numeric_consistency_checker를 커버했으나 이 메서드는 이후 추가됨.

**담당 파일:**
```
modules/core/numeric_consistency_checker.py  # _check_leverage_return_pct() + _LEVERAGE_PCT_RE — 신규
modules/core/arc_summary_utils.py            # key_stat_change 패치 (방금 적용) — 교차 검증
modules/core/fact_ledger.py                  # key_stat_change 패치 (방금 적용) — 교차 검증
```

**전용 체크 항목:**
```
[LR-1]  _check_leverage_return_pct() 구현 확인:
         - "X달러→Y달러 × N배 → Z%" 패턴 감지 regex: _LEVERAGE_PCT_RE 올바른지
         - 10%p 이상 괴리 시 MAJOR 경고 로직
         - 파싱 실패 시 graceful 처리 (silent 실패 아닌지)

[LR-2]  _LEVERAGE_PCT_RE 정규식 검증:
         - 한국어 숫자 표현 ("억원", "만원" 등) 처리 여부
         - 음수 수익률(손실) 감지 여부
         - False positive 가능성 (일반 문장에서 오탐 위험)

[LR-3]  _check_leverage_return_pct() → NC-1 advisory 체인 배선:
         - `check_all()` 또는 `run_all_checks()` 에서 호출되는지
         - 반환 advisory가 Director MC에 주입되는 경로 확인

[LR-4]  방금 적용한 key_stat_change 패치 교차 검증:
         - arc_summary_utils.py:56 — `shadow.get("key_stat_change") or shadow.get("internal_energy_loss", "0%")`
           무협 장르(key_stat_change=None): internal_energy_loss fallback → 기존 동작 유지 ✅/❌
           비무협(key_stat_change="자산+5%"): 문자열 반환 → re.search(r"(\d+)") 파싱 정상? ✅/❌
         - fact_ledger.py:292 — `shadow.get("key_stat_change") or shadow.get("internal_energy_loss")`
           key_stat_change가 숫자 아닌 문자열일 때 `isinstance(energy, int | float)` 체크 통과? ✅/❌

[LR-5]  기존 테스트 커버:
         - test_numeric_consistency.py 존재 여부 + _check_leverage_return_pct 테스트 포함 여부
```

**저장**: `docs/2026-03-06/2차_21번터미널.md`

---

## 22번 터미널 — NS-4-S2 신규 코드 검증 (Arc 시간 마커)

**배경**: NS-4-S2/S4 구현에서 `four_phase_arc_generator._ns4_extract_time_markers()` 신규 추가.
2번 터미널이 four_phase_arc_generator를 커버했으나 이 메서드는 이후 추가됨.
또한 CLAUDE.md에서 **미구현 P1 잔여**: `arc_start_state.location` 강제 주입 코드가 명시됨.

**담당 파일:**
```
modules/domain/agents/four_phase_arc_generator.py  # _ns4_extract_time_markers() 신규 + P1 잔여
```

**전용 체크 항목:**
```
[NS2-1] _ns4_extract_time_markers() 구현 확인:
         - 모듈 상단 정의 여부 (독립 유틸 함수 or 인스턴스 메서드)
         - regex 패턴: 한국어 시간 표현 (N일 후, N개월 뒤, 다음날 등) 커버 여부
         - LLM 0회 확인 (CLAUDE.md "LLM 0회" 명시)

[NS2-2] _generate_prev_context() NS-4 주입 확인:
         - return 직전 ⏱️[NS-4] 블록 실제 포함 여부
         - 이전 Arc 시간 마커 누락 시 graceful 처리

[NS2-3] P1 잔여 — arc_start_state.location 강제 주입 미구현 확인:
         - CLAUDE.md: "_generate_prev_context() → 다음 Arc arc_start_state.location 강제 주입 코드는 미구현(P1 잔여)"
         - 실제 코드에서 location 주입 코드 존재 여부 확인
         - 미구현 시 위험도: Arc 경계에서 공간 연속성 파괴 가능성
         - 패치 방향 수립 (0번 터미널 수행용)

[NS2-4] Arc 실행 상태 연속성 (TF-48) 와 NS-4 충돌 여부:
         - TF-48: WorldState/FactLedger 실행 결과 → prev_context 주입
         - NS-4: 시간 마커 → prev_context 주입
         - 두 주입이 충돌하거나 one이 other를 덮어쓰는지

[NS2-5] 기존 테스트 커버:
         - test_ns4_time_continuity.py 존재 여부 + NS-4-S2 케이스 커버 확인
```

**저장**: `docs/2026-03-06/2차_22번터미널.md`

---

## 23번 터미널 — NS-4-S4 신규 코드 검증 (Stage 4 Arc 시간 연속성)

**배경**: NS-4-S4 구현에서 `stage4_interview_round` 내 NS-4 시간 마커 + arc_pos==1 mandatory_context prepend 신규 추가.
4번 터미널이 stage4_interview_round를 커버했으나 이 코드는 이후 추가됨.

**담당 파일:**
```
modules/core/stage4_interview_round.py  # _ns4_extract_time_markers() + arc_pos==1 — 신규
```

**전용 체크 항목:**
```
[NS4-1] stage4_interview_round._ns4_extract_time_markers() 구현 확인:
         - 모듈 상단 독립 함수 여부 (22번 터미널 four_phase_arc_generator 버전과 동일 구조인지)
         - 원고 텍스트에서 시간 표현 추출 정확도

[NS4-2] NS-4-S4 Director MC 주입 배선:
         - [Timeline] advisory 직후 [Arc 시간 연속성 참고] 블록 추가 여부
         - `_director_mc_parts`에 올바르게 append되는지
         - 빈 마커 시 불필요한 빈 블록 주입 여부

[NS4-3] arc_pos==1 mandatory_context prepend 검증:
         - CLAUDE.md "arc_pos==1 시 CW mandatory_context에 위치 변경 묘사 의무화 지시 prepend"
         - 실제 arc_pos 계산 로직 확인
         - prepend가 mandatory_context 앞에 붙는지 vs 뒤에 붙는지 (순서 중요)
         - arc_pos가 1이 아닐 때 조건 분기 정상 여부

[NS4-4] arc_pos==1 + NS-4-S4 상호 간섭:
         - arc_pos==1 prepend가 NS-4 advisory 이후에 추가될 때 중복/충돌 여부
         - 두 주입이 director_mc_parts 내 순서가 올바른지

[NS4-5] 기존 테스트 커버:
         - test_pipeline_wiring.py에서 arc_pos==1 prepend 테스트 여부
         - NS-4-S4 시간 마커 주입 테스트 여부
```

**저장**: `docs/2026-03-06/2차_23번터미널.md`

---

## 24번 터미널 — YAML 신규 변경 검증 (director.yaml + chief_writer.yaml)

**배경**: 실전모순패치에서 두 핵심 YAML 파일에 신규 규칙/조항 추가.
16번 터미널이 Config 전체를 커버했으나 이 변경은 이후 추가됨.

**담당 파일:**
```
config/prompts/director.yaml       # Arc 경계 공간연속성 MAJOR 감점 조항 2곳 — 신규
config/prompts/chief_writer.yaml   # 규칙14 (4th wall 금지) — 신규 (20번 터미널과 분업)
```

> **주의**: chief_writer.yaml 규칙14는 20번 터미널(FT-3)과 겹치는 부분이 있음.
> 24번은 **director.yaml 변경 + chief_writer.yaml 규칙 체계 전체** 검토에 집중.
> 20번은 **_check_system_term_exposure() 코드**에 집중.

**전용 체크 항목:**
```
[YM-1]  director.yaml Arc 경계 공간연속성 조항 검증:
         - VARIABLE_PROMPT (L72 근방): Arc 첫 화 MAJOR 감점 조항 실제 포함 여부
         - SELECTION_PROMPT (L359 근방): 동일 조항 포함 여부
         - 두 위치 조항이 일관성 있는지 (VARIABLE_PROMPT vs SELECTION_PROMPT 내용 동일 여부)

[YM-2]  director.yaml 신규 조항과 기존 NC-3 consistency_checklist 충돌 여부:
         - space_continuity 체크리스트 항목과 신규 MAJOR 감점 조항 중복/충돌
         - Arc 첫 화 예외 처리 기술 여부 (Arc 내 이동은 허용)

[YM-3]  chief_writer.yaml 규칙 전체 체계 정합성 검토:
         - 규칙 1~14 번호 순서 연속성 확인
         - 규칙 간 충돌 여부 (특히 새 규칙14와 기존 규칙 간)
         - TF-E 톤 조절 (강압→완화) 이후 일관된 톤 유지 여부

[YM-4]  YAML 로드 경로 확인:
         - director.yaml 변경 사항이 PromptLoader를 통해 실제 로드되는지
         - 캐시 무효화 필요 여부 (싱글톤 PromptLoader가 변경 반영하는지)

[YM-5]  기존 테스트 커버:
         - test_satisfaction_step2_prompts.py 또는 관련 테스트에서 director.yaml 변경 검증 여부
         - chief_writer.yaml 규칙14 로드 테스트 여부
```

**저장**: `docs/2026-03-06/2차_24번터미널.md`

---

## 25번 터미널 — P2 잔여 처리 + arc_start_state.location P1 설계

**배경**: 2차 전수조사에서 P2로 유보된 3건 + CLAUDE.md 미구현 P1 잔여 1건.
P2 3건은 주석 보강/현황 재확인 용도. P1 잔여(location 주입)는 패치 방향 설계.

**담당 파일 (조사 대상):**
```
modules/domain/agents/director_continuity.py  # TF-17-14 P2 — fail-open 주석 보강 여부
modules/core/config_manager.py                # TF-18-02 P2 — fallback dict 리터럴 현황
modules/protocols/db_repository.py            # TF-18-20 P2 — 프로덕션 활용 가능성 재검토
modules/domain/agents/four_phase_arc_generator.py  # P1 잔여 — location 주입 설계
```

**전용 체크 항목:**
```
[P2-1]  TF-17-14 재확인: director_continuity.validate_entity_consistency() L122-124
         - fail-open 주석 현재 상태 확인 (9차 전수조사 이후 보강됐는지)
         - CLAUDE.md "advisory 성격" 명시 확인
         - P2 유지/하향/패치 여부 재판단

[P2-2]  TF-18-02 재확인: config_manager.py fallback dict 리터럴 모델명
         - 현재 리터럴 값이 models.yaml 실제 키와 일치하는지 검증
         - 모델명 변경 시 리스크 크기 재평가
         - P2 유지/패치 여부 재판단

[P2-3]  TF-18-20 재확인: db_repository.py 프로덕션 활용 가능성
         - 현재 프로덕션 import 없음 재확인
         - 타입 어노테이션 활용 가능한 곳 1~2곳 식별 (활성화 기회 탐색)
         - P2 유지/활성화 여부 재판단

[P1-R]  미구현 P1 잔여: arc_start_state.location 강제 주입
         - CLAUDE.md: "four_phase_arc_generator._generate_prev_context() → 다음 Arc
           arc_start_state.location 강제 주입 코드는 미구현(P1 잔여)"
         - 현재 _generate_prev_context() 코드에서 location 처리 상태 확인
         - 패치 방향 수립:
           WorldState.location → arc N 마지막 WorldState에서 읽어서 Arc N+1 prev_context에 주입
         - 기존 arc_end_state.location 스키마 존재 여부 확인
```

**저장**: `docs/2026-03-06/2차_25번터미널.md`

---

## 26번 터미널 — 방금 적용한 P1 패치 3건 테스트 커버리지 + 교차 검증

**배경**: 4차 오더에서 적용한 P1 패치 3건 (TF-17-05, TF-18-10A/B)에 대한
신규 단위 테스트 작성 가능성 검토 + 다른 터미널 조사 결과와 교차 검증.

**담당 파일 (조사 대상):**
```
modules/core/response_schemas.py      # fiction_term_leak 패치 테스트 범위
modules/core/arc_summary_utils.py     # key_stat_change 패치 테스트 범위
modules/core/fact_ledger.py           # key_stat_change 패치 테스트 범위
tests/test_pipeline_wiring.py         # 기존 배선 테스트 — 패치 영향 여부
tests/test_satisfaction_step2_prompts.py  # 기존 테스트 — 패치 영향 여부
```

**전용 체크 항목:**
```
[TC-1]  fiction_term_leak 스키마 추가 영향 범위:
         - DIRECTOR_AUDIT_SCHEMA를 로드하는 코드 전수 (director_ensemble.py 외)
         - 기존 테스트에서 DIRECTOR_AUDIT_SCHEMA 사용하는 픽스처/mock 파악
         - fiction_term_leak OK/ISSUE 처리 경로 단위 테스트 작성 필요 항목 도출

[TC-2]  key_stat_change or fallback 패턴 영향 범위:
         - arc_summary_utils.py의 shadow 처리가 호출되는 경로 전수
         - fact_ledger.py의 status_shadow 처리 전수
         - 무협 장르 (internal_energy_loss 값): fallback 동작 검증 케이스
         - 비무협 장르 (key_stat_change 문자열): 파싱 경로 검증 케이스

[TC-3]  fact_ledger.py key_stat_change 파싱 안전성 재확인:
         - `energy = shadow.get("key_stat_change") or shadow.get("internal_energy_loss")`
         - 비무협에서 key_stat_change = "자산 5% 증가" (문자열) 반환 시
           `isinstance(energy, int | float)` → False → update_number 미실행
           → 에너지 추적 여전히 안 됨?? (21번 터미널 LR-4와 연계)
         - 파싱 로직 추가 필요 여부 재검토

[TC-4]  기존 테스트 suite 실행 영향 분석:
         - 3개 파일 변경이 기존 3,390 passed 테스트에 영향 주는지 확인
         - 실패하는 테스트 있으면 원인 파악 → 0번 터미널 보고

[TC-5]  신규 테스트 작성 권장 목록 도출:
         - fiction_term_leak ISSUE 카운팅 테스트
         - key_stat_change 무협/비무협 분기 테스트
         - arc_summary_utils final_energy 계산 정합성 테스트
```

**저장**: `docs/2026-03-06/2차_26번터미널.md`

---

## 0번 터미널 — 5차 취합 프로세스

19~26번 터미널 완료 후:
1. 19번(신규 파일 3종) P0/P1 발견 시 즉시 패치
2. 21번 TC-3 이슈 (key_stat_change 문자열 파싱) 확인 → 필요 시 fact_ledger 추가 패치
3. 22번 P1 잔여(arc_start_state.location) 패치 방향 → 0번 구현
4. 26번 신규 테스트 작성 권장 목록 → 테스트 파일 추가
5. 최종 오탐 제거 3회 후 6차 오더 또는 완결 선언

**최종 보고서**: `docs/2026-03-06/2차_0번터미널_최종취합.md` (5차 내용 추가)

---

## 커버리지 완결 확인표

| 영역 | 1차 | 3차 | 4차 | 5차 | 비고 |
|------|-----|-----|-----|-----|------|
| Stage 2 파이프라인 | ✅ 1번 | — | — | — | |
| Arc 생성/검증 | ✅ 2번 | — | — | — | |
| Blueprint/Continuity | ✅ 3번 | — | — | — | |
| Stage 4 오케스트레이터 | ✅ 4번 | — | — | — | |
| Chief Writer/Writer | ✅ 5번 | — | — | — | |
| Director 본체 체인 | ✅ 6번 | — | — | — | |
| Director 앙상블/연속성 | ✅ 17번 | — | — | — | 4차 |
| **Director 채점** | ❌ | ❌ | ❌ | ✅ **19번** | **5차 신규** |
| Advisory 체인 | ✅ 7번 | — | — | — | |
| Memory/DB/상태 | ✅ 8번 | — | — | — | |
| Validation/Guard | ✅ 9번 | — | — | — | |
| Config SSOT/Base | ✅ 10번 | — | — | — | |
| Stage 0 | — | ✅ 11번 | — | — | |
| Services | — | ✅ 11번 | — | — | |
| **Manager(BaseAgent)** | ❌ | ❌ | ❌ | ✅ **19번** | **5차 신규** |
| **StateDeltaTracker** | ❌ | ❌ | ❌ | ✅ **19번** | **5차 신규** |
| Core 보조 에이전트 | — | ✅ 12번 | — | — | |
| 추적/관계/캐릭터 | — | ✅ 13번 | — | — | |
| 품질/스타일/장르/HUD | — | ✅ 14번 | — | — | |
| 운영/모니터링 | — | ✅ 15번 | — | — | |
| Config 잔여+Tests | — | ✅ 16번 | — | — | |
| Core 보조 + Protocol | — | — | ✅ 18번 | — | 4차 |
| **4th wall 신규 코드** | ❌ | ❌ | ❌ | ✅ **20번** | **5차 신규** |
| **레버리지 신규 코드** | ❌ | ❌ | ❌ | ✅ **21번** | **5차 신규** |
| **NS-4-S2 신규 코드** | ❌ | ❌ | ❌ | ✅ **22번** | **5차 신규** |
| **NS-4-S4 신규 코드** | ❌ | ❌ | ❌ | ✅ **23번** | **5차 신규** |
| **YAML 신규 변경** | ❌ | ❌ | ❌ | ✅ **24번** | **5차 신규** |
| **P2 잔여 + P1 location** | ❌ | ❌ | ❌ | ✅ **25번** | **5차 신규** |
| **P1 패치 테스트 커버** | ❌ | ❌ | ❌ | ✅ **26번** | **5차 신규** |
