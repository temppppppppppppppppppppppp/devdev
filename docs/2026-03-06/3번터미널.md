# 터미널 3 오더 — Stage 2/3 전수 감사 결과

> 작성: 2026-03-06
> 트리거: TF-57 병행 전수조사 터미널 3
> 범위: Stage 2/3 핵심 wiring 5개 항목
> 감리: 3회 완료 (오탐 1건 제거)

---

## 1. 조사 범위

| 파일 | 역할 |
|------|------|
| `modules/core/stage2_orchestrator.py` | Stage 2 Arc 오케스트레이터 |
| `modules/core/stage2_validation_pipeline.py` | pre-Director 검증 체인 |
| `modules/core/stage2_preflight.py` | Analyst 컨텍스트 구성 + NarrativeContextFormatter |
| `modules/core/stage2_finalizer.py` | Director 심사 + PASS_WITH_FIX 루프 |
| `modules/core/stage3_orchestrator.py` | Blueprint 생성 + quality_risk |

---

## 2. 조사 결과 (5개 항목)

### 항목 1 — NS-4-S2 wiring

**조사 대상**: `stage2_preflight.py` → `four_phase_arc_generator._ns4_extract_time_markers()` → `_generate_prev_context()` NS-4 주입 체인

**코드 근거**:
- `stage2_preflight._preflight_arc_analysis()` L480-514: NarrativeContextFormatter 호출 + enhanced_context 병합
- enhanced_context → `FourPhase.generate()` 인자 전달 → `_generate_prev_context()` 내부에서 `⏱️[NS-4]` 블록 주입
- NS-4 실제 구현 위치: `four_phase_arc_generator.py` (CLAUDE.md NS-4-S2/S4 패치 기준)

**상태**: ✅ OK

호출 체인 `stage2_preflight → FourPhase.generate() → _generate_prev_context()` 끊기지 않음. NS-4 시간 마커 주입 정상.

---

### 항목 2 — PASS_WITH_FIX 3회 카운터

**코드 근거** (`stage2_finalizer.py`):

```python
# L431-449
if _d_decision == "PASS_WITH_FIX":
    _MAX_FIX = 3                          # L433 상수화
    ...
    for _fix_i in range(_MAX_FIX):        # L440 루프
        _fix_scope = _current_audit.get("fix_scope", "")
        if not _fix_scope:
            _fix_scope = "inplace" if _score >= _inplace_thresh else "full"
            logging.warning("[PF-1] fix_scope 누락 → score=%d fallback: %s", ...)
        if _fix_scope in ("partial", "full"):
            break  # → REJECT → retry 경로 위임
```

3회 초과 시 fallback 경로:
- `L567-587`: PF-3 — PASS_WITH_FIX 소진 시 최고점 패치본 채택 (score=50으로 설정, Director 재심사 불필요)
- `L587`: `"[TF-32-V] PASS_WITH_FIX 수정 3회 내 미해결 → REJECT"` 로그

**상태**: ✅ OK

카운터 변수 `_MAX_FIX=3` 명시, 루프 구조 완비, 초과 시 fallback 경로 정상.

---

### 항목 3 — fix_scope 전파 (TF-34 검증)

**원 질문**: `stage2_validation_pipeline.run_validation()` 반환 dict에 fix_scope 키가 있는가?

**코드 근거**:
```python
# stage2_validation_pipeline.py L135-143
return {
    "action": "proceed",
    "refined_arc": refined_arc,
    "draft_validator_passed": draft_validator_passed,
    "consensus_passed": consensus_passed,
    "suspected_duplicates": suspected_duplicates,
    "corrections_made": _auto_corrections,   # [TF-25-09]
    "python_advisories": _python_advisories, # [TF-25-08]
}
# fix_scope 없음
```

**감리 결과 — 오탐 확정**:

`run_validation()`은 **pre-Director 검증 단계** (V60 auto-correct, draft validator, consensus). Director 심사 자체가 여기서 이루어지지 않음. fix_scope는 `stage2_finalizer.run_finalize()` 내부에서 Director 응답(`audit.get("fix_scope")`)으로 직접 취득.

```
# stage2_orchestrator.py L516-561
_val = validation_pipeline.run_validation(...)  # pre-Director 검증
refined_arc = _val["refined_arc"]               # fix_scope 불필요
_fin = await finalizer.run_finalize(...)        # Director 심사 + fix_scope 처리
```

TF-34 패치 "compare 경로 fix_scope 전파(L133)"는 **Stage 4 Validator** (`stage4_interview_round.py`) 패치였음. Stage 2에는 multi-candidate compare 경로 없음.

**상태**: ✅ OK (오탐 제거)

---

### 항목 4 — NarrativeContextFormatter enhanced_context 전달

**코드 근거** (`stage2_preflight.py:501-514`):
```python
_narrative_ctx = NarrativeContextFormatter.format_all(...)
if _narrative_ctx:
    enhanced_context = _narrative_ctx + "\n\n" + enhanced_context  # L510 prepend
    if attempt == 0:
        self.ctx.ui.log("      📖 [LM-G] 서사 구조 컨텍스트 주입 완료")
except Exception as _lmg_err:
    logging.warning("[LM-G] NarrativeContextFormatter 실패 (비치명): %s", ...)
```

`enhanced_context`는 이후 `FourPhase.generate()` 호출 시 내부 인자로 전달 → Analyst 프롬프트에 도달. **기능 정상**.

**갭**: `_preflight_arc_analysis()` 메서드 반환값에 `enhanced_context` 미포함. orchestrator 레벨에서 enrichment 적용 여부 외부 추적 불가능.

```python
# 반환값 예시 (L719-724 추정)
return {
    "four_phase_passed": ...,
    "refined_arc": ...,
    # enhanced_context 없음 → orchestrator에서 확인 불가
}
```

**상태**: ⚠️ GAP (P2 — 기능 정상, 추적성 이슈)

기능 영향 없음. 운영 중 enrichment 실패 여부를 orchestrator 로그에서 확인 불가능한 점만 문제.

---

### 항목 5 — quality_risk S3→S4 전달

**코드 근거** (`stage3_orchestrator.py:757-760`):
```python
if _quality_risk:
    blueprint["_stage3_meta"] = blueprint.get("_stage3_meta", {})
    blueprint["_stage3_meta"]["quality_risk"] = True
```

수신 측 (`stage4_interview_round.py:454-462`):
```python
if getattr(round_ctx.blueprint, "_stage3_meta", {}).get("quality_risk"):
    _director_mc_parts.append("[S3-META 경고] Blueprint 품질 리스크 감지...")
```

**상태**: ✅ OK

`stage3_orchestrator` → `blueprint._stage3_meta.quality_risk` → `stage4_interview_round` Director advisory 주입. 전달 경로 완비. V75-D 조기 트리거(`quality_risk=True` 시 1연속)도 `stage4_orchestrator`에서 동적 설정 (CLAUDE.md S3-META 명시).

---

## 3. TF 구성

### TF-3T-A: enhanced_context 외부 추적성 추가 (P2)

**우선도**: P2 (기능 영향 없음)

**근거**: 항목 4 GAP — `_preflight_arc_analysis()` 반환값에 enrichment 적용 여부 표시 없음.

**수정 방향**:
- `_preflight_arc_analysis()` 반환 dict에 `"narrative_enriched": bool(_narrative_ctx)` 추가
- orchestrator 로그에서 enrichment 여부 확인 가능하게 됨

**파일**: `modules/core/stage2_preflight.py`
**변경 범위**: 반환 dict 1줄 추가 (코드 영향 최소)
**테스트**: 기존 테스트 영향 없음 (반환 키 추가만)

---

## 4. 감리 로그

### 감리 1회 — 항목 3 BUG 주장 검증

| 검증 대상 | 근거 | 결론 |
|---------|------|------|
| run_validation() fix_scope 누락이 BUG인가? | stage2_orchestrator.py L516-561: run_validation()은 pre-Director 단계, fix_scope는 finalizer가 Director 응답에서 직접 취득 | **오탐 확정** |
| TF-34가 Stage 2에 적용되는가? | CLAUDE.md TF-34 내용: "compare 경로 fix_scope 전파(L133)" — Stage 4 Validator 패치 (L133은 stage4_interview_round.py 기준) | **Stage 2 해당 없음** |

→ 항목 3: BUG → OK로 하향

### 감리 2회 — 항목 4 GAP 심각도 검증

| 검증 대상 | 근거 | 결론 |
|---------|------|------|
| enhanced_context 미반환이 기능 영향 있는가? | FourPhase.generate() 내부에서 enhanced_context 직접 사용 확인 (preflight → generate 인자 전달) | **기능 영향 없음** |
| logging.warning("[LM-G] ... 비치명") 존재하는가? | stage2_preflight.py L514: 실패 시 warning 로그 있음 | **실패 감지 부분 가능** |

→ 항목 4: GAP P1 → GAP P2로 하향

### 감리 3회 — 항목 1 NS-4-S2 경로 재확인

| 검증 대상 | 근거 | 결론 |
|---------|------|------|
| Explore 에이전트가 LM-G(L480-514)를 NS-4-S2로 오인했는가? | NS-4-S2 실제 구현: four_phase_arc_generator.py `_ns4_extract_time_markers()` + `_generate_prev_context()` | 에이전트가 경로 혼용했으나 결론은 같음 — stage2_preflight → FourPhase.generate() → _generate_prev_context() 체인은 NS-4 포함 완전 |
| four_phase_arc_generator.py에서 NS-4 wiring 확인 가능한가? | CLAUDE.md NS-4-S2/S4 커밋 b8c06d8에서 완료 확인 | **OK 유지** |

→ 항목 1: OK 유지

---

## 5. 최종 결론

| 항목 | 상태 | 비고 |
|------|------|------|
| 1. NS-4-S2 wiring | ✅ OK | stage2_preflight → FourPhase → _generate_prev_context 체인 정상 |
| 2. PASS_WITH_FIX 3회 카운터 | ✅ OK | `_MAX_FIX=3`, 루프 + fallback 완비 |
| 3. fix_scope 전파 (TF-34) | ✅ OK *(오탐 제거)* | run_validation()은 pre-Director 단계, fix_scope는 finalizer가 Director에서 직접 취득 |
| 4. NarrativeContextFormatter 전달 | ⚠️ GAP P2 | 기능 정상, enhanced_context 반환 누락으로 외부 추적성만 부재 |
| 5. quality_risk S3→S4 | ✅ OK | stage3_orchestrator → blueprint._stage3_meta → stage4_interview_round 경로 완비 |

**BUG: 0건 / GAP P2: 1건 / 오탐 제거: 1건**

**Stage 2/3 핵심 wiring 이상 없음. TF-3T-A(P2) 단독 잔존.**
