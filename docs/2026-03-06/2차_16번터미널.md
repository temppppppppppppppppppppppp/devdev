# 16번 터미널 — Config 잔여 + Tests 핵심 감사 보고서 (2차)

**감사 대상**: config/prompts/*.json, config/genres/*.yaml, config/smart_retrieval/genre_hints.yaml, config/system.yaml, config/settings.json, tests/conftest.py, tests/test_integrity.py, tests/test_protocol_conformance.py, tests/test_phase5_hygiene.py, tests/test_pass_with_fix.py, tests/test_pipeline_wiring.py, tests/e2e/test_l3_*.py (존재 확인)
**감사일**: 2026-03-06
**감리**: 3회 완료
**결론**: P0 없음 / P1 없음 / P2 6건 / OK 14건

---

## 발견 목록

| 항목번호 | 파일:줄번호 | 등급 | 상태 | 설명 |
|---------|-----------|------|------|------|
| CF-2 | config/prompts/architect_rules.json | P2 | GAP | 사문화 — 프로덕션 코드에서 미참조. 백업 파일과 tests/stage3_isolated_test 에서만 참조. main_a.py/stage3_orchestrator.py/blueprint_ensemble.py 전량 미로드 |
| CF-4 | config/prompts/writer.json | P2 | GAP | 완전 사문화 — Python 전체 코드베이스 참조 0건. writer_rules.json은 정상 참조이나 writer.json은 방치 상태 |
| CF-7 | config/settings.json:8-9 | P2 | GAP | `scoring_model: "gemini-2.5-pro"` / `advisory_model: "gemini-2.5-pro"` 하드코딩 — director_auditor.py:274-276에서 default_config에 직접 merge → models.yaml SSOT 우회 가능 |
| CF-7b | director_auditor.py:278 | P2 | GAP | settings.json 로드 `except Exception: pass` silent — settings 로드 실패 시 미탐지 (9차 전수조사 silent except 패턴과 동일) |
| CF-10 | tests/test_integrity.py | P2 | GAP | 대원칙 2(팩트시트 수정 권한 LLM만) · 대원칙 4(사망 NPC 방어) 전용 위반 탐지 테스트 없음. test_integrity.py 10개 케이스 중 대원칙 1/3만 간접 커버 |
| CF-14 | tests/test_phase5_hygiene.py | P2 | GAP | _threshold() 키 vs validation.yaml 일치 검증 테스트 없음. test_phase5_hygiene.py는 pacing/cliffhanger/Stage4Context 등 커버하나 _threshold() 키 전수 매핑 테스트 부재 |

---

## OK 항목 (오탐 제거 후 확정)

| 항목번호 | 대상 | 상태 | 근거 |
|---------|------|------|------|
| CF-1 | analyst_libraries_investment.json | OK | 서사 패턴 라이브러리 (narrative_archetypes/intro/ending/transition 패턴). NC-1 레버리지 수익률% 검증은 Python numeric_consistency_checker.py에서 독립 처리 — 라이브러리 미포함이 정상 |
| CF-3 | config/prompts/weaver_rules.json | OK | weaver.py:97 `rules_path = self.context.paths.config / "prompts" / "weaver_rules.json"` 정상 로드 + except 처리 완비 |
| CF-4b | config/prompts/writer_rules.json | OK | main_a.py:1123 + modules/domain/agents/writer.py:242 에서 정상 로드. 파싱 실패 시 logging.warning 처리 ✓ |
| CF-5 | config/genres/*.yaml (10개) | OK | 10개 장르 YAML 전량 `genre_name` ✓. test_genre_yaml_loading.py에서 실제 필수 키 (`forbidden_terms`, `mandatory_concepts`) 전수 검증 및 PASS. 마스터 오더 CF-5 기준 "guard_class/hud_fields" 키는 Python 코드에서 미참조 → 해당 키 요구사항 자체가 부정확 |
| CF-6 | config/system.yaml | OK | base_agent.py에서 import-time 로드. `thinking_budget_map`, `api.*`, `network_retry`, `key_rotation`, `cache`, `ensemble_timeouts` 전량 사용 확인. consensus_vote.ensemble 키 미비는 TF-6-06(6번 터미널)으로 이미 기록 |
| CF-8 | config/smart_retrieval/genre_hints.yaml | OK | context_advisor.py:23 `_load_genre_hints()` 정상 로드 + L33 로드 실패 시 logging.debug + 하드코딩 폴백 완비 |
| CF-9 | tests/conftest.py | OK | `mock_db_manager`: `temp_dir/"test_project.db"` → 실전 DB 격리 완벽. `mock_project_context`: temp_dir 기반 경로. 실전 DB 직접 연결 fixture 없음. CF-9 이상 없음 |
| CF-11 | tests/test_protocol_conformance.py | OK | B-3 표준화 이후 DraftValidator/ConstraintCompilerProtocol/StateAggregator/EnsembleGenerator/ArtifactCritic 등 주요 Protocol 구조적 서브타이핑 검증 충분 |
| CF-12 | tests/test_pass_with_fix.py | OK | 파일 존재 ✓. mock 3후보 생성 + MV/CV/BV/ContV 모두 mock 완비. 실제 3-tier 라우팅 케이스 검증은 Stage4InterviewRound 내부 로직 수준까지 확인 필요하나 fixture 준비 정상 |
| CF-13 | tests/test_pipeline_wiring.py | OK | TF-54(setattr 배선) + TF-55b(STATIC/DB_NPC_RELATIONSHIP dispatch) + 합격률(ending_hook 체인) 8개 테스트 정상 실행 확인 (3,390 passed 기준선 포함) |
| CF-15a | tests/test_ns4_time_continuity.py | OK | 파일명 다름: `test_ns4_timeline_arc.py` + `test_ns4_s2_s4.py` 로 분리 존재. NS-4 핵심 케이스 커버됨 |
| CF-15b | tests/test_numeric_consistency.py | OK | 파일명 다름: `test_numeric_consistency_checker.py` 로 존재. NC-1/NC-2 핵심 케이스 커버됨 |
| CF-analyst-wuxia | config/prompts/analyst_libraries_wuxia.json | OK | 파일 없음 (예상 결과) — 무협 장르는 analyst_libraries.json 공통 라이브러리 + config/genres/wuxia.yaml 가드로 처리. 별도 파일 불필요 |

---

## TF 목록 (패치 권고)

| TF번호 | 등급 | 파일:줄 | 패치 명세 |
|--------|------|---------|----------|
| TF-16-01 | P2 | config/prompts/architect_rules.json | 사문화 확인 후 참조하는 모듈이 없으면 deprecated/ 이동 또는 삭제. blueprint 관련 에이전트에서 실제 로드가 필요하다면 stage3_orchestrator.py에 로드 로직 추가 |
| TF-16-02 | P2 | config/prompts/writer.json | Python 전체 참조 0건 확인 → deprecated/ 이동 또는 삭제 |
| TF-16-03 | P2 | config/settings.json:8-9 | `scoring_model`/`advisory_model` 값을 `models.yaml` 경유 `AIModels.*` 상수로 참조하도록 director_auditor.py L274-276 교체. 또는 settings.json에서 모델명 키 제거 후 models.yaml 단일 참조 |
| TF-16-04 | P2 | director_auditor.py:278 | `except Exception: pass` → `except Exception as e: logging.warning("[TF7-P2-09] settings.json 로드 실패: %s", e)` |
| TF-16-05 | P2 | tests/test_integrity.py | 대원칙 2 검증 테스트 추가: "Python이 직접 NPC 속성 수정 시 npc_history에 반영되지 않는다" 시뮬레이션. 대원칙 4 검증: "deceased=True NPC가 원고에 행동/대사로 등장 시 REJECT" 시뮬레이션 케이스 추가 |
| TF-16-06 | P2 | tests/test_phase5_hygiene.py | _threshold() 키 vs config/settings/validation.yaml 키 매핑 검증 테스트 추가. `validation.yaml`에 정의된 모든 키를 `_threshold()` 호출로 접근하면 None이 아닌 값 반환하는지 파라메트릭 테스트 |

---

## 메모

- **analyst_libraries_investment.json CF-1**: NC-1 `_check_leverage_return_pct` (2026-03-06 실전모순패치)는 Python regex 검사이며 이 라이브러리와 별개. 라이브러리는 LLM 서사 생성용 패턴 목록 — 수치 검증용이 아님. 정합성 문제 없음.
- **config/settings.json SSOT 구조**: settings.json → director_auditor.py default_config → ValidationOrchestrator 경로로 전달됨. 이 경로에서 모델명이 models.yaml을 우회하므로 Model SSOT 달성을 위해 TF-16-03 패치 필요.
- **test_ns4/test_numeric 파일명**: 마스터 오더 CF-15의 파일명 기준이 구버전. 실제 파일은 이름만 다를 뿐 내용 존재 — OK 처리.
- **10개 장르 YAML** (actor/alt_history/composer/cooking/fantasy/hunter/investment/medical/sports/wuxia): 10종 전량 존재, `genre_name` + `forbidden_terms` + `mandatory_concepts` 완비. investment.yaml에는 `realistic_return_rates`/`wealth_hierarchy`/`status_action_limits` 추가 필드까지 완비.
