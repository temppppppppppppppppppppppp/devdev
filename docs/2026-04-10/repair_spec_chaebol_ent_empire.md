# chaebol_ent_empire Repair Spec

Date: 2026-04-10
Status: completed bounded repair archive
Target:

- `chaebol_ent_empire`
- pre-repair state: `repair-worth-it`, opening `YELLOW`
- current state: opening `GREEN`, provisional keep after targeted repair

---

## 1. Core Diagnosis

Debt type:

- `opening signboard compression failure`

Current reading:

- opening proof is already timely
- the real debt is `B04~B10`
- public signboard lands too late, and the pair burns too much opening runway before the market-facing explosion becomes undeniable

Executed result:

- same-day bounded repair was completed on `B08`
- the public signboard now lands by `B08` instead of waiting until `B09`
- opening pacing triage exits `YELLOW`

Sources:

- [current-yellow-salvageability-split.md](/C:/Users/wjjo/Desktop/글도비/docs/2026-04-10/current-yellow-salvageability-split.md)
- [terminal_04_chaebol_ent_empire_deployable_greenplus_audit.md](/C:/Users/wjjo/Desktop/글도비/docs/2026-04-10/terminal_04_chaebol_ent_empire_deployable_greenplus_audit.md)
- [03_chaebol_ent_empire_tr_block_070_draft.json](/C:/Users/wjjo/Desktop/글도비/treatments/03_chaebol_ent_empire_tr_block_070_draft.json)
- [03_chaebol_ent_empire.yaml](/C:/Users/wjjo/Desktop/글도비/work_guards/03_chaebol_ent_empire.yaml)

---

## 2. Repair Surface

Recommended surface:

- `TR only`

Do not start with:

- `BI`
- `work_guard`
- broad `Phase0` rebuild

Why:

- operator reading already says the growth spine survives through `B21~B70`
- the cheapest fix is still `ARC-01` compression and earlier signboard placement

---

## 3. Bounded Edit Window

Primary edit window:

- `B04~B10`

Secondary check window:

- `B01~B03`
- `B11~B12`

What to do:

- compress setup/recovery beats inside `B04~B07`
- move one public, market-facing signboard beat earlier
- make representative reevaluation count as public consequence, not just internal admiration
- ensure the next battlefield ticket is legible before the opening feels spent

Executed edit:

- `B08` reward and power-shift surface were tightened so the casting result reads as a true public signboard and public reevaluation event
- no broad rewrite outside the opening window was used

What not to do:

- do not rewrite `B21~B70`
- do not flatten the pyrrhic or cost-bearing shape of the opening
- do not turn it into instant effortless domination

---

## 4. Preserve

Must preserve:

- `B01~B03` opening proof engine
- the pair's growth spine after `B21`
- existing battlefield rotation in the middle and late run
- the work's core tone: ambitious, costly, still market-facing

---

## 5. Success Condition

This repair is successful only if:

- opening signboard no longer reads as `B09`-late
- opening pacing triage can clear `YELLOW`
- the pair no longer needs `repair-first` note just for opening compression
- no new whole-run drag is introduced while fixing the opening

Observed result:

- opening signboard no longer reads as `B09`-late in the current triage pass
- current operator reading moved this pair out of the active opening `YELLOW` shelf
- deployable `GREENPLUS` still requires a separate manual closeout because the present opening authority remains `legacy_heuristic`

---

## 6. Operator Note

This was the cleanest current repair job and is now completed.

Shortest reading:

- do not rebuild the work
- treat this spec as completed repair archive, not active queue instruction
- for the actual execution outcome, see [chaebol_ent_empire_opening_signboard_compression_repair_note.md](/C:/Users/wjjo/Desktop/글도비/docs/2026-04-10/chaebol_ent_empire_opening_signboard_compression_repair_note.md)
