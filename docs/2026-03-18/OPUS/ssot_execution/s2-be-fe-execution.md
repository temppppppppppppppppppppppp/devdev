# S2 BE-FE 연결성 실행문서

> 최종 갱신: 2026-03-18
> 원본 SSOT: `docs/2026-03-18/OPUS/ssot/s2-be-fe-connectivity.md`
> 감리 이력: 3PASS 기본 감리 + 5PASS 적대적 감리

---

## 1. 개관

S2 SSOT에서 도출된 실행 가능 항목을 구체적 단계, 우선순위, 완료 기준, 추정 공수, 의존성과 함께 정리한다.

**S2 건강도 요약**: 28/29 Active (96.6%), 불일치 0건, 경고 W1~W8 (중 1건, 저-중 1건, 저 6건)

---

## 2. 실행 항목 테이블

| ID | 항목 | 우선순위 | 상세 | 완료 기준 | 추정 공수 | 의존성 | S2 근거 |
|----|------|---------|------|----------|----------|--------|---------|
| E01 | **채널 이중 정의 단일화** | P1 (중) | `preload.js` 로컬 `PRELOAD_METHOD_CHANNELS`를 `desktop_control_plane_contract.js`에서 import하는 구조로 전환. 현재 sandboxed preload에서 require 불안정으로 로컬 복사본 유지 중이나, Electron 40.x의 `sandbox: true` + `contextBridge` 환경에서 안정적 import 경로 확보 필요. 불가능 시 빌드 타임 코드 생성(codegen)으로 단일 소스 보장. | (1) `preload.js`에 로컬 채널 상수가 없거나, 빌드 스크립트가 단일 소스에서 생성 (2) `test_desktop_contract_refresh.py` 통과 유지 (3) 한쪽만 수정 시 빌드/테스트 실패로 즉시 감지됨 | 4h | 없음 | W1: `preload.js:3` 주석, `desktop_control_plane_contract.js` |
| E02 | **CLI_CONTRACT 동적 로딩** | P3 (저) | `main.js:117-133` 하드코딩된 10개 장르 인덱스 매핑을 `config/genres/` 디렉토리에서 동적 로딩하도록 변경. 또는 최소한 빌드 타임에 `config/genres/` 내용과 `genreIndexMap` 일치 여부를 검증하는 테스트 추가. | (1) `config/genres/` 변경 시 `CLI_CONTRACT.genreIndexMap` 자동 반영 또는 (2) 불일치 시 테스트 실패 감지 | 2h | 없음 | W2: `main.js:121-132` genreIndexMap |
| E03 | **Settings 이중 손상 복구 강화** | P3 (저) | `main.js:636` loadSettings에서 JSON 파싱 실패 시 `.bak` 백업 후 null 반환. 이중 손상(원본+백업 모두 손상) 시 기본 설정 팩토리 리셋 로직 추가. 사용자에게 "설정 초기화됨" 알림 제공. | (1) `.bak`도 손상된 경우 기본 설정 객체 반환 (2) Renderer에서 설정 초기화 안내 표시 (3) 단위 테스트: 이중 손상 시나리오 커버 | 2h | 없음 | W3: `main.js:636` loadSettings |
| E04 | **Backend 재시작 한도 초과 사용자 안내** | P2 (저-중) | `MAX_BACKEND_RESTARTS=2` (`main.js:237`) 초과 시 현재 콘솔 경고만 출력(`main.js:321`). Renderer에 명시적 오류 다이얼로그 표시 + 재시작 버튼 제공. | (1) 3회 연속 실패 시 사용자에게 다이얼로그 표시 (2) 다이얼로그에서 "재시작 시도" / "종료" 선택 가능 (3) 이벤트 로그에 실패 기록 | 3h | 없음 | W4: `main.js:237, 321` |
| E05 | **Silent .catch 로깅 추가 (8건)** | P2 (저-중) | `index.html` 내 8건의 `.catch(() => {})` 패턴에 최소한 `console.warn` 또는 상태 표시줄 알림 추가. 특히 `resolvePrompt` 자동 응답 실패(`index.html:6376`)는 Mode B 흐름 중단 원인이므로 우선 처리. | (1) 8건 모두 `.catch(err => console.warn(...))` 이상으로 변경 (2) `resolvePrompt` 실패 시 프롬프트 UI에 재시도 안내 표시 (3) grep `.catch(() => {})` 결과 0건 | 3h | 없음 | W5: `index.html:5983, 6222, 6253, 6376, 7686, 7800, 7805, 7961` |
| E06 | **WebSocket onerror 로깅 추가** | P3 (저) | `index.html:6221`의 `_ws.onerror = () => {}` 빈 핸들러에 에러 원인 로깅 추가. `console.error` + 상태 표시줄에 연결 오류 표시. | (1) `_ws.onerror` 핸들러에서 `console.error("WS error:", event)` 호출 (2) 상태 표시줄에 "연결 오류" 표시 후 자동 재연결 대기 안내 | 1h | 없음 | W6: `index.html:6221` |
| E07 | **CORS 미들웨어 예비 구현** | P4 (저/미래) | 현재 `127.0.0.1:8300` 로컬 전용이므로 긴급하지 않으나, 원격 배포 전환 시 대비하여 FastAPI `CORSMiddleware` 설정 코드를 환경변수 기반 조건부 활성화로 사전 준비. FastAPI 앱 인스턴스: `bridge_server.py:1831` (`app = FastAPI(...)` 행). | (1) `GEULDOBI_CORS_ORIGINS` 환경변수 설정 시 CORS 활성화 (2) 환경변수 미설정 시 기존 동작 유지 (3) 단위 테스트: CORS 헤더 검증 | 2h | 없음 | W7: `bridge_server.py:1831` |
| E08 | **세션/인증 레이어 설계 문서화** | P4 (저/미래) | 현재 Electron Main Process가 유일한 HTTP 클라이언트이므로 인증 불필요. 원격 배포 시나리오 대비 인증 레이어 설계 문서만 작성. 토큰 기반 인증 + 세션 관리 방안. | (1) 인증 설계 문서 `docs/implementation/auth-design-v1.md` 작성 완료 (토큰 발행/검증/세션 만료/CORS 연계 최소 4개 섹션 포함) (2) 구현 착수는 원격 배포 결정 시점까지 보류 | 2h (문서만) | E07 선행 권장 | W8: `bridge_server.py` |
| E09 | **Dead-candidate IPC 정리** | P3 (저) | `preload.js:32-34`의 `getWorkspacePath` (dead-candidate). `must_not_be_treated_as_live: true` 계약이 있으나, 코드에서 완전 제거하거나 명시적 deprecation 주석 + 향후 활용 계획 문서화. | (1) dead-candidate 코드 제거 또는 (2) 활용 계획이 있다면 live 전환 로드맵 명시 (3) `desktop-ipc-surface-contract-v1.json` 동기 갱신 | 1h | E01 이후 권장 | S2 §2.2.1 DC행, `preload.js:32-34` |
| E10 | **resolvePrompt 실패 시 Mode B 복구 흐름** | P1 (중) | W5에서 파생. `resolvePrompt` IPC 호출 실패 시 사일런트 무시되면 Engine이 타임아웃까지 대기 후 default 값 적용. 사용자 의도와 다른 default가 적용될 수 있음. 실패 시 재시도 UI + 사용자 경고 필요. | (1) `resolvePrompt` 실패 시 프롬프트 UI에 "전송 실패, 재시도" 버튼 표시 (2) 3회 재시도 실패 시 사용자에게 "수동 입력 불가" 명시 안내 (3) Engine 측 타임아웃 default 적용 시 `prompt_timeout` 이벤트로 사용자 인지 | 4h | E05 선행 | W5 파생: `index.html:6376`, `prompt_broker.py` 타임아웃 |
| E11 | **ErrorEnvelope 13코드 Renderer 측 분류 처리** | P2 (저-중) | S2에서 13개 ErrorEnvelope 코드 + 2개 Transport 코드를 문서화했으나, Renderer 측에서 코드별 분기 처리가 체계적이지 않음. 코드별 사용자 메시지 매핑 테이블을 Renderer에 구현. 대상 파일: index.html 내 bridgeFetch 응답 처리 영역 또는 신규 errorMessages.js. | (1) 13개 backend 코드 + 2개 transport 코드에 대한 사용자 메시지 매핑 객체 존재 (2) 미매핑 코드 수신 시 일반 오류 메시지 fallback (3) `RUN_ALREADY_ACTIVE`, `RISK_APPROVAL_*` 등 주요 코드에 적절한 한국어 안내 | 3h | 없음 | S2 §4.3 ErrorEnvelope 13코드 |
| E12 | **Splash 폴링 실패 30회 초과 사용자 안내** | P3 (저) | `splash.js:6` `MAX_POLL_FAILS=30` 초과 시 현재 동작 불명확. 명시적 "Backend 시작 실패" 안내 + 재시작/종료 선택 UI 필요. | (1) 30회 초과 시 스플래시 화면에 오류 메시지 표시 (2) "재시작" / "종료" 버튼 제공 (3) fallback 타임아웃(`SPLASH_FALLBACK_MS=8000`)과 조화 | 2h | 없음 | S2 §2.1.3: `splash.js:6`, `main.js:106` |

---

## 3. 실행 우선순위 매트릭스

### P1 (중) — 즉시 실행

| ID | 항목 | 근거 |
|----|------|------|
| E01 | 채널 이중 정의 단일화 | 침묵 실패 위험. 현재 테스트가 커버하나 구조적 해소 필요 |
| E10 | resolvePrompt 실패 시 Mode B 복구 흐름 | Mode B 실행 흐름에서 사일런트 오류 → 사용자 의도 무시 위험 |

### P2 (저-중) — 단기 실행

| ID | 항목 | 근거 |
|----|------|------|
| E04 | Backend 재시작 한도 초과 사용자 안내 | 3회 실패 시 사용자 무응답 상태 |
| E05 | Silent .catch 로깅 추가 (8건) | 디버깅 불가 영역 축소 |
| E11 | ErrorEnvelope Renderer 분류 처리 | 사용자 경험 일관성 |

### P3 (저) — 중기 실행

| ID | 항목 | 근거 |
|----|------|------|
| E02 | CLI_CONTRACT 동적 로딩 | 장르 추가 시 수동 동기화 비용 |
| E03 | Settings 이중 손상 복구 강화 | 극단적 실패 시나리오 |
| E06 | WebSocket onerror 로깅 | 진단 정보 부재 |
| E09 | Dead-candidate IPC 정리 | 코드 위생 |
| E12 | Splash 폴링 실패 안내 | 시작 실패 시 사용자 경험 |

### P4 (저/미래) — 원격 배포 전환 시

| ID | 항목 | 근거 |
|----|------|------|
| E07 | CORS 미들웨어 예비 구현 | 현재 로컬 전용, 원격 전환 시 필수 |
| E08 | 세션/인증 레이어 설계 | 원격 전환 시 필수 |

---

## 4. 총 추정 공수

| 우선순위 | 항목 수 | 추정 합계 |
|---------|---------|----------|
| P1 (중) | 2 | 8h |
| P2 (저-중) | 3 | 9h |
| P3 (저) | 5 | 9h |
| P4 (저/미래) | 2 | 4h |
| **합계** | **12** | **30h** |

---

## 5. 의존성 그래프

```
E01 (채널 단일화)
  └─> E09 (dead-candidate 정리) — E01 이후 권장

E05 (silent .catch 로깅)
  └─> E10 (resolvePrompt 복구) — E05 선행 권장

E07 (CORS)
  └─> E08 (인증 설계) — E07 선행 권장

E02, E03, E04, E06, E11, E12 — 독립 실행 가능
```

---

## [부록 A] 감리 이력

### 기본 3PASS 감리

#### PASS 1: 항목 완전성

| 검증 항목 | 판정 | 비고 |
|----------|------|------|
| W1~W8 전수 반영 여부 | **완전** | E01~E08이 W1~W8에 1:1 대응 |
| 추가 도출 항목 존재 여부 | **완전** | E09(dead-candidate), E10(resolvePrompt 복구), E11(ErrorEnvelope 분류), E12(Splash 실패 안내) 4건 추가 도출 |
| S2 SSOT 수치와 실행문서 수치 일치 | **일치** | IPC 25+1, 엔드포인트 9, 이벤트 8, ErrorEnvelope 13, 건강도 28/29 |

**PASS 1 결과**: 누락 항목 **0건**.

#### PASS 2: 실행 가능성

| 검증 항목 | 판정 | 비고 |
|----------|------|------|
| 각 항목에 구체적 완료 기준 존재 | **충족** | 12건 모두 측정 가능한 기준 보유 |
| 추정 공수의 합리성 | **합리적** | 단일 파일 수정 1~4h, 설계 문서 2h — Electron/FastAPI 규모 대비 적정 |
| 의존성 순환 없음 | **확인** | DAG 구조, 순환 없음 |
| 우선순위 근거 명확 | **충족** | 사용자 영향도 × 발생 가능성 기준 |

**PASS 2 결과**: 실행 불가 항목 **0건**.

#### PASS 3: S2 근거 추적

| 검증 항목 | 판정 | 비고 |
|----------|------|------|
| 각 항목의 S2 근거 열 존재 | **충족** | 12건 모두 SSOT 섹션/행 번호 명시 |
| 근거와 항목 간 논리적 연결 | **충족** | W1→E01, W2→E02, ... W8→E08 직접 대응. E09~E12는 SSOT 발견사항에서 파생 |
| 근거 없는 항목 | **0건** | 전수 확인 |

**PASS 3 결과**: 근거 없는 항목 **0건**.

---

### 적대적 5PASS 감리

#### ADV-PASS 1: 누락 공격 — "빠진 것이 있는가?"

| 공격 벡터 | 결과 | 판정 |
|----------|------|------|
| S2 §6.1 강점(S1~S8)에서 유지보수 항목 누락? | S1~S8은 현재 잘 작동하는 강점이므로 실행 항목이 아님. 단, S6(이벤트 스키마 조건부 검증)의 스키마 버전 관리 고려 가능하나 현재 v1 단일 버전이므로 시기상조 | **누락 없음** |
| S2 §4.2 회귀 테스트 6파일의 커버리지 갭? | 6개 테스트가 계약-코드 정합성을 커버. 실행 항목 E01 완료 시 테스트 갱신이 자연 수반됨 | **누락 없음** |
| S2 §3.4 설정/자재 흐름에서 누락된 엣지 케이스? | E03(이중 손상)이 커버. Material import 시 파일 크기 제한 미언급이나 S2 경고 범위 밖 | **범위 외** |
| bridgeFetch 5초 타임아웃(main.js:108)과 Splash 5초 개별 fetch 타임아웃(splash.js:17) 관계? | 독립적이므로 충돌 없음. 실행 항목 불필요 | **누락 없음** |

**ADV-PASS 1 결과**: 누락 **0건**.

#### ADV-PASS 2: 과잉 공격 — "불필요한 항목이 있는가?"

| 공격 벡터 | 결과 | 판정 |
|----------|------|------|
| E07(CORS)과 E08(인증)은 현재 불필요한 미래 작업? | P4로 분류하여 "원격 배포 전환 시"로 명시. 실행 보류 가능하므로 과잉이 아닌 사전 준비 문서화 | **적정** |
| E09(dead-candidate 정리)는 기능에 영향 없는 코드 위생? | P3으로 분류. 1h 소요로 비용 대비 코드 명확성 이득. 제거 판단은 팀 재량 | **적정** |
| E12(Splash 폴링 실패)는 발생 빈도가 극히 낮은 시나리오? | 맞지만 발생 시 사용자가 무한 대기하는 UX 문제. P3으로 적정 분류 | **적정** |

**ADV-PASS 2 결과**: 과잉 항목 **0건**.

#### ADV-PASS 3: 우선순위 공격 — "순서가 잘못되었는가?"

| 공격 벡터 | 결과 | 판정 |
|----------|------|------|
| E10(resolvePrompt 복구)이 E01(채널 단일화)보다 더 긴급? | E10은 실제 사용자 데이터 흐름에 영향, E01은 개발자 실수 방지. 둘 다 P1이 적정. E10을 E01보다 먼저 착수해도 무방 | **적정 (동일 우선순위 내 순서 유연)** |
| E04(재시작 한도)가 P2인데 E05(silent .catch)도 P2? | E05는 8건의 디버깅 사각지대, E04는 1건의 사용자 경험 문제. 둘 다 P2가 적정. E05 우선 착수 권장 (E10 선행 의존) | **적정** |
| E11(ErrorEnvelope 분류)이 P2인데 P3이 더 적절? | 13개 코드 중 `RUN_ALREADY_ACTIVE`, `RISK_APPROVAL_*` 등 사용자 빈출 코드의 안내 부재는 UX 영향. P2 유지 적정 | **적정** |

**ADV-PASS 3 결과**: 우선순위 변경 **0건**.

#### ADV-PASS 4: 완료 기준 공격 — "기준이 모호하거나 검증 불가능한가?"

| 공격 벡터 | 결과 | 판정 |
|----------|------|------|
| E01 "빌드/테스트 실패로 즉시 감지됨" — 어떤 테스트? | `test_desktop_contract_refresh.py`가 명시되어 있음. 충분히 구체적 | **구체적** |
| E05 "grep .catch(() => {}) 결과 0건" — 정규식 정확성? | `.catch(() => {})` 패턴은 grep 가능. 다만 `.catch(()=>{})` (공백 없는) 변형도 검색해야 함 | **정정 필요** |
| E08 "설계 문서 작성 완료" — 문서 위치/형식 미지정? | 문서 경로를 명시해야 함 | **정정 필요** |
| E10 "3회 재시도" — 재시도 간격 미지정? | 재시도 간격(예: 1초)을 명시해야 함 | **정정 필요** |

**ADV-PASS 4 결과**: 정정 필요 **3건** → 아래 정정 반영.

#### ADV-PASS 5: 추정 공수 공격 — "과소/과대 추정이 있는가?"

| 공격 벡터 | 결과 | 판정 |
|----------|------|------|
| E01(채널 단일화) 4h — Electron sandbox require 문제 해결 포함? | sandbox 환경에서 require 경로 해결은 복잡할 수 있으나, codegen 대안 경로 존재. 4h는 codegen 경로 기준으로 적정. require 경로 시 6h 가능 | **적정 (codegen 기준)** |
| E10(resolvePrompt 복구) 4h — UI 변경 + 재시도 로직 + 테스트? | Renderer UI 변경(프롬프트 카드에 재시도 버튼) + IPC 재시도 로직. 4h는 빠듯할 수 있으나, 기존 프롬프트 UI 구조 활용 시 가능 | **적정 (하한)** |
| E11(ErrorEnvelope 분류) 3h — 13+2 코드 메시지 매핑? | 매핑 객체 작성 + Renderer 분기 로직. 3h 적정 | **적정** |

**ADV-PASS 5 결과**: 공수 변경 **0건**.

---

### 적대적 감리 정정 반영 (ADV-PASS 4 지적 3건)

| 정정 ID | 항목 | 원본 | 정정 |
|---------|------|------|------|
| ADV-C1 | E05 완료 기준 | `grep .catch(() => {})` 결과 0건 | `grep -E "\.catch\(\s*\(\)\s*=>\s*\{\s*\}\s*\)"` 및 `grep -E "\.catch\(\(\)=>\{\}\)"` 양쪽 패턴 모두 0건 |
| ADV-C2 | E08 완료 기준 | "설계 문서 작성 완료" | "설계 문서 `docs/implementation/auth-design-v1.md` 작성 완료. 토큰 발행/검증/세션 만료/CORS 연계 최소 4개 섹션 포함" (출력 경로 반영 완료) |
| ADV-C3 | E10 완료 기준 | "3회 재시도 실패 시" | "3회 재시도(간격 1초) 실패 시. 재시도 중 스피너 표시. 3회 초과 시 프롬프트 카드에 '전송 불가' 상태 고정 + 수동 새로고침 안내" |

---

### 최종 판정

| 감리 | 결과 |
|------|------|
| 기본 3PASS | 누락 0건, 실행 불가 0건, 근거 없는 항목 0건 |
| 적대적 5PASS | 누락 0건, 과잉 0건, 우선순위 변경 0건, 완료 기준 정정 3건(반영 완료), 공수 변경 0건 |
| **종합** | **실행문서 유효. 정정 3건 반영 완료.** |
