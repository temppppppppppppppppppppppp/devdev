# Empire Youngest OPUS Context Memo

Date: 2026-03-27
Purpose: minimal handoff memo for OPUS
Target: `empire_youngest_allsector`

## 1. Current Truth

- family: `blockguide`
- mode: existing `TR + BI` pair re-audit
- canonical pair is still in `_quarantine`
- this is not fresh Planning / fresh TR / fresh BI work
- this is not a promotion run
- next unit is narrowed to `truth-reconciliation re-audit`

## 2. Canonical Pair Paths

- TR: `treatments/_quarantine/empire_youngest_allsector_tr_block_070_draft.json`
- BI: `bible/_quarantine/0_bi_empire_youngest_allsector.json`

## 3. Live Facts OPUS Should Start From

These are starting assumptions, not final truth. Confirm them by direct read.

1. live TR currently contains `70` blocks
2. live BI roadmap currently contains `70` entries
3. live status says:
   - `run_class = sequential_production`
   - `last_sequential_block_pass = 70`
   - `next_unit_type = bi_handoff`
   - `manual_audit_ready = true`
4. Stage 0 snapshot says:
   - `manual_audit_pass = true`
5. BI has nontrivial internal structure:
   - `NPC_Timeline`
   - `HistoricalEvents`
   - `OpponentTransitionPlan`

## 4. Why This Run Exists

There is a source-authority conflict.

Older survey:

- `docs/2026-03-26/blockguide-quarantine-static-quality-survey.md`

Key conflicting claims in that survey:

- `TR actual 43`
- `TR only 43/70 complete`
- `27 new blocks required`

Current live pair appears to contradict those count claims.

So this run is not:

- `just trust the old survey`
- `just assume promotion`
- `just regenerate 27 blocks`

This run is:

- prove current truth first

## 5. Immediate Task

Execute exactly one bounded `truth-reconciliation re-audit`.

Re-audit questions:

- what is the live pair truth right now
- which old claims are stale
- where is the real weak band, if any
- is the next honest unit `revival-stage probe`, `fresh TR static audit`, or `weakness report only`

## 6. Fixed Creative Anchors

Preserve:

- 2045 -> 2025 regression frame
- `세 개씩. 쉬지 않고.` doctrine
- independent-capital rule
- all-sector rolling expansion
- family-collapse memory as long-game engine
- low-affect protagonist with delayed emotional fracture

Watch weakness:

- mid-band compression around `Block 32-43`
- patterned `타자 POV`
- sector timing knowledge outrunning actual domain-scene specificity

## 7. Do Not Do In This Run

- no active promotion
- no revival-stage probe
- no TR regeneration
- no BI redesign
- no code edits
- no broad document sprawl

## 8. Main Order Doc

Read and follow:

- `docs/2026-03-27/opus-empire-youngest-truth-reaudit-order.md`

This order already fixes:

- read order
- stop conditions
- deliverable path
- handoff format

## 9. Expected Deliverable

- `docs/2026-03-27/empire-youngest-truth-reaudit-report.md`

## 10. Suggested One-Line OPUS Prompt

```text
`docs/2026-03-27/opus-empire-youngest-truth-reaudit-order.md`와 `docs/2026-03-27/empire-youngest-opus-context-memo.md`를 UTF-8로 읽고, `empire_youngest_allsector`에 대해 `truth-reconciliation re-audit` 1단위만 수행하라.
```

## 11. 3-Pass Note

- pass1: target / pair / non-goals fixed
- pass2: live facts and stale-authority conflict reduced to only what OPUS must know
- pass3: deliverable path and next-unit choices fixed to avoid scope drift

Confidence:
- 97% this memo is sufficient for low-overhead OPUS handoff
