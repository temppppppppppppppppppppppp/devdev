## Tranche 30 - Block 36

- work: `golden_canary_deepclone_probe_a_fullblock_v1`
- scope: `Block 36`
- concept: `circuit-breaker harvest / floor-switch authority`

이번 tranche에서는 `서킷브레이커` 블럭을 단순 `헷지 익절 12조와 저점 매수` 장면으로 두지 않고, `팬데믹 붕괴에서 수확을 끝내고 새 레짐으로 전환하는 권한`이 receipt로 남는 블럭으로 끌어올렸다.

이번 반영의 초점은 아래와 같다.

- `event_villain`은 폭락 자체보다 `폭락 속에서 switch timing을 잃게 만드는 순수 패닉`으로 재정의했다.
- `solution`은 단순 익절 후 매수가 아니라, `panic harvest와 floor switch를 한 호흡에 묶는 전환 행동`으로 올렸다.
- `reward`는 `헷지 익절 12조`보다 `circuit-breaker harvest와 floor-switch authority 확보`가 먼저 읽히도록 승격했다.
- `stakes`는 수익 규모보다 `하루만 빨라도 늦어도 틀어지는 바닥 전환 타이밍`으로 맞췄다.
- `relationship_delta`에는 마이클이 단순 딜러가 아니라 `전환 타이밍을 목격한 증인`으로 이동하는 변화가 들어갔다.
- `callback`에는 Block 35의 `hedge-validation receipt`가 실제 harvest로 회수된다는 연결을 명시했다.

이 tranche의 목적은 `폭락에서 벌었다`보다 `폭락의 끝을 선언하고 rebound book으로 넘어가는 사람`을 receipt로 남기는 것이다. 다음 `Block 37`의 무제한 QE 구간에서 이 블럭은 반등이 우연히 온 것이 아니라, 이미 floor switch 권한 위에서 이어지는 레짐 전환이라는 carry를 유지하는 기반이 된다.
