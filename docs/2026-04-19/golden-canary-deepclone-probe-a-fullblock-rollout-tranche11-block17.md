## Tranche 11 - Block 17

- 대상: `golden_canary_deepclone_probe_a_fullblock_v1`
- 범위: `Block 17`
- 성격: `historic bottom entry / conviction witness`

이번 tranche에서는 `666`을 단순 역사적 저점 매수 블럭으로 두지 않고, `준비된 진입권을 실제 역사적 저점에 꽂고 그 장면을 witness가 목격하는 블럭`으로 올렸다.

핵심 반영점은 아래와 같다.

- `event_villain`을 공포장 자체보다 `너무 정확한 진입이 이후 감시를 부를 수 있는 문제`로 재정의했다.
- `solution`은 막연한 올인이 아니라 `bottom-entry warrant packet`의 집행으로 바꿔, 국내/미국 execution lane을 나눠 clean entry가 보이게 했다.
- `reward`는 단순 `체결 완료`가 아니라 `historic bottom entry account`와 `conviction witness pair` receipt로 승격했다.
- `relationship_delta`는 박성호와 마이클을 각각 `국내 witness`와 `글로벌 witness`로 재배치했다.
- `future_prep`은 `Block 18`의 골드만 scrutiny가 그냥 뜬금없는 외부 압박이 아니라, 여기서 남은 witness와 완벽한 타이밍의 후과가 되게 맞췄다.

이 tranche의 목적은 `바닥을 맞혔다`를 넘어서 `바닥 entry가 실제 계정과 witness에 남아 이후 계약과 압박을 낳는 구조`를 만드는 데 있다. 다음 블럭은 이 perfect entry가 부른 감시와 족쇄를 다루는 `Block 18`이다.
