1. scope
- Fact: This artifact covers terminal `T04` only: CW input quality, context layering, prompt structure, and truncation.
- Fact: Claims were grounded in live code and live runtime artifacts, with governing authority limited to `AGENTS.md`, `docs/2026-03-17/geuldobi-v2-quality-maximization-roadmap-triage.md`, `docs/2026-03-17/geuldobi-v2-quality-maximization-full-survey-audit-order.md`, and `docs/2026-03-17/geuldobi-v2-quality-maximization-terminal-prompt-pack.md`.
- Fact: `docs/roadmap-v2.md` was treated as a non-authoritative seed only. No code edits, execution SSOT creation, or roadmap creation were performed.
- Inference: The main T04 risk is not the absence of a Stage 4 budget system; it is the coexistence of tiered budgeting with many CW-relevant literal truncation caps that can still hide upstream intent or relation detail.
- Open Question: None on lane definition.

2. included paths
- Fact: `modules/core/stage4_context_builder.py`
- Fact: `modules/core/context_advisor.py`
- Fact: `modules/domain/agents/chief_writer.py`
- Fact: `modules/domain/agents/chief_writer_context.py`
- Fact: `modules/domain/agents/chief_writer_prompts.py`
- Fact: `modules/domain/agents/base_agent.py`
- Fact: `modules/core/quality_dashboard.py`
- Fact: `modules/core/constants.py`
- Fact: `config/settings/validation.yaml`
- Fact: `config/prompts/chief_writer.yaml`
- Fact: `tests/test_stage4_context_builder.py`
- Fact: `tests/test_sc6_observability.py`
- Fact: `tests/test_chief_writer.py`
- Fact: `tests/test_chief_writer_context.py`
- Fact: `tests/test_writer_prompt_builders.py`
- Fact: `projects/0_260316/logs/quality_metrics.jsonl`
- Fact: `projects/0_260316/logs/pass_rate_monitor.json`
- Fact: `projects/test_project/logs/episode_production.jsonl`

3. excluded paths
- Fact: `modules/core/stage4_interview_round.py`, validator semantics, PASS_WITH_FIX handling, and retry taxonomy were excluded as T05 ownership.
- Fact: `modules/core/stage4_post_processor.py`, artifact durability, metadata truth, and manuscript persistence were excluded as T06 ownership except where a T04 prompt/logging dependency required a handoff note.
- Fact: `UI/`, `geuldobi-desktop/`, `modules/api/bridge_server.py`, and operator-surface interpretation were excluded as T07 ownership except where a handoff note was needed.
- Fact: `tests/` outside direct CW/context/prompt observability coverage were not deep-audited; tooling economics and smoke harnesses remain T08 ownership.
- Fact: cross-repo threshold ownership and long-context policy arbitration were not expanded beyond T04 touchpoints; that remains T09 ownership.
- Fact: reference-only execution SSOT docs and `docs/roadmap-v2.md` were not used as authority for conclusions.
- Open Question: None on exclusions.

4. facts
- Fact: `modules/core/stage4_context_builder.py` composes Stage 4 mandatory context as Tier 0, Tier 1, and Tier 2, trims Tier 2 first against `context.mandatory_context_max`, then trims Tier 1 only after Tier 2 rebalance, and records `_stage4_context_budget_meta` with tier and total char counts.
- Fact: The Stage 4 builder computes `_work_focus`, injects a work-slot summary into Tier 1 when present, and passes `work_focus=_work_focus` into `context_advisor.plan_stage4_retrieval(...)`.
- Fact: `modules/core/stage4_context_builder.py` builds a work-identity slot summary with `max_chars=1800`, asks `SemanticQueryBroker.build_stage4_relation_slice(..., max_chars=560)`, and emits a local trim marker when the slot summary exceeds the cap.
- Fact: `modules/core/context_advisor.py` still contains multiple literal selection caps inside T04-owned retrieval shaping, including `scene_breakdown[:3]`, scene value slices `[:52]`, final scene query `[:260]`, relationship change slice `[:4]`, tactical slices `[:260]` and `[:320]`, suspension term slices `[:30]`, and NPC roster caps `[:6]`.
- Fact: `modules/core/stage4_context_builder.py` still contains additional literal truncation points in T04 scope, including manuscript excerpt retrieval capped at `max_chars=3000` with per-episode excerpt `content[:800]`, plus legacy fallback query slices `_ep_tac[:1800]` and `arc_tactical[:1800]`.
- Fact: Repo-level thresholds exist for long-context behavior in `config/settings/validation.yaml` and `modules/core/constants.py`, including `context.max_context_chars=1000000`, `context.mandatory_context_max=400000`, `smart_retrieval.stage4_total_budget=300000`, `smart_retrieval.slot_max_chars_default=3000`, `session_logging.max_prompt_chars=200000`, `context.canonical_facts_budget=13000`, `context.timeline_budget=3000`, and `smart_truncate(...)`.
- Fact: `modules/domain/agents/chief_writer_context.py` assembles `writer_core_section` in this order: character voice, world-state section, writing-directive section, reference-anchor prompt, mandatory context, anti-trope prompt, justification prompt, and reflexion prompt.
- Fact: `modules/domain/agents/chief_writer_prompts.py` places `writer_core_section` before Stage 1 blueprint analysis and before the later continuity, HUD, arc tactical, style, and previous-manuscript sections in the final Chief Writer main prompt.
- Fact: `modules/domain/agents/chief_writer_context.py` slices `prev_manuscript[-2500:]` into `prev_ending`, while `prev_manuscripts_text` is passed through `smart_truncate(...)`; `modules/domain/agents/chief_writer.py` also truncates original manuscript text to `150000` chars with `head_chars=20000` in patch/fix-loop paths.
- Fact: `modules/core/stage4_context_builder.py` records Stage 4 retrieval observations with fields such as `work_focus_present`, `tracking_slots_count`, `planned_slots_count`, `source_counts`, `coverage_warnings`, `mandatory_context_chars`, `work_slot_summary_included`, `relation_slice_included`, and `trimmed_work_slot_summary`.
- Fact: `modules/core/quality_dashboard.py` persists those observations to `quality_metrics.jsonl`, but the persisted schema does not include Pack A minimum fields for explicit pre-trim size, post-trim size by section, dropped item count, or full survival summary by field.
- Fact: The same persisted schema does not satisfy Pack C minimum lineage fields for whether key Arc/Blueprint intent fields were present at Stage 2, survived Stage 3, reached Stage 4 prompt inputs, and where a drop occurred if lost.
- Fact: Live runtime evidence exists in `projects/0_260316/logs/quality_metrics.jsonl`. Sample Stage 4 rows at `2026-03-16T12:33:46.598681` and `2026-03-16T12:55:47.242631` show `planned_slots_count=8`, `advisor_path_used=true`, `source_counts` including `manuscript_db` and `db_npc_relationship`, `mandatory_context_chars=8006` and `10086`, but `relation_slice_included=false` with `coverage_warnings=["missing_relation_slice"]`.
- Fact: Those sampled Stage 4 rows also show `work_focus_present=false` and `work_slot_summary_included=false`, so the current live log sample does not prove T04 work-focus continuity in production runs even though the code path exists.
- Fact: `modules/domain/agents/base_agent.py` logs `wrapped_prompt` rather than the full CW `common_context` when `_ask_with_cached_context(...)` succeeds, because the cached path sends only the reduced wrapper plus `cached_content=cache_name` and persists the same reduced wrapper under `context_tag="cached_context"`.
- Fact: T04-targeted tests are strong around Stage 4 builder composition and coverage-warning behavior. `tests/test_stage4_context_builder.py` covers work-focus handoff, truth/retrieval/bulk ordering, headroom rebalance, relation-slice inclusion, and coverage-warning placement. `tests/test_sc6_observability.py` covers advisor wiring and retrieval-plan observability seams.
- Fact: In the searched T04 tests, explicit assertions for full cached-context prompt reconstructability or exact high-level `writer_core_section` ordering inside persisted CW prompt logs were not found.
- Inference: Theme `절삭 하드코딩 전수조사` remains live inside T04 even after the newer long-context thresholds, because several semantically important CW input slices still bypass shared config and are embedded as literal numbers in builder/advisor code.
- Inference: Theme `테마 N` remains live because the repo now has a deliberate prompt skeleton, but the actual information hierarchy is split across `writer_core_section`, later step blocks, and cache-mode prompt wrapping, which makes prompt-structure quality harder to validate from logs.
- Inference: CW-side `테마 D` remains only partially observable. Code now forwards work-focus intent into Stage 4 planning, but runtime logging does not yet prove where an Arc/Blueprint intent field was dropped across Stage 2 to Stage 4.
- Open Question: Are the sampled `work_focus_present=false` rows older than the current handoff implementation, or do current upstream runs still fail to populate the work-focus payload?
- Open Question: Is there any durable store outside the audited T04 path that can reconstruct the full CW input when cached-context mode succeeds, or is that information effectively hidden after the call?

5. hotspot files
- Fact: `modules/core/stage4_context_builder.py` is the primary T04 hotspot because it owns work-focus handoff, tiered mandatory-context composition, coverage warnings, retrieval observation emission, and multiple literal truncation points.
- Fact: `modules/core/context_advisor.py` is a hotspot because it shapes Stage 4 retrieval queries with several literal caps that directly influence what survives into the CW prompt.
- Fact: `modules/domain/agents/chief_writer_context.py` is a hotspot because it defines the internal order of CW context blocks and applies additional history-window truncation before prompt handoff.
- Fact: `modules/domain/agents/chief_writer_prompts.py` is a hotspot because it fixes the macro ordering of the final CW prompt around `writer_core_section`.
- Fact: `modules/domain/agents/base_agent.py` is a hotspot because cached-context mode alters what becomes observable in durable prompt logs.
- Fact: `modules/core/quality_dashboard.py` is a hotspot because current T04 observability adequacy depends on what its retrieval-observation schema does and does not persist.

6. side-effect notes
- Fact: File write: applicable. `modules/core/quality_dashboard.py` writes `retrieval_observation` rows to `projects/*/logs/quality_metrics.jsonl`.
- Fact: DB write: applicable but limited inside T04. Stage 4 retrieval logic itself mainly reads DB, while `modules/domain/agents/base_agent.py` writes cached-context LLM call records through `_log_llm_call_to_db(...)`.
- Fact: JSONL/log/audit sink: applicable. Stage 4 retrieval observations and standard logger output are produced in this lane.
- Fact: Console/UI output: applicable. `modules/core/stage4_context_builder.py` emits non-fatal warnings through `self.ctx.ui.log(...)` during context-load and retrieval failures.
- Fact: Rollback/recovery/retry: partially applicable. `_ask_with_cached_context(...)` falls back to direct `ask()` on cached-path failure, but Stage 4 context composition itself does not implement a dedicated rollback flow.
- Fact: Cache/global state: applicable. `_stage4_context_budget_meta` mutates in-memory state, and cached-content execution changes what is durably visible about the true CW prompt.
- Fact: Bootstrap fallback: not applicable in this T04 audit lane.
- Fact: Config/env mutation: not applicable in this T04 audit lane.
- Fact: Artifact/manuscript/blueprint write: not applicable in this survey-only T04 lane.
- Inference: The most material side effect for T04 is observability distortion, not data corruption. Cached-context success and coarse retrieval logging can hide what the CW actually received even when generation succeeds.
- Open Question: None beyond follow-up handoffs.

7. contradictions or uncertainty
- Fact: The canonical terminal prompt pack rendered some mojibake in terminal output, but the underlying workspace files were readable through explicit file reads; no byte-level evidence was found that the live source files themselves are corrupted in the T04 paths inspected.
- Fact: The current worktree is dirty, including T04-owned files such as `modules/core/context_advisor.py`, `modules/core/stage4_context_builder.py`, and `modules/domain/agents/chief_writer.py`. Live workspace state was treated as authority, and no edits were made.
- Fact: `projects/test_project/logs/quality_metrics.jsonl` and `projects/test_project/logs/pass_rate_monitor.json` are absent, while `projects/test_project/logs/episode_production.jsonl` exists. T04 observability evidence is therefore stronger in `projects/0_260316` than in `projects/test_project`.
- Inference: Because the strongest live Stage 4 evidence came from `projects/0_260316`, the survey can confirm the current logging sink exists, but it cannot claim uniform observability coverage across all project runtimes.
- Inference: The combination of code support for work-focus handoff and sampled runtime rows with `work_focus_present=false` is a real uncertainty, not a resolved contradiction. The likely causes are upstream population gaps, older runtime artifacts, or both.
- Open Question: Which runtime dataset should T10 treat as the canonical live sample for T04 prompt/input observability: `projects/0_260316`, `projects/test_project`, or a fresh rerun owned by another terminal?
- Open Question: Are missing Pack A and Pack C fields intentionally omitted by contract, or are they a known observability debt that never received a schema owner?

8. candidate execution areas
- Fact: Candidate area 1: normalize remaining literal truncation caps in `modules/core/stage4_context_builder.py`, `modules/core/context_advisor.py`, and CW prompt surfaces so T04 information loss is contract-driven rather than scattered through local constants.
- Fact: Candidate area 2: extend Stage 4 retrieval observation logging to satisfy Pack A minimums with explicit source surface, destination surface, pre-trim size, post-trim size, dropped item count, and survival summary by section or field.
- Fact: Candidate area 3: add Pack C lineage logging that tracks whether key Arc/Blueprint intent fields were present at Stage 2, survived Stage 3, reached Stage 4 prompt input, and where any drop happened.
- Fact: Candidate area 4: improve cached-context observability so the full CW prompt can be reconstructed or durably referenced when cached execution succeeds.
- Fact: Candidate area 5: strengthen tests around macro prompt ordering, cached-context logging behavior, and section-level trim telemetry.
- Inference: Candidate areas 2 through 4 are higher leverage than prompt-copy tweaks because they determine whether future quality claims can be proven instead of inferred.
- Open Question: None on execution-area ranking inside T04.

9. follow-up requests for another terminal
- Fact: Request to `T03`: identify the concrete Arc/Blueprint intent fields that should survive into T04, and check whether current Stage 2 and Stage 3 live outputs actually populate the work-focus payload now expected by the Stage 4 planner.
- Fact: Request to `T06`: confirm where `_log_llm_call_to_db(...)` persists cached-context prompt metadata, whether the full CW input is reconstructable from DB plus cache metadata, and whether any durable artifact truth exists beyond the reduced wrapper prompt.
- Fact: Request to `T07`: verify whether any operator-visible surface exposes Stage 4 retrieval coverage warnings, work-slot-summary survival, or relation-slice loss in a way that a human operator can use during triage.
- Fact: Request to `T09`: audit ownership of the remaining literal truncation numbers in T04 surfaces and determine which should become shared threshold/config contracts versus local design choices.
- Inference: T04 does not currently need a T05 handoff unless later evidence shows Director-gate retries are compensating for CW input loss rather than independent semantic issues.
- Open Question: None beyond the listed handoffs.
