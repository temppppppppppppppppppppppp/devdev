# 코덱스 의견: Opus 4.6 + modules 크래시 디버깅 조사 계획

> 상태: 아카이브 문서(2026-02-13 정적 조사 계획). 최신 실행 우선순위 문서 아님.

## 목적
- 당시 경로 `C:\Users\wjjo\Desktop\글도비\modules` 기준으로, 실행 테스트 없이 크래시 원인 후보를 구조적으로 좁힌다.
- `opus 4.6` 도입/비교 관점에서 어디가 충돌 지점인지 식별한다.

## 작업 원칙 (요청 반영)
- 실행/테스트 금지: 스크립트 실행, 통합 테스트, 부하 테스트 모두 제외.
- 정적 분석 + 로그/설정/코드 경로 추적만 수행.
- 결과물은 "원인 후보 우선순위 + 확인 절차" 형태로 정리.

## 사전 관찰 요약 (정적 확인)
- 현재 에이전트 기반 호출은 Google GenAI에 강결합:
  - `modules/domain/agents/base_agent.py` (`from google import genai`, `client.models.generate_content`)
- 모델 SSOT가 Gemini 계열 중심:
  - `modules/core/constants.py` (`AIModels.* = gemini-*`)
  - `modules/core/config_manager.py` (`settings["models"]` 기본값 Gemini)
- Stage4 오케스트레이터가 거대 try/except + 다수 하위 에이전트/validator 의존:
  - `modules/core/stage4_orchestrator.py`
- 모델 폴백/재시도/키 로테이션이 `BaseAgent.ask()`에 집중:
  - `modules/domain/agents/base_agent.py`

## 조사 플랜
1. 크래시 진입점 고정
- Stage2/Stage4/Director 계열에서 공통 진입 함수 맵 작성.
- 우선순위 경로:
  - `modules/core/stage4_orchestrator.py`
  - `modules/core/stage2_orchestrator.py`
  - `modules/domain/agents/director.py`
  - `modules/domain/agents/base_agent.py`
- 산출물: "어떤 호출 체인에서 예외가 최상위로 전파되는지" 표.

2. Opus 4.6 충돌면 조사 (호환성 관점)
- Google 전용 API 사용 지점 전수 태깅:
  - `client.models.generate_content`, `types.GenerateContentConfig`, `ThinkingConfig` 사용 위치
- 모델명 하드코딩/SSOT 경로 분리:
  - constants/config_manager/agent ctor 기본값
- 산출물: "provider-agnostic 추상화가 없는 위치" 리스트.

3. 예외 삼킴(swallow)과 급사 경로 분리
- `except Exception` 후 continue/log-only 처리 구간과, 실제 raise/return None 구간 분리.
- 특히 `BaseAgent.ask()`의 재시도 루프/폴백 루프에서:
  - rate-limit/quota/network 분기
  - 모델 전환 후 상태 복구 누락 가능성
- 산출물: "로그엔 남지만 상위에서 빈 결과로 이어지는 경로" 체크리스트.

4. 상태 변이 리스크 조사
- `self.primary_model` 임시 변경 후 복원 패턴 사용 파일 점검:
  - 예: `state_locked_arc_generator.py`, `block_enricher.py`
- 멀티스레드/병렬 구간과 공유 상태 충돌 가능성 분류:
  - `blueprint_ensemble.py` (ThreadPoolExecutor)
- 산출물: "레이스/상태 오염 가능 지점" 우선순위 리스트.

5. 설정/환경 결손 기반 크래시 후보 조사
- API key 로테이션 전제(`GOOGLE_API_KEY*`)가 깨질 때 경로 확인.
- 프로젝트 루트/경로 의존(`Path.cwd()`)으로 인한 파일/DB 경로 mismatch 가능성 확인.
- 인코딩 깨짐(주석/문자열)과 런타임 영향 분리.
- 산출물: "환경 조건 불만족 시 즉시 실패하는 조건" 표.

6. 로그 포인트 설계 (코드 변경 없이 설계안만)
- 다음 함수들에 들어가야 할 최소 로그 키 설계:
  - correlation_id, ep_num, agent_name, model, retry_count, fallback_depth, exception_type
- 핵심 함수별 로그 스키마 정의:
  - `BaseAgent.ask`
  - Stage4 메인 루프 1회전
  - Director audit/quick_judge 경로
- 산출물: "크래시 재발 시 1회 로그로 원인 좁히는 필수 필드".

7. 우선순위 기반 조사 순서 (실행 없는 데스크 리서치)
- P0: `base_agent.py` (재시도/폴백/키 로테이션)
- P1: `stage4_orchestrator.py` (대규모 orchestration + 광범위 예외 처리)
- P1: `director_*` 패밀리 (audit/ensemble/caching)
- P2: `state_locked_arc_generator.py`, `block_enricher.py` (모델 상태 변이)
- P2: validation orchestrator 계열

## 완료 기준 (Definition of Done)
- 실행 없이 아래 4개가 문서화되면 조사 1차 완료:
  - Top 5 크래시 원인 후보
  - 각 후보의 코드 근거 파일/함수
  - 로그로 판별 가능한 신호
  - 수정 방향(소규모/중규모/구조개편) 분류

## 후속 산출물 예고
- 다음 단계에서 원하면 `코덱스의견.md`를 이어서
  - "원인 후보 Top 5 실제 채점표"
  - "최소 수정 패치 제안(테스트 실행 없이)"
  형태로 확장 가능.

## 테스트 필요 후보군 (통합 1회 실행 전 확정)
### P0 (최우선)
- `modules/domain/agents/base_agent.py`
  - 사유: API 호출, 폴백, 재시도, 키 로테이션이 집중된 단일 병목.
  - 실패 신호: `quota/rate-limit/network` 분기 후 무한 재시도, 잘못된 fallback 모델 전환, 빈 응답 후 상위 전파.
- `modules/core/stage4_orchestrator.py`
  - 사유: Stage4 메인 루프에서 다수 에이전트/validator 조합 + 광범위 예외 처리.
  - 실패 신호: 중간 단계 예외가 최종적으로 `candidates 빈 배열`/조기 종료로 귀결.

### P1 (높음)
- `modules/domain/agents/director.py`
- `modules/domain/agents/director_ensemble.py`
- `modules/domain/agents/director_auditor.py`
  - 사유: 판정/재시도 로직이 Stage4 합격/실패를 직접 결정.
  - 실패 신호: parsing_error 누적, audit 결과 구조 불일치, retry_count 증가 대비 품질/결정 불안정.

### P2 (중간)
- `modules/domain/agents/state_locked_arc_generator.py`
- `modules/domain/agents/block_enricher.py`
  - 사유: `self.primary_model` 임시 변경/복원 패턴 존재, 상태 오염 가능성.
  - 실패 신호: 예외 후 모델 복원이 누락되어 다음 호출 모델이 비정상 상태.
- `modules/domain/agents/blueprint_ensemble.py`
  - 사유: `ThreadPoolExecutor` 기반 병렬 처리/타임아웃.
  - 실패 신호: worker 예외가 후보 부족으로 누적되어 후단 급사.

## 한 번에 통합 테스트 체크리스트 (원샷 실행용)
1. 테스트 범위 고정
- 장르 1개, 프로젝트 1개, 에피소드 소량(예: 1~2화)으로 고정.
- 입력 데이터/설정 버전을 기록하고 실행 중 변경 금지.

2. 관측 키 확보 (최소 로그 필드)
- `correlation_id`, `ep_num`, `agent_name`, `model`, `retry_count`, `fallback_depth`, `exception_type`.
- Stage4 한 루프 시작/종료에 동일 `correlation_id` 사용.

3. 실행 전 사전 확인
- `GOOGLE_API_KEY*` 존재 여부와 사용 순서 확인.
- 프로젝트 경로(`Path.cwd()`)가 의도한 프로젝트 루트와 일치하는지 확인.
- 테스트 중 로그 파일 저장 경로/권한 확인.

4. 통합 테스트 1회 실행
- 실행은 1회만 수행, 중간 수동 개입 금지.
- 실패하더라도 즉시 재실행하지 않고 로그 분석 우선.

5. 실패 시 역추적 순서
- 1순위: `base_agent.py` 호출/폴백/예외 분기.
- 2순위: `stage4_orchestrator.py` 루프 내 빈 결과/조기 종료 지점.
- 3순위: `director_*` 파싱/판정 구조 불일치.
- 4순위: 상태 변이(`primary_model` 복원) 및 병렬 타임아웃.

6. 합격 기준
- 크래시 없이 종료.
- 실패/재시도 발생 시에도 최종 결과가 구조적으로 유효(JSON 스키마/필수 필드 충족).
- 후보군별 로그가 누락 없이 수집됨.
