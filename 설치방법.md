# 글도비 설치 및 실행 방법

> AI 웹소설 자동 생성 시스템 — 처음 실행하는 분을 위한 안내서

---

## 준비물

- **API 키**: 담당자에게 별도로 전달받은 Google API 키 문자열
- **인터넷 연결**: 설치 및 실행 시 필요

---

## 1단계: Python 설치 확인

1. 키보드에서 `윈도우키 + R` → `cmd` 입력 → 엔터
2. 검은 창(명령 프롬프트)이 열리면 아래 명령어 입력 후 엔터:
   ```
   python --version
   ```
3. `Python 3.11.x` 또는 `Python 3.12.x` 가 표시되면 OK
4. 오류가 나거나 버전이 낮으면 → https://www.python.org/downloads/ 에서 최신 버전 설치

---

## 2단계: 폴더 열기

1. 전달받은 `글도비` 폴더를 원하는 위치에 압축 해제
2. `윈도우키 + R` → `cmd` 입력 → 엔터
3. 명령 프롬프트에 아래 형식으로 입력 (폴더 경로는 실제 위치로 변경):
   ```
   cd C:\Users\사용자이름\Desktop\글도비
   ```
   > 팁: 탐색기에서 글도비 폴더를 열고, 주소창을 클릭하면 경로가 표시됩니다.

---

## 3단계: 라이브러리 설치 (최초 1회만)

명령 프롬프트에 아래 명령어 입력 후 엔터:

```
pip install -r requirements.txt
```

설치에 1~3분 정도 소요됩니다. 완료되면 다음 단계로.

---

## 4단계: API 키 설정

1. 글도비 폴더 안의 `.env.example` 파일을 복사
2. 복사본 이름을 `.env` 로 변경
   > 주의: `.env.example` 이 아니라 반드시 `.env` 여야 합니다
3. `.env` 파일을 메모장으로 열기
4. `여기에_API_키를_붙여넣으세요` 부분을 전달받은 API 키로 교체 후 저장

```
GOOGLE_API_KEY=AIzaSy...실제키...
```

---

## 5단계: 실행

명령 프롬프트에 입력:

```
python main_a.py
```

메뉴가 표시되면 성공입니다.

---

## 기본 사용 흐름

```
[0] Stage 0    → 세계관(Bible)/플롯(Treatment) 설정
[6] OneStop    → Arc 단위 자동 생성 (설정 완료 후 사용)
[4] Stage 4   → 원고 집필만 단독 실행
```

생성된 원고는 `projects/프로젝트명/drafts/` 폴더에 저장됩니다.

---

## 문제 해결

| 증상 | 해결 방법 |
|------|-----------|
| `python` 명령을 찾을 수 없음 | Python 미설치 → 1단계부터 다시 |
| `No module named ...` 오류 | 3단계 라이브러리 설치 다시 실행 |
| `GOOGLE_API_KEY not set` 오류 | 4단계 .env 파일 확인 |
| API 오류 (429 등) | API 키 한도 초과 → 담당자 문의 |
| 기타 오류 | 오류 메시지 캡처 후 담당자 문의 |

---

문의: 담당자에게 오류 화면 스크린샷과 함께 연락 주세요.
