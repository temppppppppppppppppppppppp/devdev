# market_snapshots

Role:

- store dated promoted snapshots from runtime collectors

Convention:

- one dated folder per collection wave
- keep JSON as the stage-facing canonical snapshot
- keep CSV as operator-friendly companion export when useful
