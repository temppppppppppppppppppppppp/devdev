# 40_analysis

Role:

- hold human-facing derived analysis and promoted research snapshots
- separate stage-consumable results from runtime raw ingest

Current intended use:

- store dated market and reference snapshots under `market_snapshots/YYYY-MM-DD/`
- store canonical long-form analysis reports under `pattern_reports/`
- keep JSON as the primary canonical snapshot when a JSON/CSV pair exists
