# 80_ingest_raw

Role:

- hold raw ingest evidence promoted out of runtime collector areas

Convention:

- bucket by collection date
- preserve source prefixes from runtime outputs
- treat these files as evidence buckets, not reader-facing summaries
