# Firefly-Side Research Dry Run Plan v1

Date: 2026-05-17
Status: dry run plan, research-only
Work ID: `chaebol3_sector_rotation_successor`
Issue: #157
Inputs: `116`, `118/121`, `122/124`

## 0. Boundary

This is not a production manuscript plan.

It does not authorize:

- B11-B20;
- TR70;
- BI;
- Firefly DB/project mutation;
- `manuscript save`;
- production manuscript save;
- production S4 prompt change;
- work_guard publication.

The next Firefly-side test must be file-only and explicitly labelled `draft_not_db_saved`.

## 1. Why This Plan Exists

Material-side proof now has two smoke passes:

- `118/121`: finance/file-room surface can become scene after thesis-clean lines are removed.
- `122/124`: product-hand surface can become tactile scene after future-knowledge/meta and thesis-clean closure are removed.

This is enough to test Firefly S4 compatibility.

It is not enough to expand the story range.

The dry run's purpose is narrow:

Can Firefly-style S4 receive the compact material packet and produce file-only prose that realizes room, object, resistance, smaller permission, visible objection, and next gate without exposing the mechanism?

## 2. Firefly Harness Constraints Read Before Planning

Read surfaces checked:

- `C:\Users\User\Desktop\글도비_파이어플라이\LLM_HARNESS_READ_FIRST.md`
- `C:\Users\User\Desktop\글도비_파이어플라이\.agents\skills\write\SKILL.md` search surface
- `C:\Users\User\Desktop\글도비_파이어플라이\계류장\tmp_sector_rotation_v04_s4_dryrun.md`

Relevant constraints:

- `manuscript validate` is only evidence, not completion.
- Stage 4 needs post-write micro-audit before any completion claim.
- File-only output must not be called DB-saved or canonical.
- If no save occurs, label it `draft_not_db_saved`.
- S4 Writer Context must realize work frame, live transaction, priced loss, resistant witness, access gain, behavior ladder, final price-tag translation, final receipt/next gate as visible scene pressure.
- Document/file/table carriers only count when they move a person, cost, access, responsibility, call, room, hand, body, or object on page.

## 3. Dry Run Mode

Use strict file-only dry run.

Do not run:

- `manuscript save`;
- any command that writes to Firefly project DB;
- any production project repair command;
- any production S4 prompt or work_guard publication command.

Allowed:

- read Firefly harness docs;
- read existing `tmp_sector_rotation_v04_s4_dryrun` mooring/context notes;
- create a research-only dry run artifact under material research or Firefly experiments;
- run text/JSON validation on the artifact if applicable;
- run manual post-write micro-audit.

Recommended output location:

`material_ssot/10_research/30_work_materials/chaebol3_sector_rotation_successor/`

Reason:

- keeps this in material research;
- avoids accidental Firefly DB/project mutation;
- stays close to `116`, `118/121`, `122/124`.

## 4. S4 Input Packet For Dry Run

Use only:

1. `116` Section 2 writer-facing payload.
2. These distilled audit lessons:
   - remove thesis-clean explanation after draft;
   - convert "why it works" into physical behavior;
   - keep one objection visible on paper/board/object;
   - end on object/access position;
   - protagonist asks for less than victory;
   - adult resistance protects a practical cost.

Do not feed:

- `114`;
- `115`;
- full audit prose;
- raw donor manuscripts;
- NAS text;
- long synthesis language;
- labels like reward ladder, receipt, protected asset, AI-slop, harness.

## 5. Candidate Dry Run Unit

Run a single file-only scene or short episode slice.

Recommended unit:

`EP007 buyer desk`

Why:

- EP009-010 already passed paper/file-room smoke.
- EP006 already passed product-hand smoke.
- EP007 tests commercial outside-buyer behavior: return ledger, phone, parent calls, shelf risk, quantity shrink.
- It sits between product proof and public-use proof, so it tests whether S4 can carry the object chain forward without turning it into a report.

Alternative unit:

`EP004 first task`

Use only if the operator wants to verify first-scene role clarity before buyer desk.

## 6. Draft Requirements

The file-only dry run must include:

- boundary: `draft_not_db_saved`;
- input list: `116 Section 2 + audit lessons from 118/121 and 122/124`;
- prose sample with no headings inside the prose body;
- post-write micro-audit section;
- explicit stoplines.

The prose must show:

- real room/desk;
- sample box physically placed;
- buyer asks practical risk questions before praise;
- Doyun reduces quantity or exposure;
- return cap / support window / return ledger remains visible;
- ending object/access: sample box remains beside phone/return ledger, not front display;
- next gate: school/gym trial or public-use surface opened by the buyer desk.

The prose must not show:

- buyer admiration;
- front-display victory;
- big order;
- clean comeback language;
- future-knowledge monologue;
- mechanism labels.

## 7. Audit Requirements

Post-write micro-audit must answer:

1. Did the scene start in a real buyer desk with live objects?
2. Did Doyun ask for less than victory?
3. Did the buyer protect a practical cost?
4. Did one visible objection remain?
5. Did the ending leave object/access physically in place?
6. Did the next gate come from that object/access?
7. Did the prose contain thesis-clean lines that explain the mechanism?
8. Did dialogue sound like people protecting work, not checklist items?
9. Is this still `draft_not_db_saved`?

If any answer fails:

- patch the prose;
- record the patch;
- re-audit;
- do not proceed to Firefly DB, B11-B20, TR70, or BI.

## 8. Go / No-Go

| Question | Answer |
|---|---|
| Can we run file-only dry run next? | YES |
| Can we use Firefly DB/project state? | NO |
| Can we save manuscript? | NO |
| Can we generate B11-B20 after this plan? | NO |
| Can this test inform S4 context design? | YES |

## 9. Next Required Unit

`Run file-only EP007 buyer-desk dry run from 116, with post-write micro-audit and patch pass if needed.`

Stoplines:

- no B11-B20;
- no TR70;
- no BI;
- no Firefly DB/project mutation;
- no production manuscript save;
- no production S4 prompt change.
