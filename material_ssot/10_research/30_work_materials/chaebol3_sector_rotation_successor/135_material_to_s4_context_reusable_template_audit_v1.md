# Material-to-S4 Context Reusable Template Audit v1

Date: 2026-05-17
Status: adversarial audit complete
Target: `134_material_to_s4_context_reusable_template_v1.md`
Work ID: `chaebol3_sector_rotation_successor`
Issue: #157

## 0. Verdict

`PASS_WITH_WATCH_FOR_ONE_FILE_ONLY_PROSE_DRYRUN`

The template is usable as the next bridge from material-side research into a Firefly-style file-only prose dry run.

It does not authorize:

- B11-B20;
- TR70;
- BI;
- Firefly DB/project mutation;
- `manuscript save`;
- production manuscript save;
- production S4 prompt change;
- work_guard publication.

## 1. Pass 1 - Does This Actually Help S2-S3-S4?

Verdict: PASS.

The template does not replace the pipeline. It preserves the existing S4 Writer Context fields and only standardizes how material-side packets fill them.

Useful effect:

- BI/TR/guard philosophy is kept out of prose;
- S4 receives room, people, objects, cost, and final access;
- post-prose audit remains mandatory.

## 2. Pass 2 - Does It Create New Harness Slop?

Verdict: PASS_WITH_WATCH.

The risk remains because the template contains field names.

Mitigation is present:

- writer-visible view is separate from context fill;
- forbidden writer-visible note is explicit;
- post-prose audit blocks canary/receipt/harness/context/audit leakage.

Watch item:

The next dry run must not paste the JSON skeleton directly into prose generation without stripping or clearly quarantining field labels.

## 3. Pass 3 - Is It Too Generic?

Verdict: PASS_WITH_WATCH.

The template is generic enough to reuse across surfaces, but it is grounded in the International Group evidence chain.

It remains specific through:

- object movement;
- protected cost;
- smaller permission;
- visible objection;
- final object/access position.

Watch item:

Future use must fill all fields from a specific compact unit. Do not use it as a broad "make scene better" checklist.

## 4. Required Next Unit

Run exactly one file-only prose dry run from `132` using `134`.

Required output status:

`draft_not_db_saved`

Required audit after prose:

- line-level AI-slop residue check;
- practical resistance before curiosity/praise;
- smaller ask visible;
- final object/access token in place;
- no DB/save/prompt mutation.

Stoplines remain:

- no B11-B20;
- no TR70;
- no BI;
- no Firefly DB/project mutation;
- no production manuscript save.
