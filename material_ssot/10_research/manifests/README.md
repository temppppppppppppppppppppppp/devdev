# Research Manifests

This directory now holds bootstrap work-level research manifests.

Rules:

- one file per `work_id`
- record only confirmed source links
- point to `30_work_materials/{work_id}` when a normalized pack exists
- leave unresolved raw research explicitly deferred
- do not relocate corpus files in this wave
