# 00_registry

Role:

- record research-stage source classes, non-SSOT runtime surfaces, and migration labels
- provide the authority boundary between `material_ssot` and external or deferred research buckets

Current focus:

- keep `로직_리서치` visible as a runtime collector surface without treating it as canonical research authority
- map future bounded cutovers before moving files
