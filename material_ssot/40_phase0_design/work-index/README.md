# Work Index

This directory now holds representative work manifests for Phase 0 design.

Rules:

- one manifest per `work_id`
- point to the current live phase0 root file when it exists
- otherwise point to the preprocess-side phase0 snapshot
- connect each phase0 manifest to the Stage0 and TR manifests
