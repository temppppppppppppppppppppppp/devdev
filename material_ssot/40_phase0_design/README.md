# 40_phase0_design

Role:

- own stage-level contracts and work index for Phase 0 design
- point to live phase0 files without moving them

Primary docs:

- `work-index/`
- `contracts/`

Current live paths:

- `treatments/phase0/{work_id}_phase0_design.json`
- `treatments/preprocess/{work_id}/phase0_ready_snapshot.json`
