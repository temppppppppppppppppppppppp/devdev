# Work Index

This directory now holds representative work manifests for BI.

Rules:

- one manifest per `work_id`
- point to the current live BI file
- record current canonical status when confirmed
- connect each BI manifest back to the TR manifest
