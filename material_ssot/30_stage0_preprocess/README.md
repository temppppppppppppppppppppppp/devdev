# 30_stage0_preprocess

Role:

- own stage-level contracts and work index for Stage 0 preprocess
- point to live preprocess outputs without moving them

Primary docs:

- `work-index/`
- `contracts/`

Current live path:

- `treatments/preprocess/{work_id}`

Current transition governance path:

- `전처리_ssot`
