# Work Index

This directory now holds the first representative work manifests.

Representative-slice rule:

- one manifest per `work_id`
- anchor the manifest at Stage 0 preprocess
- include the current known chain from research through BI
- leave unresolved research or pitch links explicitly marked as deferred instead of guessing
