# Pending Cuts

Date: 2026-04-05
Status: active bounded queue
Scope: next-wave migration and normalization items that remain outside the current bounded SSOT slice

## 1. Current Queue

- decide whether the bootstrap work set should expand beyond `gatekeeper_heir`, `office_checkup_next_day`, and `wuxia_heavenly_physician`
- decide whether old `narrative_ssot/10_reference_bank/source_corpora` residue should stay in place as archive-only payload or move into a separate archive lane
- verify the NAS medical sample builder on the machine that can actually see the NAS title directory
- decide whether `전처리_ssot/docs/10_pitches` should stay legacy-active or receive a bounded stage-axis cutover later
- decide whether the bounded validator should grow from representative-work checks into broader stage-inventory checks

## 2. Current Rule

- no wholesale migration is implied by this queue
- label first, manifest first, cut later
- any future cut should stay bounded to one lane unless a broader wave is explicitly approved
