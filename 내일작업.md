# 글도비 로드맵 — 잔여 작업 전체 정리

> **프로젝트**: 글도비 — 웹소설 자동 생산 파이프라인 (Stage 0→2→4)
> **마지막 갱신**: 2026-02-13 14:10
> **참고 문서**: `CLAUDE.md`, `참고자료.md` (2087줄), `AG_조사_modules.md`

---

## 🔄 완료된 작업 (중복 금지)

| Phase | 내용 | 커밋 |
|-------|------|------|
| 1 | print→logging 전환 (973/994건) | 완료 |
| 1.5 | 레거시 파일 삭제 (editor, cleaner, evaluator) | 완료 |
| 2-B | Type Hints 95.5% (2234/2340) | 완료 |
| 5-A | 프롬프트 YAML 외부화 (43개, 87키) | 완료 |
| 5-A' | PromptLoader import 전환 (7파일) | `46c4801` |
| 5-C | 의존성 정리 | 완료 |
| 6-A | pytest 63개 신규 | 완료 |
| 6-C | pre-commit + ruff | 완료 |
| — | silent pass → warning (hud_utils, prompt_builder×5, stage2, stage4×2) | `46c4801` |
| — | genre_hud_manager 공통화 (1281→702줄) | `46c4801` |
| — | bible_extractor.py Dead Code 삭제 | `46c4801` |
| — | Context Caching 구현 (`base_agent.py` L920~1068, `chief_writer`·`director_continuity`에서 사용 중) | 기존 구현 |

---

## ⚠️ 현재 상태 (반드시 읽기)

- **God Object**: `stage2_orchestrator.py` `self.app` 341건 / `stage4_orchestrator.py` 312건 = **653건 건재**
- **memory_engine.py**: ChromaDB 비활성, import하면 에러
- **writer.py**: 레거시이지만 stage4에서 유틸 3개 직접 호출 중

---

## 📋 로드맵 (우선순위 순)

### Phase 5-B — Settings YAML + 모델 티어 중앙화 `[SAFE]`

| 항목 | 현재 | 목표 |
|------|------|------|
| 장르 가드 | `genre_guards/*.py` 하드코딩 | `config/genres/*.yaml` |
| 작품 가드 | 코드 내 분산 | `config/genres/{genre}/{작품}.yaml` |
| 모델 티어 | 파일마다 산재 | `config/models.yaml` |
| 시스템 파라미터 | `.env` + 하드코딩 | `config/system.yaml` |
| 시점 설정 | V70 구현됨, YAML 미연동 | 작품 가드 YAML 연동 |

**참고**: `참고자료.md` L1643~1676, L1467~1496 (시점 전환)
**DoD**: `config/genres/` + `config/models.yaml` 생성, wuxia YAML 1개 완성, `tests/test_genre_guard.py` 통과

---

### Phase 2-A — Pydantic 모델 도입 `[RISKY]`

| 대상 | 파일 |
|------|------|
| Arc 데이터 (bare dict) | `stage2_orchestrator.py` |
| Blueprint 데이터 | `stage2_orchestrator.py` |
| 원고 응답 | `chief_writer.py` → `stage4` |
| NPC 레지스트리 | `state_tracker.py` |

**전제**: Phase 5-B 후
**DoD**: `modules/models/` 디렉토리, Arc 모델 최소 1개 적용

---

### Phase 2-C — ABC/Protocol 인터페이스 `[RISKY]`

에이전트 20+개에 공통 인터페이스 정의. 현재 각자 다른 메서드명.
**참고**: `참고자료.md` L1887~1902
**전제**: Phase 2-A 후

---

### Phase 2.5 — ChromaDB → sqlite-vec `[RISKY]`

| 파일 | 현재 | 목표 |
|------|------|------|
| `memory_engine.py` | chromadb (비활성) | sqlite-vec |
| `blueprint_memory.py` | chromadb | sqlite-vec |
| `semantic_plot_guard.py` | genai 직접 호출 | 통합 벡터 레이어 |

**전제**: Phase 2-A 후

---

### Phase 3 — 검증 강화 + 수정 모드 + 품질 시스템 `[RISKY]`

#### 3-A. 핵심 (NPC + 검증)

| 항목 | 파일 | 변경 |
|------|------|------|
| NPC 이력 영속화 | `relationship_tracker.py` | in-memory → DB |
| Chain 2 graceful degradation | `state_tracker.py` | entity_registry=None 처리 |
| Chain 3 SKIP 반환 | `director_continuity.py` | "PASS" → "SKIP(검증불가)" |
| 에피소드 롤백 NPC 되감기 | `project_manager.py` | `auto_backtrack_v35()` 확장 |
| 사망 캐릭터 부활 방지 | `pre_llm_validator.py` | deceased 하드 REJECT |

**참고**: `참고자료.md` 3-C (NPC 시나리오 24개)

#### 3-B. 수정 모드 (피드백 기반 부분 수정)

| 점수 | 동작 |
|------|------|
| < 50 | 전체 재작성 |
| 50~80 | **수정 모드** — 기존 결과물 + 수정 지시만 전달 |
| > 80 | 통과 |

- Stage 2 (Arc/Blueprint) + Stage 4 (원고) 모두 적용
- **수정 diff 뷰**: 원본 vs 수정본 비교 (`difflib`)

**참고**: `참고자료.md` L1280~1318 (3-D), L1364~1366

#### 3-C. 독자 대리만족 프레임워크

Director 심사에 "대리만족 점수" 축 추가 (기존 100점 배분 조정):
- 좌절-보상 타이머 (좌절 3화 이내 보상 필수)
- 에피소드별 자동 태깅 ([성취], [좌절], [이행] 등)
- 장르별 대리만족 유형 (무협: 돌파/제압, 요리: 대회 우승 등)

**참고**: `참고자료.md` L1548~1610 (3-F)

#### 3-D. 품질 모니터링

| 항목 | 기존 | 추가 |
|------|------|------|
| 품질 회귀 감지 | ❌ 없음 | 에피소드 N vs N+1 점수 비교 |
| 온도 자동 조절 | 하드코딩 | `pass_rate_monitor` 연동 |
| 토큰 예산 추적 | ❌ 없음 | `quality_dashboard` 확장 |
| 드라이런 모드 | ❌ 없음 | `stage4` `dry_run` 플래그 |

#### 3-E. 콘텐츠 품질 체크

| 항목 | 기존 | 추가 |
|------|------|------|
| NPC 과잉 등장 경고 | ❌ 없음 | 신규 NPC 3명/화 초과 시 경고 |
| 크로스 에피소드 반복 | 1화 내 n-gram만 | DB 인덱싱 + 에피소드 간 비교 |
| 클리프행어 패턴 추적 | 호흡 분석만 | 엔딩 패턴 분류 추가 |
| 대사 화자 명확성 | ❌ 없음 | 화자 없는 대사 3줄+ 경고 |
| 스크린타임 밸런스 | NPC 빈도만 | 단어 수 추적 확장 |

#### 3-F. Stage 4 확장

| 항목 | 기존 | 추가 |
|------|------|------|
| 멀티 드래프트 비교 | Arc/Blueprint에만 | Stage 4 원고 확장 |
| 컨텍스트 압축 고도화 | 기본 압축만 | 중요도 가중치 + 구조화 요약 |
| 인간 편집 학습 | ❌ 없음 | diff 캡처 → 선호 학습 |
| 댓글/여론 반응 생성 | ❌ 없음 | 연예계 장르 전용 |

---

### writer.py 레거시 유틸 이전 `[MEDIUM]`

stage4에서 직접 호출하는 유틸 3개 → 적절한 모듈로 이전 후 삭제.
**시기**: Phase 2-A 작업 시 함께

---

### Phase 6-B — E2E 테스트 `[SAFE]`

| 항목 | 내용 |
|------|------|
| Smoke Test | Stage 0→2→4 1화 통과 |
| NPC 연속성 | 3화+ NPC 등장/퇴장 검증 |
| 에러 복원 | API 장애 시뮬레이션 |

**기존**: `tests/` 63개 pytest, `tests/stage4_v2_test/` 통합 테스트

---

### Phase 4 — God Object 분해 + async 통일 `[RISKY, 대규모]`

| 항목 | 현재 | 목표 |
|------|------|------|
| `self.app` 341건 | `stage2_orchestrator.py` | DI 패턴 |
| `self.app` 312건 | `stage4_orchestrator.py` | DI 패턴 |
| stage4 단일 try | 1354줄 try-except | 함수 분리 |
| 대형 파일 분할 | 2134줄 / 1633줄 | 서브모듈화 |
| async 혼용 | ThreadPoolExecutor | `asyncio.to_thread()` 통일 |

**참고**: `참고자료.md` L1613~1628, L1917~1926
**전제**: Phase 6-B(E2E) 이후. 회귀 테스트 없이 분해하면 크래시 위험.

---

## 실행 순서

```
1. Phase 5-B  (Settings YAML + 모델 티어)     ← SAFE, 독립적
2. Phase 2-A  (Pydantic)                       ← 구조 변경 시작
   └ writer.py 유틸 이전                       ← 2-A와 함께
3. Phase 2-C  (ABC/Protocol)                   ← 2-A 이후
4. Phase 2.5  (sqlite-vec)                     ← 벡터 통합
5. Phase 3    (검증 + 수정모드 + 대리만족)     ← 가장 큰 Phase
6. Phase 6-B  (E2E 테스트)                     ← 회귀 방지 체계
7. Phase 4    (God Object + async)             ← E2E 있어야 안전
```
