# 글도비 로드맵 — 잔여 작업 전체 정리

> **프로젝트**: 글도비 — 웹소설 자동 생산 파이프라인 (Stage 0→2→4)
> **마지막 갱신**: 2026-02-14
> **참고 문서**: `CLAUDE.md`, `참고자료.md` (2087줄), `AG_조사_modules.md`

---

## 🔄 완료된 작업 (중복 금지)

| 내용 | 비고 |
|------|------|
| Phase 1: print→logging (973/994건) | 완료 |
| Phase 1.5: 레거시 삭제 (editor, cleaner, evaluator) | 완료 |
| Phase 2-B: Type Hints 95.5% | 완료 |
| Phase 5-A: YAML 외부화 (43개, 87키) | 완료 |
| Phase 5-A': PromptLoader import 전환 (7파일) | `46c4801` |
| Phase 5-B Step1: models.yaml + 모델 티어 중앙화 | `748c01b` |
| Phase 5-B Step2: system.yaml + 운영 파라미터 외부화 | `c9e9374` |
| Phase 5-B Fix: director.py 깨진 import 수정 | `4741835` |
| Phase 5-C: 의존성 정리 | 완료 |
| Phase 6-A: pytest 63개 | 완료 |
| Phase 6-C: pre-commit + ruff | 완료 |
| silent pass → warning (8건/4파일) | `46c4801` |
| genre_hud_manager 공통화 (1281→702줄) | `46c4801` |
| bible_extractor.py Dead Code 삭제 | `46c4801` |
| Context Caching 구현 (chief_writer·director_continuity 사용 중) | 기존 |
| **V70.1 Arc 간 상태 연속성 검사** — `director.yaml` 프롬프트 | ⚠️ 프롬프트만 완료, 데이터 파이프라인 미구현 (Step 5-A) |
| Step 2: Pydantic v2 모델 도입 (Arc/Blueprint/Manuscript/NPC) | `75da012` |
| Step 3: Agent Protocol 인터페이스 5종 (7개 에이전트 자동 적합) | `eb18d17` → `a0fb13b` |
| **Phase 4C: Orchestrator DI 마이그레이션** (Stage2/3/4) | `3667d11`→`6a1bf98`→`6a23e41`→`fa621fe`→`2c0193e` |
| lint unblock + pre-commit 정상화 | `493e104` |

---

## ⚠️ 현재 상태

- **God Object (핵심 오케스트레이터)**: `stage2/3/4` `self.app` 직접 참조 **0건** (DI 전환 완료)
- **DI 현황**: stage2 `self.ctx` 348건 / stage3 `self.ctx` 4건 / stage4 `self.ctx` 313건
- **memory_engine.py**: **삭제됨** (Phase 4D 완료). VecMemory 단일 경로
- **writer.py**: 레거시이지만 stage4에서 유틸 3개 직접 호출 중
- **pytest `tests/` 전체 수집 실패**: `stage3_isolated_test/*.py` L20-22에서 `sys.stderr` 모듈레벨 재래핑 → capture 파괴
- **현재 작업 포커스**: **Phase 3-5B 설계 확정** — `docs/phase3_5b_blueprint.md` 승인 후 구현

---

## 📋 실행 순서 (위에서 아래로)

### Step 1. Settings YAML + 모델 티어 중앙화 `[SAFE]` ✅ 완료

| 항목 | 상태 |
|------|------|
| ~~장르 가드~~ | ✅ YAML 10개 + 가드 10개 `cfg.get()` 전환 |
| 작품 가드 | ⏳ MODERATE — Opus TF 대상 |
| ~~모델 티어~~ | ✅ `748c01b` |
| ~~시스템 파라미터~~ | ✅ `c9e9374` |
| ~~시점 설정~~ | ✅ POV는 Bible에서만 결정 (장르가드 롤백 완료) |
| ~~레거시 제거~~ | ✅ `genre_guard.py` 삭제 + WuxiaGuard 전환 |
| ~~테스트~~ | ✅ 49 PASS, ruff All checks passed |
| ~~ruff 정리~~ | ✅ 224개 린트 에러 수정 |

**참고**: `참고자료.md` L1643~1676, L1467~1496

---

### Step 2. Pydantic 모델 도입 `[RISKY]` ✅ 완료 ← Phase 2-A

| 대상 | 파일 | 상태 |
|------|------|------|
| Arc 데이터 (bare dict) | `stage2_orchestrator.py` L1768 | ✅ `validate_arc()` |
| Blueprint 데이터 | `three_phase_blueprint_generator.py` L254 | ✅ `validate_blueprint()` |
| 원고 응답 | `chief_writer.py` L347 | ✅ `validate_manuscript_candidate()` |
| NPC 레지스트리 | `modules/models/npc.py` | ✅ `NPCEntry` 모델 |

커밋: `75da012` · 모델 5파일(`modules/models/`) + 테스트 49개 PASS
**함께**: `writer.py` 레거시 유틸 3개 이전은 미착수 (CLAUDE.md L53)

---

### Step 3. ABC/Protocol 인터페이스 `[RISKY]` ✅ 완료 ← Phase 2-C

| Protocol | 적합 에이전트 |
|----------|-------------|
| PipelineGenerator | FourPhaseArcGenerator, ThreePhaseBlueprintGenerator |
| EnsembleGenerator | ArcEnsembleGenerator, BlueprintEnsembleGenerator |
| ArtifactValidator | UnifiedArcValidator |
| ArtifactCritic | ArcCritic |
| Corrector | ArcCorrector |

미적합 5개(ChiefWriter, ConsensusValidator, Critic, Director, StateExtractor) — 향후 어댑터 대상
커밋: `eb18d17` → main 머지 `a0fb13b` · 파일 3개(`modules/protocols/` + 테스트) · 24개 PASS

---

### Step 4. ChromaDB → sqlite-vec + 벡터 재연결 `[RISKY]` ✅ 완료 ← Phase 4D

| 서브 | 커밋 | 내용 |
|------|------|------|
| 4D-1 | VecMemory 구현 | sqlite-vec 벡터 저장/검색 모듈 |
| 4D-2 | 벡터 경로 재연결 | semantic_plot_guard + main_a 연결 |
| 4D-3 | 안전망 + 게이트 | py_compile + pytest + pre-commit 통과 |
| 4D-4 | ChromaDB 레거시 제거 | memory_engine.py, blueprint_memory.py 삭제 |

**완료**: `memory_engine.py`/`blueprint_memory.py` 삭제, ChromaDB 참조 0건, VecMemory 단일 경로

---

### Step 5. 검증 강화 + 수정 모드 + 품질 시스템 `[RISKY]` ← Phase 3

가장 큰 단계. 6개 서브 영역:

#### 5-B. 수정 모드 (Stage 4 패치 모드) ← **현재 포커스**

> 상세 청사진: `docs/phase3_5b_blueprint.md`

| 단계 | 작업 | 상태 |
|------|------|------|
| 설계 | `docs/phase3_5b_blueprint.md` 작성 | ✅ |
| 구현 | constants + chief_writer + yaml + stage4 | ⏳ 문서 승인 후 |
| 테스트 | `tests/test_stage4_orchestrator.py` 신규 필수 | ⏳ |
| 게이트 | py_compile + pytest + pre-commit | ⏳ |
| 커밋 | 단일 atomic 커밋 | ⏳ |

**실행 순서**: 설계 확정 → 구현(4파일) → 테스트 작성 → 게이트 통과 → 커밋

**필수 게이트**:
```bash
pytest tests/test_stage4_orchestrator.py tests/test_chief_writer.py tests/test_stage2_pipeline.py tests/test_stage3_orchestrator.py -v
pre-commit run --files modules/core/constants.py modules/domain/agents/chief_writer.py modules/core/stage4_orchestrator.py config/prompts/chief_writer.yaml
```

#### 5-A. NPC + 검증 핵심

| 항목 | 파일 | 변경 |
|------|------|------|
| **NPC 이력 영속화** | `relationship_tracker.py` | in-memory → DB |
| **V70.1 Arc 상태 스냅샷 주입** | `stage2_orchestrator.py` → `director_auditor.py` | `prev_arc_context`에 구조화된 NPC 생사/아이템/부상/관계/위치 데이터 추가 |
| Chain 2 graceful degradation | `state_tracker.py` | entity_registry=None 처리 |
| Chain 3 SKIP 반환 | `director_continuity.py` | "PASS" → "SKIP(검증불가)" |
| 에피소드 롤백 NPC 되감기 | `project_manager.py` | `auto_backtrack_v35()` 확장 |
| 사망 캐릭터 부활 방지 | `pre_llm_validator.py` | deceased 하드 REJECT |

**참고**: `참고자료.md` 3-C (NPC 시나리오 24개)

#### 5-C. 독자 대리만족 프레임워크

Director 심사에 대리만족 점수 축 추가, 좌절-보상 타이머, 에피소드 자동 태깅
**참고**: `참고자료.md` L1548~1610

#### 5-D. 품질 모니터링

| 항목 | 추가 내용 |
|------|----------|
| 품질 회귀 감지 | 에피소드 N vs N+1 비교 |
| 온도 자동 조절 | `pass_rate_monitor` 연동 |
| 토큰 예산 추적 | `quality_dashboard` 확장 |
| 드라이런 모드 | `stage4` `dry_run` 플래그 |

#### 5-E. 콘텐츠 품질 체크

| 항목 | 추가 내용 |
|------|----------|
| NPC 과잉 등장 경고 | 신규 NPC 3명/화 초과 시 경고 |
| **크로스 에피소드 반복 감지** | DB 인덱싱 + 에피소드 간 n-gram 비교 |
| 클리프행어 패턴 추적 | 엔딩 패턴 분류 추가 |
| 대사 화자 명확성 | 화자 없는 대사 3줄+ 경고 |
| 스크린타임 밸런스 | NPC 단어 수 추적 확장 |

#### 5-F. Stage 4 확장

멀티 드래프트 비교, 컨텍스트 압축 고도화, 인간 편집 학습, 댓글/여론 반응 생성

---

### Step 6. E2E 테스트 `[SAFE]` ← Phase 6-B

| 항목 | 내용 |
|------|------|
| Smoke Test | Stage 0→2→4 1화 통과 |
| NPC 연속성 | 3화+ NPC 등장/퇴장 검증 |
| 에러 복원 | API 장애 시뮬레이션 |

---

### Step 7. God Object 분해 + async 통일 `[RISKY, 대규모]` ← Phase 4 (4C 완료, 잔여만)

| 항목 | 현재 | 목표 |
|------|------|------|
| `self.app` 0건 | `stage2_orchestrator.py` | ✅ DI 전환 완료 (`self.ctx`) |
| `self.app` 0건 | `stage3_orchestrator.py` | ✅ DI 전환 완료 (`self.ctx`) |
| `self.app` 0건 | `stage4_orchestrator.py` | ✅ DI 전환 완료 (`self.ctx`) |
| stage4 단일 try | 1354줄 try-except | 함수 분리 |
| 대형 파일 분할 | 2134줄 / 1633줄 | 서브모듈화 |
| async 혼용 | ThreadPoolExecutor | `asyncio.to_thread()` 통일 |

**전제**: DI 자체는 완료(4C). 잔여 리팩토링(async 통일/분할)은 Step 6(E2E) 이후 진행.

---

## 요약

```
Step 1  Settings YAML         [SAFE]     ✅ 완료
Step 2  Pydantic + writer.py  [RISKY]    ✅ 완료 (writer.py 유틸 이전 미착수)
Step 3  ABC/Protocol          [RISKY]    ✅ 완료 (미적합 5개 어댑터 미구현)
Step 4  sqlite-vec+벡터재연결 [RISKY]    ✅ Phase 4D 완료
Step 5  검증+수정모드+품질    [RISKY]    5B(패치모드) ← 현재 포커스
Step 6  E2E 테스트            [SAFE]     Step 5 이후
Step 7  God Object 분해       [RISKY]    4C(핵심 DI) 완료, 잔여만 Step 6 이후
```
