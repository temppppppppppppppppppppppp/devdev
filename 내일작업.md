# 글도비 로드맵 - 현재 기준 (SSOT)

> 마지막 갱신: 2026-02-15
> 이 문서는 "지금부터 무엇을 할지"만 관리한다.  
> 과거 설계/분석 이력은 `참고자료.md`, `docs/*.md`를 참고한다.

---

## 1) 완료된 핵심 단계 (요약)

| Phase | 상태 | 핵심 커밋 |
|------|------|----------|
| 1 / 1.5 | 완료 | logging 전환, 레거시 삭제 |
| 2-A / 2-B / 2-C | 완료 | `75da012`, `a0fb13b` |
| 2.5 / 4D | 완료 | `9cf6e7b`, `1766016`, `9d1ae97`, `1e60d45` |
| 3-5B (Stage4 패치 모드) | 완료 | `c8c2a71`, `4416284` |
| 3-5A (NPC 이력 + 검증) | 완료 | `126dceb`, `4d669a9` |
| 5-B (validation settings 외부화) | 완료 | `e6f0f31`, `7b8a148`, `352efcc`, `afc1765`, `f678c9a`, `a389ac5` |
| 5-A / 5-A' / 5-C | 완료 | 프롬프트 및 설정 외부화, 의존성 정리 |
| 6-A / 6-C | 완료 | 테스트 기반 + pre-commit/ruff |
| 6-B (E2E 테스트) | 완료 | `1678550` — Smoke 8 + NPC 7 + Recovery 7 = 22 E2E |
| 4-R1~R2 (stage4 분할+타입화) | 완료 | `6c5f292`~`210cb47` — 1,693줄→33줄, 4 dataclass |
| 4-R3(a–g) (stage2 분할+보강) | 완료 | `4288497`~`882cd0a` — 1,763줄→100줄, 안전 초기화 5개, 테스트 22개 |

---

## 2) 현재 테스트 기준선

| 스위트 | 파일/디렉토리 | 통과 |
|--------|-------------|------|
| Unit (preflight helpers) | `test_stage2_preflight_helpers.py` | 22 |
| Pipeline (stage2) | `test_stage2_pipeline.py` + `test_stage2_context.py` | 89 |
| E2E | `tests/e2e/` | 22 |
| Regression | `test_npc_history` + `test_config_manager` + `test_stage4_orchestrator` | 78 |
| **합계** | | **211** |

> checkpoint: `57d5b91`, ctx refs: stage2 348/43, stage4 313/22

---

## 3) 지금 남은 작업 (우선순위 Top 3)

### 1순위. Phase 4-R4: stage2 async/sync 통일 (선택적)

범위: `stage2_orchestrator.py` 내 `ThreadPoolExecutor` → `asyncio.to_thread` 전환
- 기대효과: async 호출 체인 단순화, 디버깅 용이성 향상
- 리스크: 동시성 모델 변경이므로 E2E 회귀 검증 필수 (중)

### 2순위. Phase 4 잔여 구조 개선 — 모듈 경계 명확화

범위: 오케스트레이터 책임 축소 + `stage_2_arcs_async_logic`(230줄), `throttled_enrich`(153줄) 추가 분할 검토
- 기대효과: 남은 Top 10 함수 중 2개 축소, 테스트 가능 단위 증가
- 리스크: 호출자-피호출자 인터페이스 변경 필요 (저~중)

### 3순위. Phase 3 잔여 품질 기능 (백로그)

아래는 구현 완료가 아니라 백로그 상태:
1. 5-C 독자 대리만족 프레임워크
2. 5-D 품질 모니터링
3. 5-E 콘텐츠 품질 체크
4. 5-F Stage 4 확장
5. V70.1 Arc 상태 연속성 데이터 파이프라인 보강

- 기대효과: 생성 품질 직접 개선 (사용자 가치 최대)
- 리스크: 프로덕션 코드 변경 범위 크고 LLM 동작 변경 수반 (고)

---

## 4) 현재 기술 부채

1. `writer.py` 레거시 유틸 3개 의존 정리 필요
2. Protocol 미적합 5개(어댑터 대상) 후속 필요
3. `tests/test_validation.py` 13건은 pre-existing 실패로 분리 관리 필요
4. 문서 일부는 역사 기록(과거 시점)이라 최신 상태와 불일치 가능

---

## 5) 문서 사용 규칙

실행 시 우선순위:
1. 이 문서(`내일작업.md`)
2. `CLAUDE.md` 현재 상태
3. 세부 설계 문서(`docs/*.md`)

참고:
- `docs/phase3_5b_blueprint.md`는 구현 전 청사진 문서이며 현재는 이력용
- `docs/프로젝트_현황_로드맵_2026-02-14.md`는 어제/오늘/다음 작업을 한 번에 보는 브리핑 문서
- `참고자료.md`는 대형 참고서(역사 기록 포함)
