# 글도비 로드맵 — 잔여 작업 전체 정리

> **프로젝트**: 글도비 — 웹소설 자동 생산 파이프라인 (Stage 0→2→4)
> **마지막 갱신**: 2026-02-13 14:21
> **참고 문서**: `CLAUDE.md`, `참고자료.md` (2087줄), `AG_조사_modules.md`

---

## 🔄 완료된 작업 (중복 금지)

| 내용 | 비고 |
|------|------|
| Phase 1: print→logging (973/994건) | 완료 |
| Phase 1.5: 레거시 삭제 (editor, cleaner, evaluator) | 완료 |
| Phase 2-B: Type Hints 95.5% | 완료 |
| Phase 5-A: YAML 외부화 (43개, 87키) | 완료 |
| Phase 5-A': PromptLoader import 전환 (7파일) | `46c4801` |
| Phase 5-C: 의존성 정리 | 완료 |
| Phase 6-A: pytest 63개 | 완료 |
| Phase 6-C: pre-commit + ruff | 완료 |
| silent pass → warning (8건/4파일) | `46c4801` |
| genre_hud_manager 공통화 (1281→702줄) | `46c4801` |
| bible_extractor.py Dead Code 삭제 | `46c4801` |
| Context Caching 구현 (chief_writer·director_continuity 사용 중) | 기존 |
| **V70.1 Arc 간 상태 연속성 검사** — `director.yaml` 프롬프트 | ⚠️ 프롬프트만 완료, 데이터 파이프라인 미구현 (Step 5-A) |

---

## ⚠️ 현재 상태

- **God Object**: stage2 `self.app` 341건 / stage4 312건 = **653건 건재**
- **memory_engine.py**: ChromaDB 비활성, import 시 에러
- **writer.py**: 레거시이지만 stage4에서 유틸 3개 직접 호출 중

---

## 📋 실행 순서 (위에서 아래로)

### Step 1. Settings YAML + 모델 티어 중앙화 `[SAFE]` ← Phase 5-B

| 항목 | 현재 | 목표 |
|------|------|------|
| 장르 가드 | `genre_guards/*.py` 하드코딩 | `config/genres/*.yaml` |
| 작품 가드 | 코드 내 분산 | `config/genres/{genre}/{작품}.yaml` |
| 모델 티어 | 파일마다 산재 | `config/models.yaml` |
| 시스템 파라미터 | `.env` + 하드코딩 | `config/system.yaml` |
| 시점 설정 | V70 구현됨, YAML 미연동 | 작품 가드 YAML 연동 |

**참고**: `참고자료.md` L1643~1676, L1467~1496

---

### Step 2. Pydantic 모델 도입 `[RISKY]` ← Phase 2-A

| 대상 | 파일 |
|------|------|
| Arc 데이터 (bare dict) | `stage2_orchestrator.py` |
| Blueprint 데이터 | `stage2_orchestrator.py` |
| 원고 응답 | `chief_writer.py` → `stage4` |
| NPC 레지스트리 | `state_tracker.py` |

**전제**: Step 1 후
**함께**: `writer.py` 레거시 유틸 3개 이전 (CLAUDE.md L53)

---

### Step 3. ABC/Protocol 인터페이스 `[RISKY]` ← Phase 2-C

에이전트 20+개에 공통 인터페이스. 현재 각자 다른 메서드명.
**참고**: `참고자료.md` L1887~1902
**전제**: Step 2 후

---

### Step 4. ChromaDB → sqlite-vec `[RISKY]` ← Phase 2.5

| 파일 | 현재 | 목표 |
|------|------|------|
| `memory_engine.py` | chromadb (비활성) | sqlite-vec |
| `blueprint_memory.py` | chromadb | sqlite-vec |
| `semantic_plot_guard.py` | genai 직접 호출 | 통합 벡터 레이어 |

**전제**: Step 2 후

---

### Step 5. 검증 강화 + 수정 모드 + 품질 시스템 `[RISKY]` ← Phase 3

가장 큰 단계. 6개 서브 영역:

#### 5-A. NPC + 검증 핵심

| 항목 | 파일 | 변경 |
|------|------|------|
| **NPC 이력 영속화** | `relationship_tracker.py` | in-memory → DB |
| **V70.1 Arc 상태 스냅샷 주입** | `stage2_orchestrator.py` → `director_auditor.py` | `prev_arc_context`에 구조화된 NPC 생사/아이템/부상/관계/위치 데이터 추가 |
| Chain 2 graceful degradation | `state_tracker.py` | entity_registry=None 처리 |
| Chain 3 SKIP 반환 | `director_continuity.py` | "PASS" → "SKIP(검증불가)" |
| 에피소드 롤백 NPC 되감기 | `project_manager.py` | `auto_backtrack_v35()` 확장 |
| 사망 캐릭터 부활 방지 | `pre_llm_validator.py` | deceased 하드 REJECT |

**참고**: `참고자료.md` 3-C (NPC 시나리오 24개)

#### 5-B. 수정 모드 (피드백 기반 부분 수정)

| 점수 | 동작 |
|------|------|
| < 50 | 전체 재작성 |
| 50~80 | **수정 모드** — 기존 결과물 + 수정 지시만 전달 |
| > 80 | 통과 |

Stage 2 (Arc/Blueprint) + Stage 4 (원고) 모두 적용 + 수정 diff 뷰
**참고**: `참고자료.md` L1280~1318

#### 5-C. 독자 대리만족 프레임워크

Director 심사에 대리만족 점수 축 추가, 좌절-보상 타이머, 에피소드 자동 태깅
**참고**: `참고자료.md` L1548~1610

#### 5-D. 품질 모니터링

| 항목 | 추가 내용 |
|------|----------|
| 품질 회귀 감지 | 에피소드 N vs N+1 비교 |
| 온도 자동 조절 | `pass_rate_monitor` 연동 |
| 토큰 예산 추적 | `quality_dashboard` 확장 |
| 드라이런 모드 | `stage4` `dry_run` 플래그 |

#### 5-E. 콘텐츠 품질 체크

| 항목 | 추가 내용 |
|------|----------|
| NPC 과잉 등장 경고 | 신규 NPC 3명/화 초과 시 경고 |
| **크로스 에피소드 반복 감지** | DB 인덱싱 + 에피소드 간 n-gram 비교 |
| 클리프행어 패턴 추적 | 엔딩 패턴 분류 추가 |
| 대사 화자 명확성 | 화자 없는 대사 3줄+ 경고 |
| 스크린타임 밸런스 | NPC 단어 수 추적 확장 |

#### 5-F. Stage 4 확장

멀티 드래프트 비교, 컨텍스트 압축 고도화, 인간 편집 학습, 댓글/여론 반응 생성

---

### Step 6. E2E 테스트 `[SAFE]` ← Phase 6-B

| 항목 | 내용 |
|------|------|
| Smoke Test | Stage 0→2→4 1화 통과 |
| NPC 연속성 | 3화+ NPC 등장/퇴장 검증 |
| 에러 복원 | API 장애 시뮬레이션 |

---

### Step 7. God Object 분해 + async 통일 `[RISKY, 대규모]` ← Phase 4

| 항목 | 현재 | 목표 |
|------|------|------|
| `self.app` 341건 | `stage2_orchestrator.py` | DI 패턴 |
| `self.app` 312건 | `stage4_orchestrator.py` | DI 패턴 |
| stage4 단일 try | 1354줄 try-except | 함수 분리 |
| 대형 파일 분할 | 2134줄 / 1633줄 | 서브모듈화 |
| async 혼용 | ThreadPoolExecutor | `asyncio.to_thread()` 통일 |

**전제**: Step 6(E2E) 이후. 회귀 테스트 없이 분해하면 크래시 위험.

---

## 요약

```
Step 1  Settings YAML         [SAFE]     독립적, 바로 착수 가능
Step 2  Pydantic + writer.py  [RISKY]    Step 1 이후
Step 3  ABC/Protocol          [RISKY]    Step 2 이후
Step 4  sqlite-vec            [RISKY]    Step 2 이후
Step 5  검증+수정모드+품질    [RISKY]    가장 큰 Phase (서브 6개)
Step 6  E2E 테스트            [SAFE]     Step 5 이후
Step 7  God Object 분해       [RISKY]    Step 6 이후 (최후)
```
