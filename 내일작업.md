# 내일 작업 (Opus TF — Claude Code 세션 필요)

> **기준**: 다중 파일 연쇄 수정, 구조적 리팩토링, 동작 변경 수반, 회귀 테스트 필수
> **작성일**: 2026-02-13

---

## 🔴 Phase 5-A' — PromptLoader import 전환 (최우선, SAFE~MEDIUM)

> 코덱스 1순위 권장, CLAUDE.md 미착수 상태

**목표**: `_prompts import` 제거 → 호출부를 `PromptLoader.load()`로 통일

| # | 파일 | 전환 심볼 수 | 난이도 |
|---|------|-------------|--------|
| 1 | `arc_ensemble.py` | 1 (상수) | ★☆☆ |
| 2 | `blueprint_ensemble.py` | 1 (상수) | ★☆☆ |
| 3 | `director_ensemble.py` | 1 (상수) | ★☆☆ |
| 4 | `director_continuity.py` | 1 (상수) | ★☆☆ |
| 5 | `director_auditor.py` | 2 (상수) | ★★☆ |
| 6 | `analyst.py` | 9 (상수+함수) | ★★★ |
| 7 | `chief_writer.py` | 8 (상수+함수) | ★★★ |

**완료 기준 (DoD)**:
- `rg "_prompts import" modules/domain/agents` → 0건
- `python -m compileall modules/domain/agents` → 에러 0
- prompt key 오타 확인

---

## 🔴 Phase 2-A — Pydantic 모델 도입 (RISKY)

**목표**: 핵심 입출력 dict → Pydantic 모델로 승격

| 영역 | 대상 | 비고 |
|------|------|------|
| Arc 데이터 | `stage2_orchestrator` | 현재 bare dict |
| Blueprint 데이터 | `stage2_orchestrator` | 현재 bare dict |
| 원고 응답 | `chief_writer` → `stage4` | JSON 스키마 이미 부분 정의 |
| NPC 레지스트리 | `state_tracker` | 타입 불일치 다수 |

**전제**: Phase 5-A' 완료 후

---

## 🔴 Phase 2.5 — ChromaDB → sqlite-vec 전환 (RISKY)

**목표**: 벡터 저장/검색을 SQLite SSOT로 통합

| 파일 | 현재 | 변경 |
|------|------|------|
| `memory_engine.py` | chromadb 의존 | sqlite-vec |
| `blueprint_memory.py` | chromadb 의존 | sqlite-vec |
| `semantic_plot_guard.py` | genai 임베딩 직접 호출 | 통합 벡터 레이어 |

**전제**: Phase 2-A 완료 후

---

## 🟡 Phase 3 — 검증 강화 / NPC 이력 (RISKY)

**목표**: NPC 연속성 보강, 수정 모드, 검증 실패 가시화

| 항목 | 내용 |
|------|------|
| NPC 이력 추적 | 현재 `relationship_tracker`는 in-memory only → DB 영속화 |
| Chain 2 수정 | `extract_cumulative_state` 실패 시 entity_registry=None → graceful degradation |
| Chain 3 수정 | `director_continuity` 3종 검증 실패 시 "PASS" 반환 → "SKIP(검증불가)" 반환 |
| 수정 모드 | 유저가 특정 에피소드만 재생성할 수 있는 선택적 재실행 |

**참고**: `참고자료.md` 3-C NPC 시나리오 24개 참조

---

## 🟡 Phase 4 — God Object 분해 (RISKY, 대규모)

> AG 확인: `self.app` 이미 0건이므로 **일부 해소됨**

| 항목 | 내용 | 현재 상태 |
|------|------|----------|
| self.app 의존 | DI 분리 | ✅ 0건 (해소) |
| stage4 1354줄 단일 try | 함수 분리 | ❌ 미착수 |
| stage2/4 대형 파일 분할 | 서브모듈화 | ❌ 미착수 |
| ThreadPoolExecutor 정리 | 공통 래퍼 | ❌ (5/6 양호, 1건 미확인) |

---

## 🟡 Phase 5-B — Settings YAML 통합 (MEDIUM)

**목표**: 장르 가드/작품 설정을 YAML 구조로 외부화

| 항목 | 내용 |
|------|------|
| 장르 공통 설정 | `config/genres/common.yaml` |
| 작품별 오버레이 | `config/genres/{genre}/{작품}.yaml` |
| 장르 가드 외부화 | `genre_guards/*.py` → YAML 룰 기반 |

---

## 🟢 Phase 6-B — E2E 테스트 (SAFE, Opus 추천)

**목표**: 소규모 시나리오(1~2화) 파이프라인 통합 검증

| 항목 | 내용 |
|------|------|
| Smoke Test | Stage 0→2→4 1화 통과 |
| NPC 연속성 | 3화 이상에서 NPC 등장/퇴장 검증 |
| 에러 복원 | API 장애 시뮬레이션 → graceful degradation |

---

## 실행 순서 권장

```
1. Phase 5-A' (PromptLoader 전환)     ← 최우선, 리스크 低
2. Phase 5-B (Settings YAML)           ← 5-A' 이후
3. Phase 2-A (Pydantic)                ← 구조 변경 시작
4. Phase 2.5 (sqlite-vec)              ← 벡터 통합
5. Phase 3 (NPC/검증 강화)             ← 품질 안정화
6. Phase 4 (stage4 분할 등 잔여)       ← 대규모 리팩토링
7. Phase 6-B (E2E)                     ← 최종 검증
```
