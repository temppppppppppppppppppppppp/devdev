# 내일 작업 (Opus TF — Claude Code 세션)

> **프로젝트**: 글도비 — 웹소설 자동 생산 파이프라인 (Stage 0→2→4)
> **작업일**: 2026-02-14
> **마지막 갱신**: 2026-02-13 14:02

---

## 🔄 오늘(02-13) 완료된 것 — 이미 끝남, 중복 작업 금지

| 항목 | 내용 | 커밋 |
|------|------|------|
| Phase 5-A' PromptLoader 전환 | 7파일 전부 완료 (`rg "_prompts import" modules/domain/agents` → 0건) | `46c4801` |
| silent pass → warning | `hud_utils.py`, `prompt_builder.py`(5건), `stage2_orchestrator.py` | `46c4801` |
| stage4 voice/foreshadow | `save_to_json`을 `try` 안으로 이동, `logging.warning` 적용 | `46c4801` |
| genre_hud_manager 공통화 | 1281줄 → 702줄, 부모에 `update_physical_status`/`pro_root`/`pro_data` 통합 | `46c4801` |
| bible_extractor.py 삭제 | Dead Code (598줄), 외부 참조 0건 확인 후 삭제 | `46c4801` |
| CLAUDE.md 갱신 | `self.app` 341/312건 갱신, semantic_plot_guard lazy init 메모 추가 | `46c4801` |

---

## ⚠️ 현재 코드베이스 상태 (반드시 읽기)

### 주요 참고 문서
- `CLAUDE.md` — 프로젝트 전체 규칙, 핵심 파일 목록, God Object 현황
- `참고자료.md` — 모듈 분석, 위험 시나리오, NPC 연속성 24개 시나리오
- `AG_조사_modules.md` — modules/ 전체 정밀 조사 결과

### God Object 문제 (건재함)
- `modules/core/stage2_orchestrator.py` (2134줄) — `self.app` **341건**
- `modules/core/stage4_orchestrator.py` (1633줄) — `self.app` **312건**
- 합계 653건. DI 미분리 상태. 단순 치환 불가, 구조적 리팩토링 필요.

### 핵심 아키텍처
```
Stage 0 (프로젝트 초기화)
  → Stage 2 (Arc 전술 설계) — stage2_orchestrator.py
    → Stage 4 (원고 집필) — stage4_orchestrator.py
        → Director 면담 (3회) → 냉동인간 → 데이터 정산
```

---

## 📋 내일 작업 목록 (우선순위 순)

### 1. 🟡 Phase 5-B — Settings YAML 통합 + 모델 티어 중앙화 (MEDIUM)

**목표**: 장르 가드/작품 설정 + 시스템 설정을 YAML 구조로 외부화

| 항목 | 현재 파일 | 변경 |
|------|----------|------|
| 장르 공통 설정 | `modules/core/genre_guards.py` 하드코딩 | → `config/genres/common.yaml` |
| 작품별 오버레이 | 코드 내 분산 | → `config/genres/{genre}/{작품}.yaml` |
| 장르 가드 외부화 | `genre_guards/*.py` Python 로직 | → YAML 룰 기반 |
| **모델 티어 매핑** | 파일마다 모델명 산재 | → `config/models.yaml` 중앙 관리 |
| **시스템 파라미터** | `.env` + 하드코딩 혼재 | → `config/system.yaml` (토큰 한도, 재시도 등) |

**시작점**: `modules/core/genre_guards.py` 분석 후 추출 범위 결정
**참고**: `참고자료.md` L1643~1676 (5-B + 5-B' 상세 구조)

**DoD**:
- `config/genres/` + `config/models.yaml` + `config/system.yaml` 생성
- 최소 1개 장르(wuxia) YAML 완성
- 기존 테스트 `tests/test_genre_guard.py` 통과
- `python -m compileall modules/ -q` 에러 0건

---

### 2. 🟢 Context Caching 활성화 (SAFE, 독립 작업)

**목표**: `base_agent.py`의 `_context_caches` 필드 활성화 → Gemini 토큰 절감

| 항목 | 현재 | 변경 |
|------|------|------|
| `base_agent.py` `_context_caches` | 필드만 존재, 미활성 | 캐시 생성/사용 로직 구현 |

**참고**: CLAUDE.md L56, L89

**DoD**:
- 캐싱 ON/OFF 설정 가능
- 기존 동작에 영향 없음 (캐시 미적중 시 기존 로직으로 fallback)

---

### 3. 🔴 Phase 2-A — Pydantic 모델 도입 (RISKY)

**목표**: 핵심 입출력 dict → Pydantic 모델로 승격

| 영역 | 현재 파일 | 비고 |
|------|----------|------|
| Arc 데이터 | `modules/core/stage2_orchestrator.py` | bare dict, 키 불일치 빈번 |
| Blueprint 데이터 | `modules/core/stage2_orchestrator.py` | bare dict |
| 원고 응답 | `modules/domain/agents/chief_writer.py` → `stage4` | JSON 스키마 이미 부분 정의 |
| NPC 레지스트리 | `modules/domain/agents/state_tracker.py` | 타입 불일치 다수 |

**전제**: Phase 5-B 완료 후 착수 권장
**위험**: stage2/stage4 전반에 영향 → 반드시 단계적 도입

**DoD**:
- `modules/models/` 디렉토리에 Pydantic 모델 정의
- 최소 1개 모델(Arc) 적용 + 기존 동작 유지
- `python -m compileall modules/ -q` 에러 0건

---

### 4. 🔴 Phase 2-C — ABC/Protocol 인터페이스 (RISKY)

**목표**: 에이전트 20+개에 공통 인터페이스 정의

| 항목 | 현재 | 변경 |
|------|------|------|
| 에이전트 인터페이스 | 각자 다른 메서드명 (generate/write_episode/analyze 등) | `AgentProtocol` 통일 |
| 타입 체크 | 불가 | Protocol 기반 정적 검증 가능 |

**참고**: `참고자료.md` L1887~1902 (현재 vs 개선 코드 예시)
**전제**: Phase 2-A(Pydantic) 완료 후

---

### 5. 🔴 Phase 2.5 — ChromaDB → sqlite-vec 전환 (RISKY)

**목표**: 벡터 저장/검색을 SQLite SSOT로 통합

| 파일 | 현재 | 변경 |
|------|------|------|
| `modules/core/memory_engine.py` | chromadb 의존 (비활성 상태) | sqlite-vec |
| `modules/core/blueprint_memory.py` | chromadb 의존 | sqlite-vec |
| `modules/core/semantic_plot_guard.py` | genai 임베딩 직접 호출 | 통합 벡터 레이어 |

**전제**: Phase 2-A 완료 후
**위험**: 외부 의존성 변경, 데이터 마이그레이션 필요
**참고**: CLAUDE.md L54 — `memory_engine.py` 현재 비활성, import하면 에러남

---

### 6. 🟡 Phase 3 — 검증 강화 / NPC 이력 (RISKY)

**목표**: NPC 연속성 보강, 수정 모드, 검증 실패 가시화

| 항목 | 현재 파일 | 변경 |
|------|----------|------|
| NPC 이력 추적 | `modules/domain/agents/relationship_tracker.py` | in-memory → DB 영속화 |
| Chain 2 수정 | `state_tracker.py` `extract_cumulative_state` | 실패 시 entity_registry=None → graceful degradation |
| Chain 3 수정 | `modules/domain/agents/director_continuity.py` | 3종 검증 실패 시 "PASS" → "SKIP(검증불가)" |
| 수정 모드 | `stage4_orchestrator.py` | 유저가 특정 에피소드만 재생성 |
| **에피소드 롤백 NPC 되감기** | `project_manager.py` `auto_backtrack_v35()` | NPC 상태 되감기 추가 필요 |

**참고**: `참고자료.md` 섹션 3-C에 NPC 시나리오 24개, CLAUDE.md L87 에피소드 롤백

---

### 7. 🟡 writer.py 레거시 유틸 이전 (MEDIUM)

**목표**: `writer.py`에서 stage4가 직접 호출하는 유틸리티 3개를 정리

| 항목 | 현재 | 변경 |
|------|------|------|
| `writer.py` 유틸 3개 | stage4에서 직접 import | 적절한 모듈로 이전 |
| `writer.py` 본체 | 레거시 | 이전 완료 후 삭제 |

**참고**: CLAUDE.md L53
**시기**: Phase 2-A 작업 시 함께 처리 가능

---

### 8. 🔴 Phase 4 — God Object 분해 + async 통일 (RISKY, 대규모)

**목표**: `self.app` 의존성(653건) 분해, 대형 파일 분할, async 패턴 통일

| 항목 | 현재 | 목표 | 상태 |
|------|------|------|------|
| `self.app` 341건 | `stage2_orchestrator.py` | DI 패턴 적용 | ❌ |
| `self.app` 312건 | `stage4_orchestrator.py` | DI 패턴 적용 | ❌ |
| stage4 단일 try 블록 | 1354줄 짜리 try-except | 함수 분리 | ❌ |
| stage2/4 대형 파일 분할 | 2134줄 / 1633줄 | 서브모듈화 | ❌ |
| **async/sync 혼용** | `main_a.py`, `stage2`, `batch_validator` | ThreadPoolExecutor → `asyncio.to_thread()` 통일 | ❌ |

**참고**: `참고자료.md` L1917~1926 (async 혼용 현황)
**위험**: 가장 큰 리팩토링. 회귀 테스트 체계 먼저 필요 (Phase 6-B)

---

### 9. 🟢 Phase 6-B — E2E 테스트 (SAFE)

**목표**: 소규모 시나리오(1~2화) 파이프라인 통합 검증

| 항목 | 내용 |
|------|------|
| Smoke Test | Stage 0→2→4 1화 통과 |
| NPC 연속성 | 3화 이상에서 NPC 등장/퇴장 검증 |
| 에러 복원 | API 장애 시뮬레이션 → graceful degradation |

**기존 테스트**: `tests/` 디렉토리에 63개 pytest, `tests/stage4_v2_test/` 통합 테스트

---

## 실행 순서 권장

```
1. Phase 5-B (Settings YAML + 모델 티어)  ← 리스크 低, 독립적
2. Context Caching 활성화                  ← SAFE, 독립적, 토큰 절감
3. Phase 2-A (Pydantic)                    ← 구조 변경 시작
4. Phase 2-C (ABC/Protocol)                ← 2-A 이후
5. Phase 2.5 (sqlite-vec)                  ← 벡터 통합
6. Phase 3 (NPC/검증/롤백)                 ← 품질 안정화
7. writer.py 유틸 이전                     ← Phase 2-A 시 함께
8. Phase 6-B (E2E 테스트)                  ← 회귀 방지 체계
9. Phase 4 (God Object + async 통일)       ← E2E 있어야 안전
```

> **주의**: Phase 4(God Object)는 Phase 6-B(E2E) 이후에 하는 것이 안전. stage2/stage4가 2000줄+ 이므로 회귀 테스트 없이 분해하면 크래시 위험.
